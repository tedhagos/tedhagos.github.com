<?xml version="1.0" encoding="utf-8"?>

<feed xmlns="http://www.w3.org/2005/Atom">
 
 <title>Ted's blog'</title>
 <link href="http://tedhagos.com/feed/" rel="self"/>
 <link href="http://tedhagos.com/"/>
 <updated>2015-04-27T00:15:51+08:00</updated>
 <id>http://tedhagos.com/</id>
 <author>
   <name>Ted Hagos</name>
   <email>tedhagos@gmail.com</email>
 </author>

 
 <entry>
   <title>setup</title>
   <link href="http://tedhagos.com/vagrant/setup.html"/>
   <updated>2015-04-26T00:00:00+08:00</updated>
   <id>http://tedhagos.com/vagrant/setup</id>
   <content type="html">&lt;p&gt;You can install vagrant using precompiled binaries. Head over to &lt;a href=&quot;http://www.vagrantup.com/downloads.html&quot;&gt;vagrantup.com&lt;/a&gt; website and download the installer appropriate for your platform. These are wizard type installers, so you will install them in a familiar way. Download, then double click, then follow the prompts.&lt;/p&gt;

&lt;p&gt;Alternatively, you can use the package manager of your platform. OSX has either MacPorts or Homebrew. Windows has got chocolatey and Debian derived Linuxes has got the aptitude package manager.&lt;/p&gt;

&lt;p&gt;On OSX homebrew, you could install vagrant this way&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;brew update
brew tap caskroom/cask
brew install brew-cask
brew cask install vagrant virtualbox
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;On a Windows machine with chocolatey, you can do it this way&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;cinst --y virtualbox vagrant
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;On Debian derived Linux box, you might do the following&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;sudo apt-get update
sudo apt-get upgrade
sudo apt-get install vagrant virtualbox-dkms
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Make sure everything has been installed properly. On terminal window, run the following commands&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;vagrant --v
vboxmanage - v
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If you saw the actual version numbers of vagrant and virtualbox instead of getting a “command not found” or “bad command or filename” error, that means virtualbox and vagrant are good to go.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>repl</title>
   <link href="http://tedhagos.com/nodejs/repl.html"/>
   <updated>2015-04-26T00:00:00+08:00</updated>
   <id>http://tedhagos.com/nodejs/repl</id>
   <content type="html">&lt;p&gt;NodeJS includes a REPL utility. The Read Eval Print Loop is a pretty good way to start exploring node. You can type any node command in the repl and it will automatically spit out the results of what you typed. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ node
&amp;gt; 
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The chevron sign indicates that the repl is ready to take on commands. Node is javascript, so you can type valid JS commands and the repl will evaluate it. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ node
&amp;gt; new Date();
&amp;gt; Sun Apr 26 2015 23:01:58 GMT+0800
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You can even type functions in it.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ node
&amp;gt; var numarray = [1,2,3,4];
undefined
&amp;gt; function cube(num) {
... return Math.pow(num,3);
... }
undefined
&amp;gt; numarray.map(cube);
[ 1, 8, 27, 64 ]
&amp;gt; 
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The repl is just a good tool to have if you want to quickly test a statement or an expression. It allows instant feedback if a piece of code will work or not without you having to construct a proper source file. &lt;/p&gt;

&lt;p&gt;You can end the repl session by pressing &lt;code&gt;CTRL C&lt;/code&gt; twice on the terminal.&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>jpanelmenu</title>
   <link href="http://tedhagos.com/jquery/jquery-panel-scrollbar.html"/>
   <updated>2015-04-26T00:00:00+08:00</updated>
   <id>http://tedhagos.com/jquery/jquery-panel-scrollbar</id>
   <content type="html">&lt;p&gt;Download the jpanelmenu javascript file from &lt;a href=&quot;http://jpanelmenu.com&quot;&gt;jpanelmenu.com&lt;/a&gt; and include it in your page. Don’t forget to include JQuery as well, JPanelMenu needs JQuery.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;head&amp;gt;
  &amp;lt;script src=&quot;../js/jquery-1.11.2.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;script src=&quot;../js/jquery.jpanelmenu.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;link rel=&quot;stylesheet&quot; type=&quot;text/css&quot; href=&quot;/css/style.css&quot; /&amp;gt;
&amp;lt;/head&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Structure your webpage like you normally would. JPanelMenu will monkey around with the DOM anyway. It will restructure the DOM during runtime and put its own div wrappers, so don’t sweat your static structure so much. Make it as simple as possible. Having said that, JPanel will look for an element with an &lt;code&gt;id=menu&lt;/code&gt;, this is the part of page that it will hide. Change the display property of that element to &lt;strong&gt;none&lt;/strong&gt;.&lt;/p&gt;

&lt;p&gt;JPanelMenu will also look for an element whose &lt;code&gt;class=menu-trigger&lt;/code&gt;. This will be some sort of a hotspot or a button which when clicked, will trigger the sliding of the menu div.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;body&amp;gt;
&amp;lt;div id=&quot;menu&quot; style=&quot;display:none;&quot;&amp;gt;
  &amp;lt;ul&amp;gt;
    &amp;lt;li&amp;gt;&amp;lt;a href=&quot;#&quot;&amp;gt;Menu 1&amp;lt;/a&amp;gt;&amp;lt;/li&amp;gt;
    &amp;lt;li&amp;gt;&amp;lt;a href=&quot;#&quot;&amp;gt;Menu 2&amp;lt;/a&amp;gt;&amp;lt;/li&amp;gt;
    &amp;lt;li&amp;gt;&amp;lt;a href=&quot;#&quot;&amp;gt;Menu 3&amp;lt;/a&amp;gt;&amp;lt;/li&amp;gt;
    &amp;lt;li&amp;gt;&amp;lt;a href=&quot;#&quot;&amp;gt;Menu 4&amp;lt;/a&amp;gt;&amp;lt;/li&amp;gt;
  &amp;lt;/ul&amp;gt;
&amp;lt;/div&amp;gt;

&amp;lt;div id=&quot;wrapper&quot;&amp;gt;
  &amp;lt;header&amp;gt;
    &amp;lt;span class=&quot;menu-trigger&quot; style=&quot;cursor:pointer;&quot;&amp;gt;&amp;amp;#9776&amp;lt;/span&amp;gt;
  &amp;lt;/header&amp;gt;
  &amp;lt;article&amp;gt;&amp;lt;/article&amp;gt;
  &amp;lt;footer&amp;gt;&amp;lt;/footer&amp;gt;
&amp;lt;/div&amp;gt;
&amp;lt;/body&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Lastly, to activate JPanelMenu, you need to instantiate and turn it on.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;script type=&quot;text/javascript&quot;&amp;gt;
  $(document).ready(function(){
    var jPM = $.jPanelMenu();
    jPM.on();
  });
&amp;lt;/script&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;A basic structure of a whole page that uses the the JPanelMenu might look something like the html code sample below&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;!DOCTYPE HTML&amp;gt;
&amp;lt;html lang=&quot;en&quot;&amp;gt;
&amp;lt;head&amp;gt;
  &amp;lt;script src=&quot;../js/jquery-1.11.2.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;script src=&quot;../js/jquery.jpanelmenu.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
  &amp;lt;link rel=&quot;stylesheet&quot; type=&quot;text/css&quot; href=&quot;/css/style.css&quot; /&amp;gt;
&amp;lt;/head&amp;gt;
&amp;lt;body&amp;gt;
&amp;lt;div id=&quot;menu&quot; style=&quot;display:none;&quot;&amp;gt;
  &amp;lt;ul&amp;gt;
    &amp;lt;li&amp;gt;&amp;lt;a href=&quot;#&quot;&amp;gt;Menu 1&amp;lt;/a&amp;gt;&amp;lt;/li&amp;gt;
    &amp;lt;li&amp;gt;&amp;lt;a href=&quot;#&quot;&amp;gt;Menu 2&amp;lt;/a&amp;gt;&amp;lt;/li&amp;gt;
    &amp;lt;li&amp;gt;&amp;lt;a href=&quot;#&quot;&amp;gt;Menu 3&amp;lt;/a&amp;gt;&amp;lt;/li&amp;gt;
    &amp;lt;li&amp;gt;&amp;lt;a href=&quot;#&quot;&amp;gt;Menu 4&amp;lt;/a&amp;gt;&amp;lt;/li&amp;gt;
  &amp;lt;/ul&amp;gt;
&amp;lt;/div&amp;gt;

&amp;lt;div id=&quot;wrapper&quot;&amp;gt;
  &amp;lt;header&amp;gt;
    &amp;lt;span class=&quot;menu-trigger&quot; style=&quot;cursor:pointer;&quot;&amp;gt;&amp;amp;#9776&amp;lt;/span&amp;gt;
  &amp;lt;/header&amp;gt;
  &amp;lt;article&amp;gt;&amp;lt;/article&amp;gt;
  &amp;lt;footer&amp;gt;&amp;lt;/footer&amp;gt;
&amp;lt;/div&amp;gt;

&amp;lt;script type=&quot;text/javascript&quot;&amp;gt;
  $(document).ready(function(){
    var jPM = $.jPanelMenu();
    jPM.on();
  });
&amp;lt;/script&amp;gt;

&amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;styling&quot;&gt;Styling&lt;/h1&gt;

&lt;p&gt;JPanelMenu will rewrite the DOM of the page. So if you think you can style the sliding panel by using the &lt;code&gt;#menu&lt;/code&gt; selector, that won’t work. If you inspect the elements of the page using firebug or the chrome developer tools, you will find that the sliding panel is actually enclosed in an element named &lt;code&gt;#jPanelMenu-menu&lt;/code&gt;. This is the element you need to style. Code below is a snippet taken from &lt;code&gt;style.css&lt;/code&gt;.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;#jPanelMenu-menu {
  border-right: 1px solid #dbdbdb;
  margin: 5px;
  text-align: center;
  line-height: 2em;
  background: #fff;
}
#menu {
  padding: 10px;
}
  .menu-trigger {font-size: 3em; cursor: pointer; color: #979797;}

#jPanelMenu-menu a {
  text-decoration: none;
  color: #838383;
}
#jPanelMenu-menu a:hover {
  text-decoration: underline;
  color: #000;
}
#jPanelMenu-menu ul {
  list-style-type: none;
  margin:0;
  padding:0;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The code above changed the style of the unordered list and anchor tags because the sliding panel was intended to hide a menu navigation panel. The navigation panel is built by a bunch of anchor tags enumerated using an unordered list.&lt;/p&gt;

&lt;h2 id=&quot;remove-the-scrollbar&quot;&gt;Remove the scrollbar&lt;/h2&gt;

&lt;p&gt;The JPanel adds a whitespace on the side, which is probably intended for a scrollbar. To remove the whitespace without removing the scrolling ability of the panel, you need to edit the &lt;code&gt;jquery.jpanelmenu.js&lt;/code&gt;&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;Find the line &lt;code&gt;overflow-y:scroll&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;Replace it with &lt;code&gt;overflow-y:visible&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
</content>
 </entry>
 
 <entry>
   <title>jquery basics</title>
   <link href="http://tedhagos.com/jquery/basics.html"/>
   <updated>2015-04-26T00:00:00+08:00</updated>
   <id>http://tedhagos.com/jquery/basics</id>
   <content type="html">&lt;p&gt;JQuery is pretty good at a lot of things, the most usage though is DOM selection and manipulation and event handling. To get it going, we simply include it in a page using the &lt;strong&gt;&amp;lt;script&amp;gt;&lt;/strong&gt; tag. First, you need to download it from &lt;a href=&quot;https://jquery.com&quot;&gt;JQuery’s home page&lt;/a&gt;. Save it somewhere inside the docroot of your web project. Then reference it from the &lt;strong&gt;&amp;lt;script&amp;gt;&lt;/strong&gt; tag.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;head&amp;gt;
  &amp;lt;script src=&quot;js/jquery-1.11.2.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
&amp;lt;/head&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;In the example above, the JQuery js file is stored inside the &lt;strong&gt;js&lt;/strong&gt; folder and the html page is on the top folder of docroot, hence we need to reference the js file relative to the location of the webpage that is defining the inclusion of the jquery library.&lt;/p&gt;

&lt;p&gt;Alternatively, you can include jquery in your webpage by loading it using a CDN, a content delivery network. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;script src=&quot;//code.jquery.com/jquery-1.11.2.min.js&quot;&amp;gt;&amp;lt;/script&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;basic-usage&quot;&gt;Basic Usage&lt;/h1&gt;

&lt;p&gt;Before doing anything on the DOM, you need to make sure that all DOM elements have already been loaded and rendered. This is why our JQuery codes will look like this&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;script&amp;gt;
$(document).ready(function(){
  // DOM manipulation and other codes here .
});
&amp;lt;/script&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Before JQuery, your code might have looked like this&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;script&amp;gt;
body.onload = function(){
  // DOM manipulation and other codes here .
}
&amp;lt;/script&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You can use JQuery as a drop-in replacement for these kinds of codes.&lt;/p&gt;

&lt;h1 id=&quot;hunting-for-elements&quot;&gt;Hunting for elements&lt;/h1&gt;

&lt;p&gt;In traditional javascript, we used codes like &lt;/p&gt;

&lt;p&gt;&lt;code&gt;var b = document.getElementById(&#39;btnLogin&#39;);&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;in order to get a programmatic reference to an html element with an &lt;strong&gt;id=btnLogin&lt;/strong&gt;. To do the same task in jquery, we will use codes like this&lt;/p&gt;

&lt;p&gt;&lt;code&gt;var b = $(&quot;#btnLogin&quot;);&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;JQuery uses CSS like syntax. That is why there is a pound/sharp sign before &lt;strong&gt;btnLogin&lt;/strong&gt;. If you have constructed CSS files before, the jquery syntax for element ids and names will be familiar to you. &lt;/p&gt;

&lt;h1 id=&quot;common-usage&quot;&gt;Common Usage&lt;/h1&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;body&amp;gt;
&amp;lt;button id=&quot;btnLogin&quot;&amp;gt;Login&amp;lt;/button&amp;gt;
&amp;lt;script&amp;gt;

$(document).ready(function(){
  // get a DOM reference to the login button 
  var blogin = $(&quot;#btnlogin&quot;);
  blogin.click(function(){
    alert(&quot;Button click&quot;);
  });

  // detect browser type
  
});

&amp;lt;/script&amp;gt;
&amp;lt;/body&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;binding-events&quot;&gt;Binding events&lt;/h1&gt;

&lt;h1 id=&quot;references&quot;&gt;References&lt;/h1&gt;

</content>
 </entry>
 
 <entry>
   <title>How to Include other scripts</title>
   <link href="http://tedhagos.com/php/howto-include-other-scripts.html"/>
   <updated>2015-03-26T00:00:00+08:00</updated>
   <id>http://tedhagos.com/php/howto-include-other-scripts</id>
   <content type="html">&lt;p&gt;There are four ways to include other php scripts into the current file. PHP has four statements that allows us to essentially copy the contents of another php script and embed it on the script currently being evaluated. These statements are&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;strong&gt;include(“scriptname.php”)&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;include_once(“scriptname.php”)&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;require(“scriptname.php”)&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;require_once(“scriptname.php”)&lt;/strong&gt;&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;p&gt;http://php.net/manual/en/function.include.php&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>PhoneGap Dev Environment</title>
   <link href="http://tedhagos.com/phonegap/installation.html"/>
   <updated>2015-03-25T00:00:00+08:00</updated>
   <id>http://tedhagos.com/phonegap/installation</id>
   <content type="html">&lt;p&gt;To install the PhoneGap SDK, you will need &lt;strong&gt;NodeJS&lt;/strong&gt; and &lt;strong&gt;npm&lt;/strong&gt;. You can download the binaries of Node which are available from the &lt;a href=&quot;http://nodejs.org&quot;&gt;nodejs.org&lt;/a&gt;. The binaries are simple to install, just install them as you would do any other software you installed on your platform. It is as simple as download the file, double click it, follow the prompts. You may to adjust your system path settings after the installation so that you can run node and npm from the command line.&lt;/p&gt;

&lt;p&gt;Alternatively, you can install node and npm using some package managers. On a Mac with Homebrew as package manager, node and npm can be installed as follows.&lt;/p&gt;

&lt;p&gt;On a terminal window, run the following command&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ brew update
$ brew doctor
$ brew install node # this will install npm as well
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;On Windows with Chocolatey as package manager, run PowerShell as Administrator and then run the following command&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;C:\&amp;gt; cinst node -y
C:\&amp;gt; cinst npm -y
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Next step is to install the PhoneGap tool chain&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ sudo npm install -g phonegap
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;On Windows, run &lt;strong&gt;cmd&lt;/strong&gt; as Adminstrator then run the command&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;C:\&amp;gt; npm install -g phonegap
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If you get a “command not found error”, the node and npm directories are probably not yet added to system path variable. Make the necessary adjustment/edits to the path variable and run the npm install command again.&lt;/p&gt;

&lt;p&gt;When the installation has finished, run &lt;code&gt;phonegap&lt;/code&gt; on a command line just to make sure everything installed properly. The result you should get from running &lt;code&gt;phonegap&lt;/code&gt; should be a help screen, not a “command not found error”&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>MAMP Installation</title>
   <link href="http://tedhagos.com/php/mamp.html"/>
   <updated>2015-03-02T00:00:00+08:00</updated>
   <id>http://tedhagos.com/php/mamp</id>
   <content type="html">&lt;p&gt;MAMP is short for Mac, Apache, MySQL and PHP. It is the WAMP (Windows Apache MySQL PHP) equivalent on Macs. The installer bundles 3 programs, a web server (Apache), a database server (MySQL) and a scripting language (PHP).&lt;/p&gt;

&lt;p&gt;You don’t need the MAMP installer in order to get the 3 programs, Apache in fact, is bundled with OSX. You can get the other two programs using eithe Fink, Brew or Macports. So why are we bothering with MAMP? Because MAMP gives an installation process that has less hassle or friction. Installing the 3 programs without MAMP involves more than just downloading and running installer programs. There is configuration that needs to be done afterwards. It is the configuration activity that usually gets beginning programmers stomped. Wiring the 3 programs to work together can prove to be daunting if it is your first time to setuy a web stack development environment. MAMP eases that configuration process for us.  &lt;/p&gt;

&lt;p&gt;&lt;strong&gt;1 Download the installer&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;The installer is at &lt;a href=&quot;http://www.mamp.info/en/downloads/&quot;&gt;mamp.info/downloads&lt;/a&gt;. The MAMP installer has 2 editions. The first one is simply called MAMP and the other is MAMP Pro. There is only one installer for both editions, so just hit the download button. After the installation, you will be asked if you want to upgrade to MAMP Pro. The differences between the two editions is beyond our scope for now. &lt;/p&gt;

&lt;p&gt;Install the downloaded file just how you would install any other Mac programs. You will be guided by a series of wizard-type dialog boxes.&lt;/p&gt;

&lt;p&gt;Click &lt;strong&gt;Continue&lt;/strong&gt; on this screen&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-1.png&quot; alt=&quot;mamp-1&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Also click &lt;strong&gt;Continue&lt;/strong&gt; on this one&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-2.png&quot; alt=&quot;mamp-2&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;2 Read the license agreement&lt;/strong&gt; and click &lt;strong&gt;Continue&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-3.png&quot; alt=&quot;mamp-3&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Agree to license agreement and click &lt;strong&gt;Continue&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-4.png&quot; alt=&quot;mamp-4&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;3 Accept the default setting&lt;/strong&gt; which is “install for all users” and click &lt;strong&gt;Continue&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-5.png&quot; alt=&quot;mamp-5&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The screen tells you how much space it will take up on the disk. It also offers a slight customization. If you want to install the program files to a folder other than the default, you may do so by clicking the &lt;strong&gt;Change Install Location&lt;/strong&gt; button. You will be asked where you would like to put the program files instead. For now, just accept the default location and click &lt;strong&gt;Continue&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-6.png&quot; alt=&quot;mamp-6&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The installer, by default will put the program files in &lt;strong&gt;/Applications/MAMP&lt;/strong&gt; folder. This is a system folder, and as such is  off-limits to users who don’t have administrator or root privileges. That is why the installer is asking for permission if you would like to allow the action. &lt;/p&gt;

&lt;p&gt;In order to proceed, you need to give your password to the installer. This is the same password that you use when logging in to your Mac. It is also the same password that you installed other applications on your Mac the past. After typing your password, click &lt;strong&gt;Install&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-7.png&quot; alt=&quot;mamp-7&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-8.png&quot; alt=&quot;mamp-8&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;4 Close&lt;/strong&gt; the installer when all files have been written to disk. You will notice that the “Continue button” has been replaced by &lt;strong&gt;Close&lt;/strong&gt; &lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-9.png&quot; alt=&quot;mamp-9&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;5 Launch MAMP&lt;/strong&gt;. Open a Finder window and click “Applications” on the side bar. If you prefer to use the Finder menu, click &lt;strong&gt;Go&lt;/strong&gt; then &lt;strong&gt;Applications&lt;/strong&gt;. Alternatively, you can also press &lt;strong&gt;Shift + CMD + A&lt;/strong&gt;, that will also take you the Applications folder. The MAMP program is inside the MAMP folder. Double click it.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-10.png&quot; alt=&quot;mamp-10&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Ignore this warning. We are not going to use MAMP Pro. Also you may want to uncheck the box that says “Check for MAMP Pro when starting MAMP”, so that the next time you launch MAMP, you won’t be bothered by warning again.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-11.png&quot; alt=&quot;mamp-11&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Click &lt;strong&gt;Launch MAMP&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-12.png&quot; alt=&quot;mamp-12&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The management screen of MAMP is a bit simplistic. Notice that Apache and MySQL has status indicators on the right upper portion of the screen, but there’s none for PHP. You won’t see PHP in there because PHP is already configured to work with Apache. Also, PHP is not a standalone program, its a scripting language, so a status indicator doesn’t really make sense for it. &lt;/p&gt;

&lt;p&gt;&lt;strong&gt;6 Configure&lt;/strong&gt; the services&lt;/p&gt;

&lt;p&gt;At the moment Apache and MySQL are not running. When they do, the status boxes will change their colors to green. Right now they are greyed out because. Do not click the &lt;strong&gt;Start Servers&lt;/strong&gt; just yet. We still have to do a little bit of configuration. To do that, click &lt;strong&gt;Preferences&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-13.png&quot; alt=&quot;mamp-13&quot; /&gt;&lt;/p&gt;

&lt;p&gt;There is not much to configure on the “Start/Stop” tab, except to check the box that says “When starting MAMP, Start servers”. This way, everytime we start MAMP, Apache and MySQL will start automatically. &lt;/p&gt;

&lt;p&gt;Next, we will configure the Webserver. Click the &lt;strong&gt;WebServer&lt;/strong&gt; tab&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-14.png&quot; alt=&quot;mamp-14&quot; /&gt;&lt;/p&gt;

&lt;p&gt;MAMP contains two webservers, one Apache and the other one is NGINX (pronounced as engine X). NGINX is relatively newer, and arguably, faster than Apache. Both are very capable webservers but they are architecturally different. The choice on what to use between the two is not always obvious because it depends on what language and other technologies you will use in your web application. A discussion on the merits or comparisson of these two webservers is beyond our scope. &lt;/p&gt;

&lt;p&gt;For our purpose, we will use Apache. Apache is already selected by default, so you don’t need to anything there. &lt;/p&gt;

&lt;p&gt;The next item we will configure is the &lt;strong&gt;Document Root&lt;/strong&gt; or docroot for short. The docroot is a special folder that holds all the files of your website or web application. When somebody goes to your website by typing its URL address on a web browser, the webserver, which in our case Apache, it will look for files to serve inside the docroot folder. The MAMP installer decided that it will put the docroot on /Applications/MAMP/htdocs, but we can change that. &lt;/p&gt;

&lt;p&gt;Changing the location of the docroot has some benefits for developoment. If we relocate it to somewhere inside our user folder, then we can add, edit, modify and delete files inside the docroot to our heart’s content without being bothered to authenticate ourselves each time. &lt;/p&gt;

&lt;p&gt;To change the location of docroot, click eithe the arrow icon or the folder with ellipsis (the one with 3 dots) right beside the &lt;strong&gt;Document Root&lt;/strong&gt; label. It will launch a Finder window and let you select a new folder which will be the new docroot.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-15.png&quot; alt=&quot;mamp-15&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Select a folder somewhere inside /Users/yourname, in my case, I am  choosing a folder inside my ~/Dropbox folder named &lt;strong&gt;cares&lt;/strong&gt;. Click the &lt;strong&gt;Select&lt;/strong&gt; button&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-17.png&quot; alt=&quot;mamp-17&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The location of the new docroot is now selected and reflected on MAMP preferences. Click &lt;strong&gt;Ok&lt;/strong&gt; to close the preferences window.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-18.png&quot; alt=&quot;mamp-18&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;7 Start MAMP&lt;/strong&gt; by click the “Start Servers” icon. Wait for a little while until you see the status boxes of Apache and MySQL turn green. As soon as the server starts, a browser session will automatically be launched for you and you will see the welcome page of MAMP.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-19.png&quot; alt=&quot;mamp-19&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The welcome page of MAMP automatically opens right after you start the servers. It contains links for &lt;strong&gt;phpinfo&lt;/strong&gt; and &lt;strong&gt;phpadmin&lt;/strong&gt;. We won’t &lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-20.png&quot; alt=&quot;mamp-20&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;8 Shutdown MAMP&lt;/strong&gt;. When you are done, you can shutdown MAMP by clicking the &lt;strong&gt;Shutdown servers&lt;/strong&gt; icon then click the &lt;strong&gt;Quit&lt;/strong&gt; button to dismiss the MAMP management window&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/mamp-21.png&quot; alt=&quot;mamp-21&quot; /&gt;&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>Working with Bytes</title>
   <link href="http://tedhagos.com/ocjp/working-with-bytes.html"/>
   <updated>2015-02-16T00:00:00+08:00</updated>
   <id>http://tedhagos.com/ocjp/working-with-bytes</id>
   <content type="html">&lt;pre&gt;&lt;code&gt;byte a = 128;
System.out.println(++a);
&lt;/code&gt;&lt;/pre&gt;

</content>
 </entry>
 
 <entry>
   <title>Hello</title>
   <link href="http://tedhagos.com/nodejs/hello.html"/>
   <updated>2015-01-29T00:00:00+08:00</updated>
   <id>http://tedhagos.com/nodejs/hello</id>
   <content type="html">&lt;p&gt;While the repl allows you to write node codes, as a programmer, you will write your commands in a source file and then run it using the node interpreter.&lt;/p&gt;

&lt;p&gt;We will create a directory where we can save our node source files and then we will run them on the node interperter.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mkdir ~/nodeplayground &amp;amp;&amp;amp; cd ~/nodeplayground
touch hello.js
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;On a Windows machine, this might be accomplished with these commands&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;mkdir c:\Users\yourname\nodeplayground
cd c:\Users\yourname\nodeplayground
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Then you can create hello.js using your program editor of choice. Node programs are javascript programs. So whatever you have used to write JS codes before, that should be fine.&lt;/p&gt;

&lt;p&gt;Edit &lt;code&gt;hello.js&lt;/code&gt; and put the following codes in it&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;var http=require(&#39;http&#39;)

app=http.createServer(sayHi);

function sayHi(request, response) {
  response.writeHead(200, {&#39;Context-Type&#39; : &#39;text/plain&#39;});
  response.end(&quot;Hello Node\n&quot;);
}

app.listen(8000, &#39;127.0.0.1&#39;);
console.log(&quot;Web Server has started&quot;);
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Choose or create a directory to save our first node program. &lt;/p&gt;

&lt;p&gt;Node has a &lt;strong&gt;REPL&lt;/strong&gt; environment where you can interactively write commands and expressions. As you type something on the REPL, it will be evaluated and interpreted by Node and results will immediately shown to you. The REPL is a good tool to use for learning Node, but most of the things we will do in Node will not be on the REPL. We will write our programs in a JavaScript file and execute that file on the Node runtime. To Demonstrat this workflow, we will write a simple program.&lt;/p&gt;

&lt;h2 id=&quot;steps&quot;&gt;Steps&lt;/h2&gt;

&lt;p&gt;&lt;strong&gt;1. Create a Node source file&lt;/strong&gt;. Node programs are JavaScript programs that run on the server side. But still, they are JavaScript programs nonetheless. Create a folder where you can store our exercise files. Ideally, this folder is inside your user folder. On a Mac this will be &lt;code&gt;/Users/yourname&lt;/code&gt; and on Windows it will be &lt;code&gt;c:\Users\yourname&lt;/code&gt;&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-javascript&quot; data-lang=&quot;javascript&quot;&gt;&lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;http&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;require&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;http&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;nx&quot;&gt;app&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;http&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;createServer&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;sayHi&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;

&lt;span class=&quot;kd&quot;&gt;function&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;sayHi&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;request&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;response&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;nx&quot;&gt;response&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;writeHead&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;200&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;Context-Type&amp;#39;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&amp;#39;text/plain&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;});&lt;/span&gt;
  &lt;span class=&quot;nx&quot;&gt;response&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;end&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&amp;quot;Hello Node\n&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;

&lt;span class=&quot;nx&quot;&gt;app&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;listen&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;8000&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&amp;#39;127.0.0.1&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;nx&quot;&gt;console&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;log&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&amp;quot;Web Server has started&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;h2 id=&quot;exercise&quot;&gt;Exercise&lt;/h2&gt;

&lt;h2 id=&quot;glossary&quot;&gt;Glossary&lt;/h2&gt;

&lt;dl&gt;
  &lt;dt&gt;Callback&lt;/dt&gt;
  &lt;dd&gt;A function call back is.&lt;/dd&gt;
  &lt;dt&gt;Idiomatic JavaScript&lt;/dt&gt;
  &lt;dd&gt;What is this&lt;/dd&gt;
&lt;/dl&gt;
</content>
 </entry>
 
 <entry>
   <title>Basic Setup</title>
   <link href="http://tedhagos.com/nodejs/basic-setup.html"/>
   <updated>2015-01-29T00:00:00+08:00</updated>
   <id>http://tedhagos.com/nodejs/basic-setup</id>
   <content type="html">&lt;p&gt;There are 3 ways to install node, each with pros and cons. You can get node going in your system using anyone of the following&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;strong&gt;Precompiled binaries&lt;/strong&gt;. This is the simplest. You simply head over towards &lt;a href=&quot;http://nodejs.org&quot;&gt;nodejs.org&lt;/a&gt; and download the precompiled binary that is right for your system. NodeJS installers are available for OSX, Linux and Windows. If you install node this way, you would have installed npm as well. Npm is the node package manager. This is the easiest for most users because the installation process is very familiar to anyone who have installed applications before. It is a a wizard. You download, double click and follow the prompts. There is nothing more to it. When a new version of node comes along though, you have to go through this process again&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Node Version Manager&lt;/strong&gt;. This method of installation allows you to run multiple versions of node within the sample computer. You can install a new version (or an older version) of node with just a few simple commands. This is not as simple as using precompiled binaries because before you can install node itself, you need to install the nvm first. If you are using OSX or Linux, you can use nvm. If you are on windows, nvm is currently not officially supported on that platform. There were people who made nvm available to Windows users.Y0u can find the instructions on how to get nvm going on Windows on the &lt;a href=&quot;https://github.com/creationix/nvm&quot;&gt;github page for nvm&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Compilation from source&lt;/strong&gt;. This has the highest difficulty of all the options for node installation. If you have installed software before by compiling them from source, you maybe right at home with this option. If you haven’t, the process for a first time user may prove to be daunting. This is the most flexible though because you can control the compilation using options. If you want to run node on a platform other than OSX,  Linux or Windows, this maybe the only the option available to you&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Other package managers&lt;/strong&gt;. OSX users have homebrew or macports, Windows users have chocolatey and Linux users have either yum, aptitude and many other else, depending on the Linux flavor. Node has been around for a quite a while now and its precompiled binaries have made it to the most popular and common code repositories&lt;/li&gt;
&lt;/ol&gt;

&lt;h1 id=&quot;os-package-managers&quot;&gt;OS Package Managers&lt;/h1&gt;

&lt;p&gt;On a Linux machine with Debian or Ubuntu, you could install node with the following commands&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;sudo apt-get update
sudo apt-get upgrade
sudo apt-get -y build-essential
sudo apt-get -y install curl
curl -sL https://deb.nodesource.com/setup | sudo bash -
sudo apt-get -y install nodejs npm
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Installation instructions for other Linux distros can be found at &lt;a href=&quot;https://github.com/joyent/node/wiki/Installing-Node.js-via-package-manager&quot;&gt;joyent wiki page at github&lt;/a&gt;. &lt;/p&gt;

&lt;p&gt;On OSX with homebrew, you could install it with the command &lt;code&gt;brew install node&lt;/code&gt;. You need to ensure that the folder &lt;code&gt;/usr/local/bin&lt;/code&gt; is included in your system path because brew will put the node executable on that folder. On Windows with the chocolatey package manager, you could get node going with the command &lt;code&gt;cinst nodejs&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;For information on chocolatey, head over to their website at &lt;a href=&quot;http://chocolatey.org&quot;&gt;chocolatey.org&lt;/a&gt;. For information on homebrew, go to their website at &lt;a href=&quot;http://brew.sh&quot;&gt;brew.sh&lt;/a&gt;. You may need to install some other software before you can even install either chocolatey or brew.&lt;/p&gt;

&lt;h1 id=&quot;node-version-manager&quot;&gt;Node Version Manager&lt;/h1&gt;

&lt;p&gt;We need to install nvm first before we can proceed with the node installation. You will need either &lt;code&gt;curl&lt;/code&gt; or &lt;code&gt;wget&lt;/code&gt; to install nvm. In our case, we will use curl. If you don’t have &lt;code&gt;curl&lt;/code&gt;, you need to get it first. On OSX you can get it with &lt;code&gt;brew install curl&lt;/code&gt; and on Linux you can get using &lt;code&gt;sudo apt-get install curl&lt;/code&gt;. Once done, open a terminal window and run the command &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;curl https://raw.githubusercontent.com/creationix/nvm/v0.24.1/install.sh | bash
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You may need to completely quit the terminal window and relaunch it again in order to use nvm. At this point, we can now install node. Once nvm is up and running, node can be installed with a simple nvm command like &lt;code&gt;nvm install 0.10&lt;/code&gt;. This command will fetch the node binaries from preconfigured software repositories where node is hosted. You can test if node was downloaded to your system by using the commadn &lt;code&gt;node -v&lt;/code&gt;. It should echo on your screen the version number of node. Here’s a small list of nvm commands that you might need for daily use&lt;/p&gt;

&lt;dl&gt;
  &lt;dt&gt;nvm&lt;/dt&gt;
  &lt;dd&gt;Displays a help file on how to use nvm. It lists options and flags the nvm command&lt;/dd&gt;
  &lt;dt&gt;nvm install 0.10&lt;/dt&gt;
  &lt;dd&gt;Installs node version .10. You don’t need to specify anything after 10, nvm will automatically fetch the latest version in the .10 series. Just replace 0.10 for whatever version of node that you want to install&lt;/dd&gt;
  &lt;dt&gt;nvm use 0.10&lt;/dt&gt;
  &lt;dd&gt;Switch to version .10 of node&lt;/dd&gt;
  &lt;dt&gt;nvm use 0.08&lt;/dt&gt;
  &lt;dd&gt;Switch to version .08, if you have installed it before&lt;/dd&gt;
  &lt;dt&gt;nvm alias default 0.10&lt;/dt&gt;
  &lt;dd&gt;It makes version 0.10 of node the default version. Every time you open a terminal window and use the command &lt;code&gt;node -v&lt;/code&gt;, it will always point to version 0.10 &lt;/dd&gt;
&lt;/dl&gt;

&lt;h1 id=&quot;references&quot;&gt;References&lt;/h1&gt;

&lt;ol&gt;
  &lt;li&gt;Joyent page at github - &lt;a href=&quot;https://github.com/joyent/node/wiki/Installing-Node.js-via-package-manager&quot;&gt;github.com/joyent/node/wiki&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;Creationix github page - &lt;a href=&quot;https://github.com/creationix/nvm&quot;&gt;github.com/creationix/nvm&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;

</content>
 </entry>
 
 <entry>
   <title>Threads</title>
   <link href="http://tedhagos.com/java/threads.html"/>
   <updated>2015-01-27T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/threads</id>
   <content type="html">&lt;p&gt;When you call someone over the phone, you can’t talk right away because the line is still ringing. You need to wait for the other person to pick up, you will then say hello, introduce yourself then the conversation can begin. The steps that are necessary to complete a phone call are carried out in a very serial way. Before the last step can begin, the step before that needs to complete first, so is the step before that and the step before it etc. Everything is in the critical path. If we wrote programs this way, we’d be in trouble. &lt;/p&gt;

&lt;p&gt;Imagine that we have to write a program that accepts input from a user, that input will be used as a key to look up a value in a remote database. If we will write this program in a serial way, it is likely that the users of the program will complain at some point when the program becomes non-responsive. A database  operation can take a long time especially if it is on a remote location. &lt;/p&gt;

&lt;p&gt;REWORD
When the user submits his query, our program will wait until the database completes its action before it can return control to our user interface. If &lt;/p&gt;

&lt;p&gt;If the database query takes a long time to complete, and this is commonplace, the user interface will freeze because it is waiting for the database operation to return the control.
END REWORD&lt;/p&gt;

&lt;p&gt;When an action has to wait for some other action to complete before it can begin, that other action is referred to as a &lt;strong&gt;blocking operation&lt;/strong&gt; because it prevents the action that comes after it from starting.  &lt;/p&gt;

&lt;p&gt;Consider the email workflow as a contrast to the telephone workflow. The send, receive, reply action between the two parties does not have any blocking restrictions. You can send the email and you don’t need to wait for it to be received, you can go about doing other things with your time. The send, receive and reply actions on the email are &lt;strong&gt;non-blocking&lt;/strong&gt;. The email workflow might be a bad model to illustrate the non-blocking concept, but it gets the point across. The sender is free to do other things once the message has been sent.  &lt;/p&gt;

&lt;p&gt;Java programs can be written in a non-blocking way by using &lt;strong&gt;Thread&lt;/strong&gt; objects. The general idea is to group all the statements you don’t want to run independently, in a &lt;strong&gt;non-blocking&lt;/strong&gt; way, inside a Thread object and let it run independently.  &lt;/p&gt;

&lt;p&gt;main is a single thread&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/threads.png&quot; alt=&quot;threads&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Things to discuss&lt;/p&gt;

&lt;p&gt;asynchrony and concurrency
telephone, and email
receptionist
boss and delegation, or a clone of myself. I will hand over a post-it note to my clone and let him do the tasks. That’s it.&lt;/p&gt;

&lt;p&gt;key thing is not to block, I need to explain the concept of blocking&lt;/p&gt;

&lt;pre&gt;&lt;code class=&quot;language-java&quot;&gt;class Test extends Thread {

  public static void main (String[] args) {
    Test t = new Test();
    t.start();
    System.out.println(&quot;About to finish main&quot;);
  }

  public void run() {
    for (int i=0; i &amp;lt; 10 ; i++ )
      System.out.println(&quot;i = &quot; +i);
  }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&quot;glossary&quot;&gt;Glossary&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;Blocking operation&lt;/li&gt;
  &lt;li&gt;Non-blocking operation&lt;/li&gt;
  &lt;li&gt;Concurrent&lt;/li&gt;
  &lt;li&gt;Asynchronous&lt;/li&gt;
&lt;/ol&gt;

</content>
 </entry>
 
 <entry>
   <title>Switch JDK on OSX</title>
   <link href="http://tedhagos.com/java/howto-switch-jdk-on-osx.html"/>
   <updated>2015-01-26T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/howto-switch-jdk-on-osx</id>
   <content type="html">&lt;p&gt;To set the &lt;code&gt;JAVA_HOME&lt;/code&gt; variable on OSX, use the tool &lt;code&gt;/usr/libexec/java_home&lt;/code&gt; on a terminal window. That command will echo where the current JDK is pointing to. You can copy the output  of that command from the clipboard with this command&lt;/p&gt;

&lt;p&gt;&lt;code&gt;$ /usr/libexec/java_home | pbcopy&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;then update the &lt;strong&gt;~/.bash_profile&lt;/strong&gt; so you can export the JAVA_HOME variable. Tools like Apache Ant and Maven expects this variable to be present in order to locate the installed JDK on your system. Some IDEs like Android Studio also expects to find this variable defined somewhere in your system. &lt;/p&gt;

&lt;p&gt;Alternatively, you can add this line to the &lt;strong&gt;/.bash_profile&lt;/strong&gt;&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;export JAVA_HOME=$(/usr/libexec/java_home)
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&quot;when-you-have-more-than-one-jdk-installed&quot;&gt;When you have more than one JDK installed&lt;/h2&gt;

&lt;p&gt;If you have JDK 1.5,1.6,1.7 and Java 8 installed on the Mac, &lt;code&gt;java_home&lt;/code&gt; will point the most recent version of JDK you installed.  There used to be a way in OSX to switch JDKs, it was a utility called &lt;strong&gt;Java Preferences&lt;/strong&gt;. It’s gone now. It has been gone for quite some time. If you want to see all the JDKs installed in your system, run this command&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ /usr/libexec.java_home -V
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You might be tempted to fool around with symlinks in order so switch between one version of the JDK to another. Don’t. It’s a bad idea, especially if you are not very handy with the SHELL. Use the &lt;code&gt;java_home&lt;/code&gt; tool instead.&lt;/p&gt;

&lt;p&gt;I found the script floating around on the internet while I was searching for a solution on how to safely switch from one version of the JDK to another.  &lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;&lt;span class=&quot;k&quot;&gt;function&lt;/span&gt; setjdk&lt;span class=&quot;o&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;  
  &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;[&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;$# &lt;/span&gt;-ne &lt;span class=&quot;m&quot;&gt;0&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;]&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;then&lt;/span&gt;  
   removeFromPath &lt;span class=&quot;s1&quot;&gt;&amp;#39;/System/Library/Frameworks/JavaVM.framework/Home/bin&amp;#39;&lt;/span&gt;  
   &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;[&lt;/span&gt; -n &lt;span class=&quot;s2&quot;&gt;&amp;quot;${JAVA_HOME+x}&amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;]&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;then&lt;/span&gt;  
    removeFromPath &lt;span class=&quot;nv&quot;&gt;$JAVA_HOME&lt;/span&gt;  
   &lt;span class=&quot;k&quot;&gt;fi&lt;/span&gt;  
   &lt;span class=&quot;nb&quot;&gt;export &lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;JAVA_HOME&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;sb&quot;&gt;`&lt;/span&gt;/usr/libexec/java_home -v &lt;span class=&quot;nv&quot;&gt;$@&lt;/span&gt;&lt;span class=&quot;sb&quot;&gt;`&lt;/span&gt;  
   &lt;span class=&quot;nb&quot;&gt;export &lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;PATH&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;$JAVA_HOME&lt;/span&gt;/bin:&lt;span class=&quot;nv&quot;&gt;$PATH&lt;/span&gt;  
  &lt;span class=&quot;k&quot;&gt;fi&lt;/span&gt;  
  &lt;span class=&quot;nb&quot;&gt;echo &lt;/span&gt;JAVA_HOME &lt;span class=&quot;nb&quot;&gt;set &lt;/span&gt;to &lt;span class=&quot;nv&quot;&gt;$JAVA_HOME&lt;/span&gt;  
  java -version  
 &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;  

&lt;span class=&quot;k&quot;&gt;function&lt;/span&gt; removeFromPath&lt;span class=&quot;o&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;  
 &lt;span class=&quot;nb&quot;&gt;export &lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;PATH&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;=&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;$(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;echo&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;$PATH&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;|&lt;/span&gt; sed -E -e &lt;span class=&quot;s2&quot;&gt;&amp;quot;s;:$1;;&amp;quot;&lt;/span&gt; -e &lt;span class=&quot;s2&quot;&gt;&amp;quot;s;$1:?;;&amp;quot;&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;)&lt;/span&gt;  
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;You can append this to &lt;strong&gt;~/.bash_profile&lt;/strong&gt; so it is available everytime you open a terminal window. &lt;/p&gt;

&lt;p&gt;This is how to use it&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;
$ setjdk 1.7 // to use JDK 7
$ setjdk 1.8 // to use JDK 8

&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;a href=&quot;https://developer.apple.com/library/mac/documentation/Darwin/Reference/ManPages/man1/java_home.1.html&quot;&gt;developer.apple.com/java_home&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;http://raibledesigns.com/rd/entry/installing_openjdk_7_on_os&quot;&gt;raibledesigns.com/installing openjdk on osx&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;
</content>
 </entry>
 
 <entry>
   <title>Show Posts in a given category</title>
   <link href="http://tedhagos.com/jekyll/howto-show-posts-in-category.html"/>
   <updated>2015-01-26T00:00:00+08:00</updated>
   <id>http://tedhagos.com/jekyll/howto-show-posts-in-category</id>
   <content type="html">&lt;p&gt;The &lt;strong&gt;categories&lt;/strong&gt; variable is a list in Liquid. In order to selectively display posts that belongs to a specific category, we need to do 3 things. First is to traverse all the &lt;strong&gt;posts&lt;/strong&gt;, then traverse the &lt;strong&gt;categories&lt;/strong&gt; list  and lastly, inquire if the category is a match to the category we want to display on the page. The code snippet below will display all the post, the title at least, that belongs to the ‘linux’ category.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;
{% for post in site.posts %}
  {% for cat in post.categories %}
    {% if cat == &#39;linux&#39; %}
      {{ post.title }} &amp;lt;br/&amp;gt;
    {% endif %}
  {% endfor %}
{% endfor %}

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Alternatively, we can use the &lt;strong&gt;site.categories.CATEGORY&lt;/strong&gt; variable in Jekyll to do exactly what the code sample above does. I prefer the second approach because it has less code. It is easier to read, maintain and most importantly, less processing time.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;

{% for post in site.categories.linux %}
  {{ post.title }} &amp;lt;br/&amp;gt;
{% endfor %}


&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;a href=&quot;http://jekyllrb.com/docs/variables/&quot;&gt;jekylrb.com/variables&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;
</content>
 </entry>
 
 <entry>
   <title>Highlight Liquid Code</title>
   <link href="http://tedhagos.com/jekyll/howto-highlight-liquid-code-in-jekyll.html"/>
   <updated>2015-01-26T00:00:00+08:00</updated>
   <id>http://tedhagos.com/jekyll/howto-highlight-liquid-code-in-jekyll</id>
   <content type="html">&lt;p&gt;You cannot highlight Liquid snippet in Jekyll by simply enclosing the snippet with &lt;code&gt;pre&lt;/code&gt; tag or the &lt;strong&gt;{ % highlight% } { % endhighlight% }&lt;/strong&gt; pair. Jekyll will interpret the snippet instead of displaying it.&lt;/p&gt;

&lt;p&gt;To display Liquid snippet in Jekyll, you can enclose it between &lt;strong&gt;{ % raw % } and { % endraw % }&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;images/raw-endraw.png&quot; alt=&quot;raw-endraw&quot; /&gt;&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Yosemite upgrade and Homebrew</title>
   <link href="http://tedhagos.com/osx/homebrew-yosemite.html"/>
   <updated>2015-01-24T00:00:00+08:00</updated>
   <id>http://tedhagos.com/osx/homebrew-yosemite</id>
   <content type="html">&lt;p&gt;Homebrew uses Ruby 1.8 and Yosemite uses Ruby 2.0. This is why Homebrew doesn’t work with OSX 10.10. This would have been easily fixed if you did a brew update and brew upgrade before going up to Yosemite. Just in case you did upgrade to Yosemite without upgrading Homebrew, the fix is this&lt;/p&gt;

&lt;p&gt;Edit the file &lt;code&gt;/usr/local/Library/brew.rb&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The first line on the file is&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;#!/System/Library/Frameworks/Ruby.framework/Versions/1.8/usr/bin/ruby -W0
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Replace the hard coded reference to the Ruby version number. The edited line should read&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;#!/System/Library/Frameworks/Ruby.framework/Versions/Current/usr/bin/ruby -W0
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You have to commit the changes you made on brew.rb before you can brew upgrade, otherwise, you will be back to square one.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ cd $(brew --repository)
$ git commit -a -m &quot;changed brew rb to point to Ruby 2
$ brew update
$ brew upgrade
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If the cd $(brew –repository) command gives you problem, just cd to usr/local/Library then do the commit while in that directory.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;a href=&quot;http://jcvangent.com/fixing-homebrew-os-x-10-10-yosemite/&quot;&gt;Hans Van Gent, Fixing Homebrew&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;

</content>
 </entry>
 
 <entry>
   <title>Emacs Setup</title>
   <link href="http://tedhagos.com/emacs/emacs-notes.html"/>
   <updated>2015-01-10T00:00:00+08:00</updated>
   <id>http://tedhagos.com/emacs/emacs-notes</id>
   <content type="html">&lt;h1 id=&quot;setup&quot;&gt;Setup&lt;/h1&gt;

&lt;p&gt;Emacs is fairly ubiquitous already. It has been around for a long time so you can pretty much get it no matter what your choice of platform is. But it is pretty at home with UNIXes and the Linuxes. &lt;/p&gt;

&lt;p&gt;For Linux, the &lt;code&gt;sudo-apt-get install emacs-nox&lt;/code&gt; should do it. This will work for Debian and Debian-ish platforms. I haven’t used RedHat or it’s derivatives for a while now but you may have to use their &lt;code&gt;yum&lt;/code&gt;  package manager. I specified &lt;em&gt;emacs-nox&lt;/em&gt; because I don’t want to install the GTK or X11 version of emacs – &lt;em&gt;nox&lt;/em&gt; means “no x windows”. &lt;/p&gt;

&lt;p&gt;In OSX, the  &lt;code&gt;brew install emacs&lt;/code&gt; is what you will use if you use the &lt;em&gt;HomeBrew&lt;/em&gt; package manager. You can use macports of Fink if that is what you use as a package manager.&lt;/p&gt;

&lt;p&gt;Windows users can download the binary distributions from the &lt;a href=&quot;http://ftp.gnu.org/gnu/emacs/windows/&quot;&gt;Emacs site&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;In OSX, you need to a little bit more setup because OSX comes with an outdated version of GNU emacs, so you may have to delete that first and then do something inside &lt;code&gt;/usr/bin/&lt;/code&gt; &lt;/p&gt;

&lt;p&gt;&lt;code&gt;$ sudo rm /usr/bin/emacs&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;$ sudo rm -fvr /usr/share/emacs&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;$ sudo ln -s /usr/local/bin/emacs /usr/bin/emacs&lt;/code&gt;  &lt;/p&gt;

&lt;p&gt;You can now launch emacs from any terminal window. &lt;/p&gt;

&lt;h1 id=&quot;configuration-and-startup-files&quot;&gt;Configuration and Startup files&lt;/h1&gt;

&lt;p&gt;Generally, emacs reads the files ~/.emacs and ~/.emacs.d/init.el. This is where you can write your startup settings e.g. setting of color theme, treatment of line wraps, how to treat tabs (or spaces) etc. &lt;/p&gt;

&lt;p&gt;Emacs uses lisp, actually a specific kind of lisp called &lt;em&gt;elisp&lt;/em&gt; which stands for emacs lisp. This the language you will use if you want to monkey around emacs and bend it to submission. Don’t worry too much if you don’t know much about lisp or don’t know it all. The emacs community is large. There is no shortage of blog post and Gists which features ready made emacs init files that you can mindlessly cut and paste into your own.&lt;/p&gt;

&lt;p&gt;If your needs are simple, e.g. convert the tabs to spaces, set the number of spaces for a tab, write source codes using the K &amp;amp; R convention, write your diary and convert it to HTML, a quick Google search about these things will usually do the fix for you. There are pretty standard ways of accomplishing these things in the emacs init files.&lt;/p&gt;

&lt;h1 id=&quot;opening-and-closing-files&quot;&gt;Opening and Closing files&lt;/h1&gt;

&lt;p&gt;After you launched emacs, you can open a file by pressing &lt;code&gt;C x C f&lt;/code&gt; that means, press the control key, press &lt;code&gt;x&lt;/code&gt;, don’t let go of the control key, and press &lt;code&gt;f&lt;/code&gt;. Emacs will wait for you to input a name of the file. Pay attention to input area in the lower left of the emacs window. You will type the name of the file and it’s path in there. &lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/emacs-find-file.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;You can close the file by using the command &lt;code&gt;C x C c&lt;/code&gt; that means press &lt;code&gt;CTRL x&lt;/code&gt; but don’t let the CTRL key go. While the CTRL is still depressed, press &lt;code&gt;s&lt;/code&gt;. That will save the file. If you want to save the file using another name (Save As), the command is &lt;code&gt;C x C w&lt;/code&gt;.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;C x C c&lt;/code&gt; will exit emacs. In case you have unsaved work, emacs won’t let you leave without confirming your action. It will nag you whether you want to save any unsaved work etc. &lt;/p&gt;

&lt;h1 id=&quot;packages&quot;&gt;Packages&lt;/h1&gt;

&lt;p&gt;Emacs has a lively community of users and contributors. If there is a functionality you need and it’s not built-in with emacs, chances are somebody built a package for that. So you need to know how to get additional software for emacs. &lt;/p&gt;

&lt;p&gt;Software packages for emacs are lisp files. They have the extension &lt;em&gt;.el&lt;/em&gt; and there are plenty of sources for that. The more popular sources &lt;a href=&quot;http://elpa.gnu.org/packages&quot;&gt;elpa&lt;/a&gt;, &lt;a href=&quot;http://marmalade-repo.org/packages&quot;&gt;marmalade&lt;/a&gt; and &lt;a href=&quot;http://melpa.milkbox.net/packages/&quot;&gt;melpa&lt;/a&gt; . There are others but these three seems to be favorites. &lt;/p&gt;

&lt;p&gt;To use these software sources, you need to tell emacs about their existence. That is done via the init files. So if you already have installed emacs, go ahead and use it for edit your init files. You can follow these steps.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;$ touch ~/.emacs&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;$ emacs ~/.emacs&lt;/code&gt;  &lt;/p&gt;

&lt;p&gt;Once emacs has opened the &lt;em&gt;.emacs&lt;/em&gt; init file, put the following codes inside&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-lisp&quot; data-lang=&quot;lisp&quot;&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;setq&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;package-archives&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;((&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;gnu&amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;.&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;http://elpa.gnu.org/packages/&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
                         &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;marmalade&amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;.&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;http://marmalade-repo.org/packages/&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
                         &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;melpa&amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;.&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;http://melpa.milkbox.net/packages/&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;These settings and URL values were lifted straight from the &lt;a href=&quot;http://www.emacswiki.org/emacs/ELPA&quot;&gt;EmacsWiki&lt;/a&gt; site. There are other ways of configuring the package manager on that site, you should go visit that page. &lt;/p&gt;

&lt;p&gt;Save the file by pressing &lt;code&gt;CTRL + x&lt;/code&gt; don’t let the CTRL key go, keep it pressed, then press &lt;code&gt;s&lt;/code&gt;. In emacs lingo, you will see this command written as &lt;code&gt;C - x C - s&lt;/code&gt;. The C stands for the CTRL key. So remember that when you see this notation, it means press CTRL key then press &lt;code&gt;x&lt;/code&gt; after that press &lt;code&gt;s&lt;/code&gt; without letting go of the CTRL key. That command saves the contents of your current buffer to the file &lt;em&gt;~&lt;/em&gt;.emacs/. &lt;/p&gt;

&lt;p&gt;You have to exit your emacs session and start it over again. The restart is necessary because emacs needs to read the new entries on it’s init file. To exit emacs, press &lt;code&gt;C - x C - c&lt;/code&gt;. You already know what that notation means. That is the command to exit emacs. &lt;/p&gt;

&lt;p&gt;Now start emacs again and then press &lt;code&gt;ESC - x&lt;/code&gt;, you can also press &lt;code&gt;ALT - x&lt;/code&gt;, it will yield the same result. Pay attention to message window on left lower portion of emacs, it says &lt;strong&gt;M - x&lt;/strong&gt;. It’s waiting for you to type something. It’s waiting for a command. Now type &lt;code&gt;list-packages&lt;/code&gt; RET (RET means return key or entery key). It might take a little while because emacs is communicating with online repositories. It’s grabbing a list of available software which it will show you onscreen in a little while. &lt;/p&gt;

&lt;p&gt;Take the time to browse through them, if something catches your fancy, go ahead and position the cursor on that list entry then press &lt;code&gt;i&lt;/code&gt;, that means mark it for installation. Then press &lt;code&gt;x&lt;/code&gt; to install what was marked. &lt;/p&gt;

&lt;p&gt;When you are all done. Press &lt;code&gt;q&lt;/code&gt; to quit the screen which lists the software packages. &lt;/p&gt;

&lt;h1 id=&quot;java-coding&quot;&gt;Java Coding&lt;/h1&gt;

&lt;p&gt;You need to define a &lt;em&gt;hook&lt;/em&gt; when emacs enters Java mode — when it opens a &lt;em&gt;.java&lt;/em&gt; source file. Then set theh appropriate variables inside the hook
1.  Use spaces in place of tabs – &lt;code&gt;indent-tabs-mode nil&lt;/code&gt;
2.  Use 2 spaces for indenting code – &lt;code&gt;c-basic-offset 2&lt;/code&gt;
3.  Use the Kernighan &amp;amp; Ritchie convention for aligning the curly braces – &lt;code&gt;c-default-style k&amp;amp;r&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Below is the copy-pastable setting that you can plunk in the &lt;em&gt;.emacs&lt;/em&gt; init file&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-lisp&quot; data-lang=&quot;lisp&quot;&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;add-hook&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;&amp;#39;java-mode-hook&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
    &lt;span class=&quot;s&quot;&gt;&amp;quot; Treat JDK 1.5 annotations as comments&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;setq&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;c-comment-start-regexp&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;(@|/(/|[*][*]?))&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;modify-syntax-entry&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;?@&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;&amp;lt;b&amp;quot;&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;java-mode-syntax-table&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;

&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;add-hook&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;&amp;#39;java-mode-hook&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lambda&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;setq&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;c-default-style&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;k&amp;amp;r&amp;quot;&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;c-basic-offset&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;tab-width&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;indent-tabs-mode&lt;/span&gt; &lt;span class=&quot;no&quot;&gt;nil&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;The &lt;em&gt;c-default-style k&amp;amp;r&lt;/em&gt; means it will use the curly braces alignment of Kernighan and
Ritchie, the opening curly brace is at the right most side, immediately following the
closing parens and the closing curly brace is flushed at the left side. &lt;/p&gt;

&lt;p&gt;The K &amp;amp; R style isn’t the only one you can use. The other indenting styles are well documented in EmacsWiki/Indenting Cand on WikiPedia/IndentStyle&lt;/p&gt;

&lt;h1 id=&quot;text-expansion-autocorrection&quot;&gt;Text expansion, AutoCorrection&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;(setq save abbrevs nil)&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;(setq-default abbrev-mode t)&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;(load &quot;~/.emacs.d/abbrev_defs&quot;)&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;note-taking-with-org&quot;&gt;Note taking with org&lt;/h1&gt;

&lt;p&gt;You must download org-mode using the package manager (package.el), get org-mode from there either from elpa, melpa or marmalade&lt;/p&gt;

&lt;p&gt;To publish in HTML, M-x org-html-export-to-html, that’s it or you might want to look at this 
&lt;a href=&quot;http://orgmode.org/worg/org-tutorials/org-publish-html-tutorial.html&quot;&gt;http://orgmode.org/worg/org-tutorials/org-publish-html-tutorial.html&lt;/a&gt;&lt;/p&gt;

&lt;h1 id=&quot;color-problem-with-tmux&quot;&gt;Color problem with tmux&lt;/h1&gt;

&lt;p&gt;TMUX messes with the color scheme that is setup in emacs. You need to fix the color scheme in tmux. There are plenty of ways to do this. I would think there is a way in .tmux.conf, the &lt;a href=&quot;https://wiki.archlinux.org/index.php/tmux&quot;&gt;TMUX section of ArchWiki&lt;/a&gt; has some detailed advice on this one. Or you can simply pass the TERM value when you start tmux. In the &lt;em&gt;.bashrc&lt;/em&gt; or or &lt;em&gt;.bash_profile&lt;/em&gt; add an alias to tmux&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;alias tmux=&quot;TERM=screen-256color-bce tmux&quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Now open tmux as you normally would e.g. &lt;code&gt;tmux new -s coding&lt;/code&gt; then open emacs. TMUX shouldn’t be messing around with the emacs color-scheme now.&lt;/p&gt;

&lt;p&gt;Another route you can try is to customise the &lt;code&gt;frame-background-mode&lt;/code&gt; variable. Try the three different settings (0 = light, 1 = dark and 2 = automatic). Invoke the customise variable command &lt;code&gt;M x customize-variable RET&lt;/code&gt; then type &lt;code&gt;frame-background-mode RET&lt;/code&gt;. You will see the following screen.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/frame-background-mode.png&quot; alt=&quot;img&quot; /&gt; &lt;/p&gt;

&lt;p&gt;Go to value menu, the RET. Choose either 0,1 or 2. Then go to the State menu and choose from one of the options. You can set the value of background just for the current session or you can choose to save it for future sessions. &lt;/p&gt;

&lt;div id=&quot;footnotes&quot;&gt;
&lt;h2 class=&quot;footnotes&quot;&gt;Footnotes: &lt;/h2&gt;
&lt;div id=&quot;text-footnotes&quot;&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.1&quot; name=&quot;fn.1&quot; class=&quot;footnum&quot; href=&quot;#fnr.1&quot;&gt;1&lt;/a&gt;&lt;/sup&gt; Think of a buffer as a scratch pad. It&#39;s an editing session, there is a subtle difference between a physical file and a buffer, but for now, think of them as the same thing. If you are more comfortable with the notion that you are editing a &quot;file&quot; instead of buffer, then go ahead with that. Eventually, when you have used emacs enough, you will realize the difference.&lt;/div&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.2&quot; name=&quot;fn.2&quot; class=&quot;footnum&quot; href=&quot;#fnr.2&quot;&gt;2&lt;/a&gt;&lt;/sup&gt; There is an indepth treatment of C indent styles [here](http://www.emacswiki.org/emacs/IndentingC)&lt;/div&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.3&quot; name=&quot;fn.3&quot; class=&quot;footnum&quot; href=&quot;#fnr.3&quot;&gt;3&lt;/a&gt;&lt;/sup&gt; WikiPedia shows some sample and historical context on each style. The link is [here](http://en.wikipedia.org/wiki/Indent_style)&lt;/div&gt;


&lt;/div&gt;
&lt;/div&gt;
</content>
 </entry>
 
 <entry>
   <title>Remove .DS_STORE from a Git Repo</title>
   <link href="http://tedhagos.com/osx/remove-ds-store-from-git.html"/>
   <updated>2014-12-19T00:00:00+08:00</updated>
   <id>http://tedhagos.com/osx/remove-ds-store-from-git</id>
   <content type="html">&lt;pre&gt;&lt;code&gt;$ cd path/to/repo
$ find . -name .DS_Store -print0 | xargs -0 git rm -f --ignore-unmatch
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Once everything is removed, make sure that .DS_STORE will not be added to the repo in the future. Add the following line to the &lt;strong&gt;.gitignore&lt;/strong&gt; file&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;.DS_STORE
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Commit and push as usual&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>Publishing on Leanpub</title>
   <link href="http://tedhagos.com/writing/LeanPub.html"/>
   <updated>2014-12-12T00:00:00+08:00</updated>
   <id>http://tedhagos.com/writing/LeanPub</id>
   <content type="html">&lt;p&gt;The whole getting started, syntax, Dropbox integrations, getting paid etc are all on &lt;a href=&quot;http://leanpub.com/manual&quot;&gt;http://leanpub.com/manual&lt;/a&gt;
. The book is free. There is no execuse why you should not read it.&lt;/p&gt;

&lt;h1 id=&quot;orgmode-doc-to-leanpub-markdown&quot;&gt;Orgmode doc to Leanpub Markdown&lt;/h1&gt;

&lt;p&gt;This is settled already. There is no export to do. Org mode is pre writing. I will write in Markdown when publishing to LeanPub. Practice your typing!
-   What kind of markup do I use
-   Are all of them handled gracefully by the Orgmode exporter
-   If not, what are the solutions. Perhaps sed and awk&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;What happens to footnotes&lt;/li&gt;
  &lt;li&gt;What happens to beginex – they are actually respected by LeanPub&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;heading-structure&quot;&gt;Heading Structure&lt;/h2&gt;

&lt;p&gt;Keep the #title for the title, but make sure that the chapter title is is heading 1, which meang single asterisk. When you export the document to markdown format, the chapter title becomes heading 1&lt;/p&gt;

&lt;p&gt;Use the traditional orgmode. Write as it is.&lt;/p&gt;

&lt;h1 id=&quot;how-will-you-handle-beginexample-and-endexample&quot;&gt;How will you handle begin_example and end_example&lt;/h1&gt;

&lt;p&gt;Sed scripts, perhaps&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Java Home Variable</title>
   <link href="http://tedhagos.com/osx/java-home-osx.html"/>
   <updated>2014-12-08T00:00:00+08:00</updated>
   <id>http://tedhagos.com/osx/java-home-osx</id>
   <content type="html">&lt;p&gt;Mac OSX no longer includes Java. You need to install it on your own. The installation is straightforward, the Oracle site has an installer for OSX, just get that. The installer executes without issues and it automatically includes the Java executable to the system path.&lt;/p&gt;

&lt;p&gt;The JAVA_HOME variable, however, is a different matter. The installer will not automatically create an entry for it. Some tools like ANT or Maven and some software like Tomcat does require that the JAVA_HOME be set, lest you will have some annoying command line issues with the tools.&lt;/p&gt;

&lt;p&gt;There is a command line tool in OSX which points to the folder where Java is installed, the command is &lt;code&gt;/usr/libexec/java_home&lt;/code&gt;. If you run it from a terminal app, it will point to the installation folder of the JDK. You can cut and paste the location of the folder in your &lt;strong&gt;~/.bash_profile&lt;/strong&gt; so the JAVA_HOME variable will always be set when you use a terminal window. Or, instead of cutting and pasting the result of &lt;code&gt;/usr/libexec/java_home&lt;/code&gt;, you can execute it as part of your startup script. Add the following line to  &lt;code&gt;~/.bash_profile&lt;/code&gt;. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;export JAVA_HOME=$(/usr/libexec/java_home)
&lt;/code&gt;&lt;/pre&gt;
</content>
 </entry>
 
 <entry>
   <title>Delete Conflicted Copies in Dropbox</title>
   <link href="http://tedhagos.com/tools/Delete-Conflicted-Copies-Dropbox.html"/>
   <updated>2014-12-07T00:00:00+08:00</updated>
   <id>http://tedhagos.com/tools/Delete-Conflicted-Copies-Dropbox</id>
   <content type="html">&lt;p&gt;You need to be sure you have a backup of your files. These commands are on CLI, if you make a mistake, the only way to go back is to restore the files using the tools found in the Dropbox website.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;
$ cd ~/Dropbox/thefolder
$ find . -type f -name &quot;* conflicted copy*&quot; -exec rm -f {} \;
$ awk &#39;!/conflicted/&#39; .git/packed-refs &amp;gt; temp &amp;amp;&amp;amp; mv temp $ .git/packed-refs

&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The last command is applicable to you if the &lt;strong&gt;thefolder&lt;/strong&gt; is also a git repo. If it is not, you should not use that command. Actually it won’t hurt if you accidentally typed that last command if thefolder is not a git repo because it will not find the &lt;code&gt;.git&lt;/code&gt; hidden folder&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>OSX Yosemite Mail Problem</title>
   <link href="http://tedhagos.com/osx/osx-yosemite-mail-problem.html"/>
   <updated>2014-11-27T00:00:00+08:00</updated>
   <id>http://tedhagos.com/osx/osx-yosemite-mail-problem</id>
   <content type="html">&lt;p&gt;Mail.app in Yosemite has a default setting of using &lt;strong&gt;secure authentication&lt;/strong&gt; when it sends password. If you email server is not using secure authentication for checking credentials, you will not be able to send out any email.&lt;/p&gt;

&lt;p&gt;The fix is simple. You only need to uncheck two option boxes on the &lt;strong&gt;Accounts Preferences&lt;/strong&gt; window of Mail.app. The two checkboxes will prevent Mail.app from overriding your manual setting. One of them is found on the “Advanced” tab of the Accounts Preferences and other other one can be found on the “Edit SMTP Server list” window. You will also need to check one option box that will allow you to send plain username and password to your server.&lt;/p&gt;

&lt;p&gt;When Mail.app is open, go to &lt;strong&gt;Mail&lt;/strong&gt; menu,  &lt;strong&gt;Preferences&lt;/strong&gt;, then click the &lt;strong&gt;Accounts&lt;/strong&gt; tab. &lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/accounts-pref-1.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Uncheck the box that says “Automatically detect and maintain settings”. If this is checked, Mail.app may overwrite whatever settings you change. We don’t need that. On the same window, find the option box that says “Allow insecure authentication”. This will let you authenticate using plain username and passwords.&lt;/p&gt;

&lt;p&gt;From the “Advanced” tab, go to “Account information” tab and find the dropdown box for “Outgoing Mail Server”. &lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/accounts-pref-2.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Click the dropdown and go to “Edit SMTP Server list”&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/accounts-pref-3.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Go to the “Advanced” tab and uncheck the “Automatically detect and maintain settings” option&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/accounts-pref-4.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;When Mail.app ask you if you want to save the changes, save the changes. &lt;/p&gt;

&lt;h1 id=&quot;editing-the-plist&quot;&gt;Editing the plist&lt;/h1&gt;

&lt;p&gt;The build of the Mail.app is suspicious, sometimes it respects the changes you’ve done on the Preferences window, sometimes it doesn’t. If you find that Mail.app is ignoring your changes, you can go straight to the &lt;strong&gt;plist&lt;/strong&gt; file. &lt;/p&gt;

&lt;p&gt;Open the file &lt;strong&gt;~/Library/Mail/V2/MailData/Accounts.plist&lt;/strong&gt;. Find the entry for &lt;strong&gt;UserAllowsInsecureAuthentication&lt;/strong&gt; and make sure the value is &lt;strong&gt;true&lt;/strong&gt;&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-xml&quot; data-lang=&quot;xml&quot;&gt;&lt;span class=&quot;nt&quot;&gt;&amp;lt;dict&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;AccountName&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;string&amp;gt;&lt;/span&gt;PH&lt;span class=&quot;nt&quot;&gt;&amp;lt;/string&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;AccountType&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;string&amp;gt;&lt;/span&gt;SMTPAccount&lt;span class=&quot;nt&quot;&gt;&amp;lt;/string&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;AuthenticationScheme&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;string&amp;gt;&amp;lt;/string&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;CanonicalEmailAddress&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;string&amp;gt;&lt;/span&gt;tedhagos@somedomain.com&lt;span class=&quot;nt&quot;&gt;&amp;lt;/string&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;ConfigureDynamically&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;false/&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;Hostname&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;string&amp;gt;&lt;/span&gt;101.202.303.404&lt;span class=&quot;nt&quot;&gt;&amp;lt;/string&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;MaxMessageBytes&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;integer&amp;gt;&lt;/span&gt;31457280&lt;span class=&quot;nt&quot;&gt;&amp;lt;/integer&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;PortNumber&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;integer&amp;gt;&lt;/span&gt;587&lt;span class=&quot;nt&quot;&gt;&amp;lt;/integer&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;ShouldUseAuthentication&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;string&amp;gt;&lt;/span&gt;YES&lt;span class=&quot;nt&quot;&gt;&amp;lt;/string&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;UserAllowsInsecureAuthentication&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;true/&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;Username&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;string&amp;gt;&lt;/span&gt;tedhagos@somedomain.com&lt;span class=&quot;nt&quot;&gt;&amp;lt;/string&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;key&amp;gt;&lt;/span&gt;uniqueId&lt;span class=&quot;nt&quot;&gt;&amp;lt;/key&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;string&amp;gt;&lt;/span&gt;12345678790&lt;span class=&quot;nt&quot;&gt;&amp;lt;/string&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;/dict&amp;gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;The snippet above is not the whole plist file, you need to look for a &lt;strong&gt;dict&lt;/strong&gt; entry whose key is &lt;strong&gt;SMTPAccount&lt;/strong&gt;. There is more than one dict entry in the plist file, so be careful where you make the changes. Make sure you are making on the email account which you’d like to allow for insecure passing of username and password.&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>WebSphere 8.5.5 Liberty Profile</title>
   <link href="http://tedhagos.com/websphere/websphere-822.html"/>
   <updated>2014-11-19T00:00:00+08:00</updated>
   <id>http://tedhagos.com/websphere/websphere-822</id>
   <content type="html">&lt;h1 id=&quot;websphere-family-of-products&quot;&gt;WebSphere family of products&lt;/h1&gt;

&lt;p&gt;&lt;img src=&quot;../images/was-family.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;h1 id=&quot;websphere-editions&quot;&gt;WebSphere Editions&lt;/h1&gt;

&lt;p&gt;&lt;img src=&quot;../images/was-editions.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;h1 id=&quot;installing-the-liberty-profile&quot;&gt;Installing the Liberty Profile&lt;/h1&gt;

&lt;h2 id=&quot;system-requirements&quot;&gt;System Requirements&lt;/h2&gt;

&lt;p&gt;Minimum supported Java Level &lt;a href=&quot;http://www-01.ibm.com/support/knowledgecenter/#!/was_beta_liberty/com.ibm.websphere.wlp.nd.multiplatform.doc/ae/rwlp_restrict.html&quot;&gt;http://www-01.ibm.com/support/knowledgecenter/#!/was_beta_liberty/com.ibm.websphere.wlp.nd.multiplatform.doc/ae/rwlp_restrict.html&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;Which OS, Windows, Linux and Mac OSX. I need to find official versions &lt;/p&gt;

&lt;h2 id=&quot;archive-install&quot;&gt;Archive install&lt;/h2&gt;

&lt;h2 id=&quot;wdt-for-eclipse&quot;&gt;WDT for Eclipse&lt;/h2&gt;

&lt;h2 id=&quot;installation-manager&quot;&gt;Installation Manager&lt;/h2&gt;

&lt;h1 id=&quot;core-concepts&quot;&gt;Core Concepts&lt;/h1&gt;

&lt;h2 id=&quot;cell-nodes-and-profiles&quot;&gt;Cell, Nodes and Profiles&lt;/h2&gt;

&lt;h2 id=&quot;full-profile-and-liberty-profile&quot;&gt;Full Profile and Liberty Profile&lt;/h2&gt;

&lt;h1 id=&quot;liberty-profile&quot;&gt;Liberty Profile&lt;/h1&gt;

&lt;h2 id=&quot;description&quot;&gt;Description&lt;/h2&gt;

&lt;h2 id=&quot;simplified-configuration&quot;&gt;Simplified Configuration&lt;/h2&gt;

&lt;p&gt;There is only one XML file &lt;/p&gt;

&lt;h2 id=&quot;dynamic-runtime&quot;&gt;Dynamic runtime&lt;/h2&gt;

&lt;h2 id=&quot;deployment-options&quot;&gt;Deployment Options&lt;/h2&gt;

&lt;h1 id=&quot;installation&quot;&gt;Installation&lt;/h1&gt;

&lt;p&gt;The Liberty Profile is provided as a no-charge and free to distribute set of packages. It comes as a set of jar files. &lt;/p&gt;

&lt;h2 id=&quot;stand-alone&quot;&gt;Stand alone&lt;/h2&gt;

&lt;p&gt;Download the jars from &lt;a href=&quot;https://developer.ibm.com/wasdev/downloads/liberty-profile-beta/&quot;&gt;WASDev.net&lt;/a&gt;. &lt;/p&gt;

&lt;table border=&quot;2&quot; cellspacing=&quot;0&quot; cellpadding=&quot;6&quot; rules=&quot;groups&quot; frame=&quot;hsides&quot;&gt;


&lt;colgroup&gt;
&lt;col class=&quot;left&quot; /&gt;

&lt;col class=&quot;left&quot; /&gt;
&lt;/colgroup&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th scope=&quot;col&quot; class=&quot;left&quot;&gt;File&lt;/th&gt;
&lt;th scope=&quot;col&quot; class=&quot;left&quot;&gt;Description&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;

&lt;tbody&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;wlp-beta-runtime-xx.xx.xx.jar&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;The core runtime&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;wlp-beta-extended-xx.xx.xx.jar&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Extended runtime, if you will need JMS, WebServices etc&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;wlp-beta-admin-xx.xx.xx.jar\`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;A Rudimentary admin for the Liberty Profile. Very basic&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Install each of the executable jars above using&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;java -jar wlp-runtime-xx.xx.xx.jar
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Replace the xx.xx.xx with the actual values of the jar file names you downloaded. The above command will install the Liberty Profile runtime. It will ask for a few questions e.g. would you like to read the license, do you accept the license and which directory would you like to install the Liberty runtime. The default installation directory is the current directory where you ran the &lt;strong&gt;java -jar&lt;/strong&gt; command. From hereon, we will refer to this directory as &lt;strong&gt;wasroot&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;Next, install the extended runtime jars and the admin jars. It will ask for exactly the same questions.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;java -jar wlp-beta-extended.xx.xx.xx.jar
java -jar wlp-beta-admin.xx.xx..xx.jar
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&quot;installing-the-liberty-profile-in-eclipse&quot;&gt;Installing the Liberty Profile in Eclipse&lt;/h2&gt;

&lt;p&gt;If the &lt;a href=&quot;https://developer.ibm.com/wasdev/downloads/liberty-profile-beta/&quot;&gt;WASDev.net&lt;/a&gt; page is still open in your browser, look for the &lt;strong&gt;Online&lt;/strong&gt; icon. Drag and drop the icon with the Eclipse logo inside an open instance of Eclipse. That will launch an installation window. &lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/wasdev-page-net.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;You can also install the Liberty tools in Eclipse by using the Eclipse Market Place.&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;Open Eclipse&lt;/li&gt;
  &lt;li&gt;From the Help menu, click &lt;strong&gt;Market Place&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;Search for &lt;strong&gt;WebSphere&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;Look for Liberty Profile for Luna. If you are using Eclipse Luna, otherwise, find the Liberty Profile for whatever edition of Eclipse you are using&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;Follow the prompts. Eclipse will restart after the installation&lt;/p&gt;

&lt;h3 id=&quot;creating-the-server-in-eclipse&quot;&gt;Creating the server in Eclipse&lt;/h3&gt;

&lt;ol&gt;
  &lt;li&gt;Click &lt;strong&gt;File&lt;/strong&gt;, &lt;strong&gt;New&lt;/strong&gt;, &lt;strong&gt;Other&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;Find the &lt;strong&gt;Server&lt;/strong&gt; option&lt;/li&gt;
  &lt;li&gt;When asked to select the Server type, look for the &lt;strong&gt;IBM&lt;/strong&gt; folder and click &lt;strong&gt;WebSphere Application Server v8.5 Liberty&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;Accept the default server name&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;It will ask you to either point to an existing Liberty Profile runtime or define a new location. We have already installed our runtime above, so we will use that folder&lt;/p&gt;

&lt;h1 id=&quot;creating-servers&quot;&gt;Creating Servers&lt;/h1&gt;

&lt;p&gt;The server scripts that you will need need are inside the *wasroot*/bin folder. For purposes of convenience, you may include that folder into your system path so you can invoke them from any directory&lt;/p&gt;

&lt;p&gt;You can define many servers inside the Liberty Profile. Each server you create will be stored in the &lt;strong&gt;wasroot&lt;/strong&gt; /usr/servers folder. Each server will have its own self contained configuration. &lt;/p&gt;

&lt;p&gt;To create a server, run the command&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;server create firstServer
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;This will create a server folder named firstServer. You should now see it in &lt;strong&gt;wasroot&lt;/strong&gt; /usr/servers/firstServer&lt;/p&gt;

&lt;h2 id=&quot;starting-and-stopping-servers&quot;&gt;Starting and Stopping servers&lt;/h2&gt;

&lt;p&gt;To start a server, use the command &lt;strong&gt;server run firstServer&lt;/strong&gt;. This will run the server in the foreground, which means you will see all the console logs on the terminal. You can also start the server using the command &lt;strong&gt;server start firstServer&lt;/strong&gt; which will run the server in the background. You will not see the cconsole logs in the terminal, but they are logged inside &lt;strong&gt;wasroot&lt;/strong&gt; /logs/console.log&lt;/p&gt;

&lt;p&gt;A server that was started using the &lt;strong&gt;server run&lt;/strong&gt; can be stopped by pressing &lt;strong&gt;CTRL C&lt;/strong&gt;. If you started the server using &lt;strong&gt;server start&lt;/strong&gt;, you need to stop it using the command &lt;strong&gt;server stop firstServer&lt;/strong&gt;&lt;/p&gt;

&lt;h2 id=&quot;server-configuration&quot;&gt;Server Configuration&lt;/h2&gt;

&lt;p&gt;The server configuration in WAS 8.5 is now a development artifact. This is a good thing because it is now simpler to include the server configuration in source control. When a server is created, you are given a really basic configuration file, but it has reasonable defaults. The Liberty Profile is configured by exception. It operates from a set of built-in configuration settings. &lt;/p&gt;

&lt;p&gt;Each server you will create will reside inside the &lt;strong&gt;wasroot&lt;/strong&gt; /usr/servers folder. In our earlier example, we created a server named &lt;strong&gt;firstserver&lt;/strong&gt;. That server definition is inside &lt;strong&gt;wasroot&lt;/strong&gt; /usr/servers/firstserver&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;.
├── apps
├── dropins
├── logs
├── server.xml
└── workarea

4 directories, 1 file
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The &lt;strong&gt;server.xml&lt;/strong&gt; file is located in the top level directory of a defined server, which in our case is named &lt;strong&gt;firstserver&lt;/strong&gt;. If you try to run this server right now, without adding any JSP, Servlet or even a static HTMl file, you won’t see much. You will simply see the greeting page of the Liberty Profile. We will add some development assets to this project in a little while.&lt;/p&gt;

&lt;p&gt;The listing below is an example of a server configuration for a newly created server&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;server description=&quot;new server&quot;&amp;gt;

    &amp;lt;!-- Enable features --&amp;gt;
    &amp;lt;featureManager&amp;gt;
        &amp;lt;feature&amp;gt;jsp-2.2&amp;lt;/feature&amp;gt;
    &amp;lt;/featureManager&amp;gt;

    &amp;lt;!-- To access this server from a remote client add a host attribute to the following element, e.g. host=&quot;*&quot; --&amp;gt;
    &amp;lt;httpEndpoint httpPort=&quot;9080&quot; httpsPort=&quot;9443&quot; id=&quot;defaultHttpEndpoint&quot;/&amp;gt;

&amp;lt;/server&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The Liberty Profile’s default port number is &lt;strong&gt;9080&lt;/strong&gt;. It will listen on this port the moment you start a server. If you want your application to listen to another port number e.g. port 80, you simply need to change the value of &lt;strong&gt;httpPort&lt;/strong&gt; in the server.xml file.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;server description=&quot;new server&quot;&amp;gt;

    &amp;lt;!-- Enable features --&amp;gt;
    &amp;lt;featureManager&amp;gt;
        &amp;lt;feature&amp;gt;jsp-2.2&amp;lt;/feature&amp;gt;
    &amp;lt;/featureManager&amp;gt;

    &amp;lt;httpEndpoint httpPort=&quot;80&quot; id=&quot;defaultHttpEndpoint&quot; host=&quot;*&quot;/&amp;gt;

&amp;lt;/server&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The listing above has two changes. The httpPort is now listening on port 80. Which means that when you access this on your browser, you won’t need to specify the port number anymore because 80 is the default for all browsers when it makes an HTTP request. The other change is that I have added a &lt;strong&gt;host&lt;/strong&gt; attribute in the httpEndPoint element. This will allow you to access the application from any device other localhost. If you don’t specify the host attribute, you will not be able to access your application from remote machines.&lt;/p&gt;

&lt;p&gt;The server.xml configuration file has the following characteristics&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;It is purely XML. Hence you can manage it using the simplest of editors. As such, it also human readable&lt;/li&gt;
  &lt;li&gt;It is small, easy to back up and simple to include in a source control&lt;/li&gt;
  &lt;li&gt;It can be shared across an application development team&lt;/li&gt;
  &lt;li&gt;Composeable and feature driven. Features that you will add can easily add their own configuration to the system&lt;/li&gt;
  &lt;li&gt;Forgiving. If you missed an attribute on the feature declaration, the built-in default values will kick in. The level of frustration is low&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;features&quot;&gt;Features&lt;/h2&gt;

&lt;p&gt;Features are the units of functionality by which you control the pieces of the runtime environment that are loaded into a particular server.&lt;/p&gt;

&lt;p&gt;Use the configuration file server.xml to declare which features you want to load. The set of features is enclosed within the **&lt;featuremanager&gt;** element, and each feature within the **&lt;feature&gt;** subelement. For example:&lt;/feature&gt;&lt;/featuremanager&gt;&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;server description=&quot;boo server&quot;&amp;gt;
  &amp;lt;dataSource jndiName=&quot;jdbc/mysql&quot;&amp;gt;
    &amp;lt;jdbcDriver id=&quot;mysqlDriver&quot; libraryRef=&quot;mysql-connector&quot;/&amp;gt;
    &amp;lt;properties URL=&quot;jdbc:mysql://localhost/liberty&quot; password=&quot;&quot; user=&quot;root&quot;/&amp;gt;
  &amp;lt;/dataSource&amp;gt;
 
  &amp;lt;library description=&quot;MySQL JDBC Driver&quot; id=&quot;mysql-connector&quot; name=&quot;MySQL Connector&quot;&amp;gt;
    &amp;lt;fileset dir=&quot;/Users/ted/progtools/mconnector&quot; id=&quot;mysql-connector-jar&quot; includes=&quot;mysql-connector-java-*.jar&quot;/&amp;gt;
  &amp;lt;/library&amp;gt;
 
  &amp;lt;featureManager&amp;gt;
    &amp;lt;feature&amp;gt;localConnector-1.0&amp;lt;/feature&amp;gt;
    &amp;lt;feature&amp;gt;servlet-3.0&amp;lt;/feature&amp;gt;
    &amp;lt;feature&amp;gt;jdbc-4.0&amp;lt;/feature&amp;gt;
  &amp;lt;/featureManager&amp;gt;
 
  &amp;lt;applicationMonitor updateTrigger=&quot;mbean&quot;/&amp;gt;
 
  &amp;lt;application id=&quot;AccessMySQL&quot; location=&quot;AccessMySQL.war&quot; name=&quot;AccessMySQL&quot; type=&quot;war&quot;/&amp;gt;
  &amp;lt;httpEndpoint httpPort=&quot;9080&quot; httpsPort=&quot;9443&quot; id=&quot;defaultHttpEndpoint&quot;/&amp;gt;

    &amp;lt;webApplication id=&quot;DBAccess&quot; location=&quot;DBAccess.war&quot; name=&quot;DBAccess&quot;/&amp;gt;
&amp;lt;/server&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The listing above is a server that uses quite a few features. It uses jdbc4 and servlet3 features. The localconnector was added to this server so we can use the Java Management Extensions, &lt;strong&gt;jmx&lt;/strong&gt;, on this local server. It just speed things up a bit. For a more complete list of features, refer to &lt;a href=&quot;http://www-01.ibm.com/support/knowledgecenter/api/content/nl/en-us/was_beta_liberty/com.ibm.websphere.wlp.nd.multiplatform.doc/ae/rwlp_feat.html&quot;&gt;Liberty Features page&lt;/a&gt;&lt;/p&gt;

&lt;h2 id=&quot;eclipse&quot;&gt;Eclipse&lt;/h2&gt;

&lt;p&gt;Use the context menu of Eclipse to start, stop and restart the server&lt;/p&gt;

&lt;h1 id=&quot;deploying-apps&quot;&gt;Deploying Apps&lt;/h1&gt;

&lt;h2 id=&quot;eclipse-1&quot;&gt;Eclipse&lt;/h2&gt;

&lt;h2 id=&quot;command-line&quot;&gt;Command Line&lt;/h2&gt;

&lt;h1 id=&quot;recipes&quot;&gt;Recipes&lt;/h1&gt;

&lt;h2 id=&quot;jsp&quot;&gt;JSP&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;File, &lt;strong&gt;Dynamic Web Project&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;Use the default location&lt;/li&gt;
  &lt;li&gt;Check that Target Runtime is &lt;strong&gt;WebSphere Application Server Liberty Profile&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;Dynamic web module version is 3.0&lt;/li&gt;
  &lt;li&gt;Uncheck &lt;strong&gt;Add the project to an EAR&lt;/strong&gt;, we will not be adding this to an Enterprise Archive&lt;/li&gt;
  &lt;li&gt;We do not need to generate a web.xml file for this exercise, we will use the defaults of the Liberty profile. Click Finish&lt;/li&gt;
  &lt;li&gt;On the &lt;strong&gt;Servers&lt;/strong&gt; tab of Eclipse, use the context menu of our defined Liberty Profile server. Right click and choose &lt;strong&gt;Add and Remove&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;Add a JSP file to our Project. Right click on the project, choose &lt;strong&gt;New JSP&lt;/strong&gt;. Accept the defaults, they are reasonably right&lt;/li&gt;
  &lt;li&gt;When the JSP editor opens in Eclipse, just add whatever text you want inside the &lt;strong&gt;body&lt;/strong&gt; tag&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;9.. Add the JSPSample project. Choose it from the list of &lt;strong&gt;Available&lt;/strong&gt;, click the Add button so it goes to the &lt;strong&gt;Configured&lt;/strong&gt; list and click &lt;strong&gt;Finish&lt;/strong&gt;&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;On the servers tab, right click on our defined Liberty server, and choose &lt;strong&gt;Start&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;Open a browser and open &lt;a href=&quot;http://localhost:9080/JSPSample/Sample.jsp&quot;&gt;http://localhost:9080/JSPSample/Sample.jsp&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;servlet&quot;&gt;Servlet&lt;/h2&gt;

&lt;p&gt;Create a new project in Eclipse, &lt;strong&gt;File&lt;/strong&gt;, &lt;strong&gt;New&lt;/strong&gt; then choose &lt;strong&gt;Dynamic Web Project&lt;/strong&gt;. Give the project a name, say, &lt;strong&gt;third&lt;/strong&gt;  and then uncheck the &lt;strong&gt;Add Project to an EAR&lt;/strong&gt;. We won’t be using EAR files still. Follow the prompts and click Finish. Add a Servlet to project third, give it a name and specify any package to your liking. Implement the &lt;strong&gt;doGet()&lt;/strong&gt; method and add a simple println command to it&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;package com.tedhagos;

import java.io.IOException;
import java.io.PrintWriter;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/**
 * Servlet implementation class Tres
 */
@WebServlet(&quot;/Tres&quot;)
public class Tres extends HttpServlet {
private static final long serialVersionUID = 1L;
       
    /**
     * @see HttpServlet#HttpServlet()
     */
    public Tres() {
        super();
        // TODO Auto-generated constructor stub
    }

/**
 * @see HttpServlet#doGet(HttpServletRequest request, HttpServletResponse response)
 */
protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
// TODO Auto-generated method stub
PrintWriter out = response.getWriter();
out.println(&quot;Hello Tres&quot;);
}
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;In our JSP example earlier, we deployed the JSP project by using the &lt;strong&gt;Add and Remove&lt;/strong&gt; context menu of the Liberty Profile server in Eclipse. For this example, we will explore the command line deployment.  &lt;/p&gt;

&lt;p&gt;Export a war file for our project third. You can do this in Eclipse by clicking &lt;strong&gt;File&lt;/strong&gt;, &lt;strong&gt;Export&lt;/strong&gt;, then find the folder named Web, select &lt;strong&gt;WAR file&lt;/strong&gt;. Click the next button. Choose project third from the Web Project drop down. Choose a destination folder. This folder is where Eclipse will write the war file.&lt;/p&gt;

&lt;p&gt;Next, copy the export war file to &lt;strong&gt;wasroot&lt;/strong&gt; /usr/servers/firstserver/dropins. The &lt;strong&gt;dropins&lt;/strong&gt; folder is constantly polled by the Liberty Profile. Any WAR or EAR file that you put here will be decompressed and deployed.  &lt;/p&gt;

&lt;h2 id=&quot;jdbc&quot;&gt;JDBC&lt;/h2&gt;

&lt;p&gt;There are 
1.  Prepare the database
2.  Server XML features
3.  The servlet which that access the database&lt;/p&gt;

&lt;h3 id=&quot;database-preparation&quot;&gt;Database preparation&lt;/h3&gt;

&lt;p&gt;We will use a MySQL database for this example. The SQL script in the code listing below basically creates a database named “liberty” and  a table named “users” with a single column. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;drop database if exists liberty;
create database liberty;
use liberty;
create table users (
  id MEDIUMINT NOT NULL AUTO_INCREMENT,
    name CHAR(30) NOT NULL,
      PRIMARY KEY (id));
desc users;
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&quot;server-configuration-1&quot;&gt;Server configuration&lt;/h3&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;server description=&quot;boo server&quot;&amp;gt;
  &amp;lt;dataSource jndiName=&quot;jdbc/mysql&quot;&amp;gt;
    &amp;lt;jdbcDriver id=&quot;mysqlDriver&quot; libraryRef=&quot;mysql-connector&quot;/&amp;gt;
    &amp;lt;properties URL=&quot;jdbc:mysql://localhost/liberty&quot; password=&quot;&quot; user=&quot;root&quot;/&amp;gt;
  &amp;lt;/dataSource&amp;gt;
 
  &amp;lt;library description=&quot;MySQL JDBC Driver&quot; id=&quot;mysql-connector&quot; name=&quot;MySQL Connector&quot;&amp;gt;
    &amp;lt;fileset dir=&quot;/Users/ted/progtools/mconnector&quot; id=&quot;mysql-connector-jar&quot; includes=&quot;mysql-connector-java-*.jar&quot;/&amp;gt;
  &amp;lt;/library&amp;gt;
 
  &amp;lt;featureManager&amp;gt;
    &amp;lt;feature&amp;gt;localConnector-1.0&amp;lt;/feature&amp;gt;
    &amp;lt;feature&amp;gt;servlet-3.0&amp;lt;/feature&amp;gt;
    &amp;lt;feature&amp;gt;jdbc-4.0&amp;lt;/feature&amp;gt;
  &amp;lt;/featureManager&amp;gt;
 
  &amp;lt;applicationMonitor updateTrigger=&quot;mbean&quot;/&amp;gt;
 
  &amp;lt;application id=&quot;AccessMySQL&quot; location=&quot;AccessMySQL.war&quot; name=&quot;AccessMySQL&quot; type=&quot;war&quot;/&amp;gt;
  &amp;lt;httpEndpoint httpPort=&quot;9080&quot; httpsPort=&quot;9443&quot; id=&quot;defaultHttpEndpoint&quot;/&amp;gt;

    &amp;lt;webApplication id=&quot;DBAccess&quot; location=&quot;DBAccess.war&quot; name=&quot;DBAccess&quot;/&amp;gt;
&amp;lt;/server&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&quot;session-bean&quot;&gt;Session Bean&lt;/h2&gt;

&lt;h2 id=&quot;jpa&quot;&gt;JPA&lt;/h2&gt;

&lt;h2 id=&quot;message-driven-bean&quot;&gt;Message Driven Bean&lt;/h2&gt;

&lt;h1 id=&quot;jvm-tuning&quot;&gt;JVM Tuning&lt;/h1&gt;

&lt;p&gt;Each server created in the Liberty Profile comes with the default JVM options. The defaults are reasonable but in case you need to tweak it, you simply need to create a file named jvm.options and place it at the root directory of a server folder. In our case, if we would like to alter some settings of JVM for our &lt;strong&gt;firstserver&lt;/strong&gt;, we need to create the options file and place in &lt;strong&gt;wasroot&lt;/strong&gt; /usr/servers/firstserver/jvm.options. If you want the JVM options to be applicable to all servers, put the options file in &lt;strong&gt;wasroot&lt;/strong&gt; /etc/jvm.options.&lt;/p&gt;

&lt;p&gt;A change in the JVM options is one of the rare occassions where you have to restart your servers.  &lt;/p&gt;

&lt;p&gt;One of the most settings tweak that is done to the JVM is to alter the memory allocated to JVM. By default, Liberty Profile has the following setting&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;-Xms4m
-Xmx488m
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;That means the minimum Java heap size is 4 megabytes and the maximum heap size is 488 megabytes&lt;/p&gt;

&lt;p&gt;On the rare occassion that you want to see a verbose log of the garbage collection process, you can include the following setting on the jvm.options. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;verbose:gc
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;There are many settings that can be set in the jvm.options file, for a complete reference, you can consult this page &lt;a href=&quot;http://jvm-options.tech.xebia.fr&quot;&gt;JVM Options page at Xebia.fr&lt;/a&gt;&lt;/p&gt;

&lt;h1 id=&quot;server-options&quot;&gt;Server options&lt;/h1&gt;

&lt;h1 id=&quot;resources&quot;&gt;Resources&lt;/h1&gt;

&lt;ol&gt;
  &lt;li&gt;Redbooks&lt;/li&gt;
  &lt;li&gt;IBM&lt;/li&gt;
&lt;/ol&gt;

</content>
 </entry>
 
 <entry>
   <title>First Program</title>
   <link href="http://tedhagos.com/nodejs/firstprogram.html"/>
   <updated>2014-11-05T00:00:00+08:00</updated>
   <id>http://tedhagos.com/nodejs/firstprogram</id>
   <content type="html">
&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-javascript&quot; data-lang=&quot;javascript&quot;&gt;&lt;span class=&quot;c1&quot;&gt;// server.js&lt;/span&gt;
    
&lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;http&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;require&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;http&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;nx&quot;&gt;http&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;createServer&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;kd&quot;&gt;function&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;req&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;res&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;nx&quot;&gt;res&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;writeHead&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;200&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;Content-Type&amp;#39;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&amp;#39;text/plain&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;});&lt;/span&gt;
  &lt;span class=&quot;nx&quot;&gt;res&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;end&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;Hello World\n&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;}).&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;listen&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1337&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&amp;#39;127.0.0.1&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;nx&quot;&gt;console&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;log&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;Server running at http://127.0.0.1:1337/&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;What is it doing
Why do we have to write our own webserver
How come it is not like Apache where all I have to do is write HTML files and plunk it into the DocRoot
What is the deal with the call back
And why that self executing function&lt;/p&gt;

&lt;p&gt;Can it be written in clearer, more verbose way&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-javascript&quot; data-lang=&quot;javascript&quot;&gt;&lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;http&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;require&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;http&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;path&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;require&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;path&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;fs&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;require&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;fs&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;url&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;require&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;url&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;

&lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;pages&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;s1&quot;&gt;&amp;#39;&amp;#39;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;Landing page&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;nx&quot;&gt;about&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&amp;#39;About&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;nx&quot;&gt;foo&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;Yeah Foo&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;nx&quot;&gt;goo&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;Yeah Goo&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;nx&quot;&gt;xoo&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;callxoo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;};&lt;/span&gt;

&lt;span class=&quot;kd&quot;&gt;function&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;callxoo&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(){&lt;/span&gt;
  &lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;retval&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;nx&quot;&gt;empname&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;Ted Hagos&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
    &lt;span class=&quot;nx&quot;&gt;email&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;tedhagos@gmail.com&amp;quot;&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;};&lt;/span&gt;
  &lt;span class=&quot;c1&quot;&gt;// I cannot return the bare JS object because&lt;/span&gt;
  &lt;span class=&quot;c1&quot;&gt;// the HTTP object of node will throw an error.&lt;/span&gt;
  &lt;span class=&quot;c1&quot;&gt;// First type argument must be a string or buffer&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;JSON&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;stringify&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;retval&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
  
&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;

&lt;span class=&quot;nx&quot;&gt;http&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;createServer&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;kd&quot;&gt;function&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;request&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;response&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;){&lt;/span&gt;
    
    &lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;lookup&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;path&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;basename&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;decodeURI&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;request&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;url&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;));&lt;/span&gt;
    &lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;thedata&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;kc&quot;&gt;null&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
    &lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;temp&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;kc&quot;&gt;null&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
    
    &lt;span class=&quot;c1&quot;&gt;// Check first if we are looking for an HTML file&lt;/span&gt;
    &lt;span class=&quot;nx&quot;&gt;fs&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;exists&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;lookup&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;kd&quot;&gt;function&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;exists&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;){&lt;/span&gt;
      &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;exists&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;){&lt;/span&gt;
        &lt;span class=&quot;nx&quot;&gt;fs&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;readFile&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;lookup&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&amp;#39;utf-8&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;kd&quot;&gt;function&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;woops&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;filecontent&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;){&lt;/span&gt;
          &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;!&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;woops&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;
            &lt;span class=&quot;c1&quot;&gt;//console.log(filecontent);&lt;/span&gt;
            &lt;span class=&quot;nx&quot;&gt;response&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;writeHead&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;200&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;Content-Type&amp;#39;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&amp;#39;text/html&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;});&lt;/span&gt;
            &lt;span class=&quot;nx&quot;&gt;response&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;end&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;filecontent&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
          &lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;
        &lt;span class=&quot;p&quot;&gt;});&lt;/span&gt; 
      &lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;
      &lt;span class=&quot;k&quot;&gt;else&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;
        &lt;span class=&quot;nx&quot;&gt;console&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;log&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&amp;quot;inside the else&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
      &lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;});&lt;/span&gt;
        
    &lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;pages&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;lookup&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;!=&lt;/span&gt; &lt;span class=&quot;kc&quot;&gt;undefined&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;){&lt;/span&gt;
      &lt;span class=&quot;nx&quot;&gt;console&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;log&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&amp;quot;inside pages&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
      &lt;span class=&quot;nx&quot;&gt;response&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;writeHead&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;200&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;s1&quot;&gt;&amp;#39;Content-Type&amp;#39;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&amp;#39;text/html&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;});&lt;/span&gt;
      &lt;span class=&quot;nx&quot;&gt;response&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;end&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;pages&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;lookup&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]);&lt;/span&gt;
    &lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;           
  &lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;).&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;listen&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;8080&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

</content>
 </entry>
 
 <entry>
   <title>Prep</title>
   <link href="http://tedhagos.com/websphere/01-Preparation.html"/>
   <updated>2014-11-04T00:00:00+08:00</updated>
   <id>http://tedhagos.com/websphere/01-Preparation</id>
   <content type="html">&lt;p&gt;You have to download it first.&lt;/p&gt;

&lt;h1 id=&quot;editions&quot;&gt;Editions&lt;/h1&gt;

&lt;h1 id=&quot;what-you-need&quot;&gt;What you need&lt;/h1&gt;

</content>
 </entry>
 
 <entry>
   <title>Vim Setup</title>
   <link href="http://tedhagos.com/linux/vim-setup.html"/>
   <updated>2014-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/linux/vim-setup</id>
   <content type="html">&lt;p&gt;Most of Vim addons can be installed manually, you just need to put them inside inside &lt;code&gt;~/.vim/&lt;/code&gt; folder, but it’s a lot simpler to automate that install/config process if you use some sort of a Vim package manager. There are a couple out there, one of them is &lt;strong&gt;Vundle&lt;/strong&gt;. First thing to do is to grab it from github. &lt;/p&gt;

&lt;p&gt;&lt;code&gt;$ git clone https://github.com/gmarik/Vundle.vim.git ~/.vim/bundle/Vundle.vim&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Add the following lines to ~/.vimrc&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;set rtp+=~/.vim/bundle/vundle/Vundle.vim
call vundle#begin()

Plugin &#39;gmarik/Vundle.vim&#39;
Plugin &#39;MarcWeber/vim-addon-mw-utils&#39;
Plugin &#39;Valloric/YouCompleteMe&#39;
Plugin &#39;tomtom/tlib_vim&#39;
Bundle &#39;snipmate-snippets&#39;
Plugin &#39;honza/vim-snippets&#39;
Plugin &#39;garbas/vim-snipmate&#39;
Plugin &#39;tpope/vim-rails.git&#39;
Plugin &#39;tpope/vim-fugitive&#39;
Plugin &#39;tpope/vim-surround&#39;
Plugin &#39;tpope/vim-sleuth&#39;
Plugin &#39;tpope/vim-markdown.git&#39;
Plugin &#39;ricardovaleriano/vim-github-theme.git&#39;

call vundle#end()
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You don’t have to install all those plugins, go and read up what those plugins are supposed to do before you add them to your own vimrc. &lt;/p&gt;

&lt;p&gt;If you used Vim to edit .vimrc, you can switch command mode and execute &lt;code&gt;:PluginInstall&lt;/code&gt;. If you didn’t use vim to edit vimrc, you can start vim and install the plugins using the command &lt;code&gt;vim +PluginInstall +qall&lt;/code&gt;. &lt;/p&gt;

&lt;p&gt;There are more instructions from the &lt;a href=&quot;https://github.com/gmarik/Vundle.vim&quot;&gt;GitHub pages of Vundle&lt;/a&gt; &lt;/p&gt;

&lt;h2 id=&quot;resources&quot;&gt;Resources&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;A working .vimrc is &lt;a href=&quot;../resources/linux/vimrc.txt&quot;&gt;available here&lt;/a&gt;, rename it to &lt;em&gt;.vimrc&lt;/em&gt; and put it in your home folder &lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;other-settings&quot;&gt;Other settings&lt;/h2&gt;

&lt;table border=&quot;2&quot; cellspacing=&quot;0&quot; cellpadding=&quot;6&quot; rules=&quot;groups&quot; frame=&quot;hsides&quot;&gt;
&lt;colgroup&gt;
&lt;col class=&quot;left&quot; /&gt;
&lt;col class=&quot;left&quot; /&gt;
&lt;/colgroup&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`set nocompatible`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Does practically nothing, but better safe than sorry&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`filetype off`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;*Vundle* requires this, so just go with it&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`set expandtab`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Use spaces rather than tabs&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`set shiftwidth=2`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;The number of spaces to use for indent&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`set ruler`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;So you can see col and row position of cursor&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`colorscheme`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Just so it&#39;s easy on the eyes&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`:imap ;; &lt;esc&gt;`
&lt;td class=&quot;left&quot;&gt;So that I only have to press `;` 2x to enter normal mode&lt;/td&gt;

&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`abbr`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Use this for autocorrect entries&lt;/td&gt;
&lt;/tr&gt;





&lt;/esc&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/tbody&gt;&lt;/table&gt;
</content>
 </entry>
 
 <entry>
   <title>OSX Cheatsheet</title>
   <link href="http://tedhagos.com/osx/osx-cheatsheet.html"/>
   <updated>2014-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/osx/osx-cheatsheet</id>
   <content type="html">&lt;h1 id=&quot;routing-table&quot;&gt;Routing table&lt;/h1&gt;

&lt;p&gt;The routing table does not show using &lt;code&gt;route print&lt;/code&gt; in OSX, this may have worked in Linux but not here. Use the command &lt;code&gt;netstat -nr&lt;/code&gt; instead&lt;/p&gt;

&lt;h1 id=&quot;showhide-hidden-files-finder&quot;&gt;Show/Hide hidden files Finder&lt;/h1&gt;

&lt;p&gt;The OSX Finder does not show hidden files by default. A file is hidden if the name starts with a dot or period e.g. &lt;em&gt;.bash_profile&lt;/em&gt;, &lt;em&gt;.emacs.d&lt;/em&gt; etc. If you want to see these kinds of files in the Finder, execute this commands using the Terminal.app&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ defaults write com.apple.finder AppleShowAll Files -bool YES
$ killall Finder
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;To reverse the process, execute the following commands&lt;/p&gt;

&lt;p&gt;The &lt;code&gt;killall&lt;/code&gt; command will restart the Finder. After it has restarted, you should be able to see hidden files.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ defaults write com.apple.finder AppleShowAll Files -bool NO
$ killall Finder
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;setting-the-hostname&quot;&gt;Setting the hostname&lt;/h1&gt;

&lt;p&gt;The &lt;code&gt;hostname&lt;/code&gt; command, when executed on a terminal window will give you, well, the hostname, which is the name of your machine. On a networked environment, this hostname gets affected by DHCP; it overrides the hostname of your machine. If you want to set the hostname, execute &lt;code&gt;sudo scutil --set HostName anyname_you_want&lt;/code&gt; on a terminal window&lt;/p&gt;

&lt;h1 id=&quot;anti-alias&quot;&gt;Anti alias&lt;/h1&gt;

&lt;p&gt;On the terminal, type&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ defaults --currentHost write -globalDomain AppleFontSmoothing -int 1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The value 1 in &lt;code&gt;-int 1&lt;/code&gt; means light anti aliasing, a value of &lt;em&gt;2&lt;/em&gt; will set anti aliasing to medium and and &lt;em&gt;3&lt;/em&gt; will be for strong anti aliasing&lt;/p&gt;

&lt;h1 id=&quot;homebrew-package-management&quot;&gt;Homebrew package management&lt;/h1&gt;

&lt;p&gt;You need XCode before you can install homebrew. Make sure you have downloaded it from the AppStore and that you have also installed it’s command line utilities. After that, get a terminal window and type&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ ruby -e &quot;$(curl -fsSL https://raw.github.com/Homebrew/homebrew/go/install)&quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Ruby comes packaged with OSX so this command will work. Now test it out.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ brew install git # will install git
$ brew install wget
$ brew install curl 
$ brew install axel # wget in steroids
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;colored-terminal-window&quot;&gt;Colored terminal window&lt;/h1&gt;

&lt;p&gt;Open up &lt;code&gt;~/.bash_profile&lt;/code&gt; and add the following line&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;alias ls=&quot;ls -G&quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;picture-file-location-of-photobooth&quot;&gt;Picture file location of Photobooth&lt;/h1&gt;

&lt;p&gt;They are in &lt;em&gt;~/Pictures/Photo Booth Library Pictures&lt;/em&gt;&lt;/p&gt;

&lt;h1 id=&quot;cli-copy-paste&quot;&gt;CLI Copy Paste&lt;/h1&gt;

&lt;p&gt;To copy content of files while on a terminal, use &lt;code&gt;pbcopy&lt;/code&gt; e.g. &lt;code&gt;cat somefile | pbcopy&lt;/code&gt;. The contents of somefile are now on the clipboard. You can paste it with the usual &lt;code&gt;CTRL v&lt;/code&gt; if you are using any GUI editor. If you need to perform a paste on the terminal, use &lt;code&gt;pbpaste&lt;/code&gt; e.g. &lt;code&gt;echo pbpaste&lt;/code&gt;, the contents of the clipboard will be echoed to the terminal.&lt;/p&gt;

&lt;h1 id=&quot;assistive-technology-in-109-mavericks&quot;&gt;Assistive Technology in 10.9 (mavericks)&lt;/h1&gt;

&lt;p&gt;Not anymore in its usual place. This is now in &lt;code&gt;System Preferences -&amp;gt; Security and Privacy -&amp;gt; Privacy&lt;/code&gt;. TextExpander will not work as soon as you upgrade to 10.9. But if you quickly visit and reenable TextExpander in Assistive technology dialog window, all will be well again&lt;/p&gt;

&lt;h1 id=&quot;scanning&quot;&gt;Scanning&lt;/h1&gt;

&lt;p&gt;Use &lt;em&gt;Image capture&lt;/em&gt;, the &lt;em&gt;Scaner and Printer Queue&lt;/em&gt; or &lt;em&gt;Preview&lt;/em&gt;. All of these built-in software in have capabilities for image capture&lt;/p&gt;

&lt;h1 id=&quot;turn-off-iphoto-when-an-ios-device-connects&quot;&gt;Turn off iPhoto when an iOS device connects&lt;/h1&gt;

&lt;p&gt;When you plug an iOS device, iPhoto quickly springs into action offering to sync Photos. You can turn it off
1.  Launch &lt;em&gt;Image Capture&lt;/em&gt;, &lt;code&gt;cmd + space&lt;/code&gt; then type Image Capture in Spotlight
2.  On the lower left region of &lt;em&gt;Image Capture&lt;/em&gt; window, look for &lt;strong&gt;Connecting this iPad opens iPhoto&lt;/strong&gt;
3.  Click the dropdown. Choose &lt;strong&gt;No Application&lt;/strong&gt;&lt;/p&gt;

&lt;h1 id=&quot;external-usb-drives&quot;&gt;External USB drives&lt;/h1&gt;

&lt;p&gt;You can &lt;code&gt;cd&lt;/code&gt; to the external drive in &lt;code&gt;~/Volumes&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;back-up-mailapp&quot;&gt;Back up Mail.app&lt;/h1&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;code&gt;mkdir ~/MailBackup&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;cd ~/MailBackup&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;cp -r ~/Library/Mail/V2&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;

&lt;h1 id=&quot;convert-matroska-mkv-files-to-mp4&quot;&gt;Convert matroska (mkv) files to mp4&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;ffmpeg i movietitle.mkv -vcodec copy -acodec copy movietitle.mp4&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;This only involves changing of containers. No encoding or conversion will happen. If you have a lot of mkv files to convert, a script file is a better approach. On the same folder where the mkv files are located, create a script file, say &lt;em&gt;convert.sh&lt;/em&gt;, type the following script inside the script&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;for i in $(ls *mkv); do ffmpeg -i -vcodec copy -acodec copy $i.mp4; done
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;chmod u+x convert.sh&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;convert.sh&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;share-internet&quot;&gt;Share internet&lt;/h1&gt;

&lt;p&gt;If you don’t mind OSX changing changing the subnet when &lt;em&gt;Internet Sharing&lt;/em&gt; is activated, you don’t have to do this. If on the other hand, you have other devices that needs a fixed subnet e.g. file servers, other PCs that have statically defined IPs, then you need to control OSX’s behavior on NAT&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo defaults write /Library/Preferences/SystemConfiguration/com.apple.nat NAT -dict-add SharingNetworkNumberStart 192.168.3.0&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;simple-music-player&quot;&gt;Simple music player&lt;/h1&gt;

&lt;p&gt;Get Cogx, head over to &lt;a href=&quot;http://cogx.org&quot;&gt;cogx.org&lt;/a&gt;&lt;/p&gt;

&lt;h1 id=&quot;squeaky-clean-hard-drive&quot;&gt;Squeaky clean hard drive&lt;/h1&gt;

&lt;p&gt;Removing apps is simple, just go the &lt;em&gt;Applications folder and remove it. But there are residual files left when doing this. Also those language files take up hard drive. If you are using an SSD with limited drive space, then every Gig counts. Get a commercial app. /Clean My Mac&lt;/em&gt; is decent and cheap enough&lt;/p&gt;

&lt;h1 id=&quot;download-accelerator&quot;&gt;Download accelerator&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;brew install axel&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;axel -n 10 http://wordpress.org/latest.zip&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The int value &lt;em&gt;10&lt;/em&gt; means it will use 10 threads to pull the file simultaneously. It’s similar to &lt;em&gt;wget&lt;/em&gt;, only this time, there are 10 wget running at the same time&lt;/p&gt;

&lt;h1 id=&quot;rip-audio-cds-to-mp3&quot;&gt;Rip audio cds to mp3&lt;/h1&gt;

&lt;p&gt;Get &lt;a href=&quot;http://sbooth.org/Max/&quot;&gt;MAX&lt;/a&gt; from SBooth. Work on the archive files and put it in /Applications folder or where you want them. It’s a GUI tool, so it’s easy to work with.&lt;/p&gt;

&lt;p&gt;MAX uses musicbrainz.org for the CD metadata, the service is not good enough. GraceNotes, which is used by iTunes is more capable. The problem is, I couldn’t find a direct way to connect MAX to GraceNotes. You need a bit of AppleScripting, this solution was the from the SBooth forums as well.&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;Create a Script in AppleScript, Save it anywhere you can find it easily. The script can be found on the link above (the sbooth forum), but I saved it in gist.github.com, here’s the link &lt;a href=&quot;https://gist.github.com/tedhagos/5845190&quot;&gt;https://gist.github.com/tedhagos/5845190&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;When you insert the CD, iTunes will launch as well (you need this)&lt;/li&gt;
  &lt;li&gt;Run the script you created. That will get the meta data of iTunes&lt;/li&gt;
  &lt;li&gt;Rip Away&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;This solution needs both iTunes and MAX running at the same time. When iTunes grabs the metadata from Gracenotes, the script grabs the metadata from iTunes&lt;/p&gt;

&lt;h1 id=&quot;rip-audio-cds-to-flac-format&quot;&gt;Rip audio cds to flac format&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;brew install cdparanoia&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;brew install abcde&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;touch ~/.abcde.conf&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Put the following codes inside ~/.abcde.conf&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;CDROM=/dev/disk1
OUTPUTTYPE=flac
INTERACTIVE=n
PADTRACKS=y
OUTPUTDIR=~/Music/abcde
OUTPUTFORMAT=&#39;${ARTISTFILE}/${ALBUMFILE}/${TRACKNUM} - ${TRACKFILE}&#39;
VAOUTPUTFORMAT=&#39;Various/${ALBUMFILE}/${TRACKNUM} - ${TRACKFILE}&#39;
mungefilename ()
{
  echo &quot;$@&quot; | sed s,:, -,g | tr /* _+ | tr -d &#39;&quot;?[:cntrl:]
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;dev/disk1 is where my CD is mounted, find out where yours is mounted. A simple &lt;code&gt;df&lt;/code&gt; command should give you some clue where the CD drive is mounted. After that, ripping can start. To rip, do the following&lt;/p&gt;

&lt;p&gt;&lt;code&gt;diskutil unmount /dev/disk1&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;abcde&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;diskutil eject /dev/disk1&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The unmounting is necessary because abcde works with raw block devices and cannot function when the CD is mounted. I shamlessly ripped most of this guide from &lt;a href=&quot;http://hints.macworld.com/article.php?story=2005101620055677&quot;&gt;extempore, MacWorld&lt;/a&gt;&lt;/p&gt;

&lt;h1 id=&quot;php-pear&quot;&gt;PHP pear&lt;/h1&gt;

&lt;p&gt;This is no longer included in 10.8, maybe it did not even make it in Leopard, I’m not sure. Just curl it&lt;/p&gt;

&lt;p&gt;&lt;code&gt;curl http://pear.php.net/go-pear.phar &amp;gt; go-pear.phar&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo php -q go-pear.phar&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;PEAR installation will be at ~/pear/, bin, etc and all. Next, update your ~/.bash_profile to include pear in the system path.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;echo &quot;export PATH=$PATH:~/pear/bin:.&quot; &amp;gt;&amp;gt; ~/.bash_profile&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;phar--php-composer&quot;&gt;PHAR &amp;amp; PHP composer&lt;/h1&gt;

&lt;p&gt;Create a php.ini file first. OSX does have it at &lt;em&gt;private/etc. Don’t copy the ini file somewhere else, it needs to be at /private/etc&lt;/em&gt;. Make sure that php.ini is actually in that directory&lt;/p&gt;

&lt;p&gt;&lt;code&gt;cd /private/etc&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo cp php.ini.default php.ini&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Add the following to php.ini, it’s okay to add it at the very beginning, it didn’t give me problems that way&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;detect_unicode = Off
date.timezone = YourCountry/YourCity
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;After saving the update php.ini,&lt;/p&gt;

&lt;p&gt;&lt;code&gt;cd /path/to/your/workarea&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;curl -s http://getcomposer.org/installer | php&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;composer.phar should be downloaded to /path/to/your/workarea. Test it&lt;/p&gt;

&lt;p&gt;&lt;code&gt;php composer.phar&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;write-markdown-files-export-latex-html-etc&quot;&gt;Write markdown files, export LaTex, HTML etc&lt;/h1&gt;

&lt;p&gt;Get Pandoc, there are installation instructions on the &lt;a href=&quot;http://johnmacfarlane.net/pandoc/installing.html&quot;&gt;johnmacfarlane/pandoc&lt;/a&gt; page. If you don’t want to monkey around with Haskell, get the binary distributions. If you prefer the Haskell route on the other hand
1.  Get Haskell, either via brew, macport or fink
2.  Update the system path to include the &lt;em&gt;cabal&lt;/em&gt; binaries, they would have been installed at ~/.cabal/bin
3.  &lt;code&gt;sudo cabal update&lt;/code&gt;
4.  &lt;code&gt;sudo cabal install pandoc&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;show-the-week-number-in-cal&quot;&gt;Show the week number in cal&lt;/h1&gt;

&lt;table border=&quot;2&quot; cellspacing=&quot;0&quot; cellpadding=&quot;6&quot; rules=&quot;groups&quot; frame=&quot;hsides&quot;&gt;


&lt;colgroup&gt;
&lt;col class=&quot;left&quot; /&gt;

&lt;col class=&quot;left&quot; /&gt;
&lt;/colgroup&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`ncal -w`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;shows the week numbers&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`ncal -e`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;shows the dates for Easter&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;h1 id=&quot;keyboard-shortcut-for-full-screen&quot;&gt;Keyboard shortcut for Full Screen&lt;/h1&gt;

&lt;p&gt;There is no built-in or standard shortcut for entering fullscreen. You have to define it. It isn’t like Lubuntu where F10 gets any app into full screen mode. Fortunately, it’s quite easy to add a keyboard shortcut for it.
1.  Go to &lt;strong&gt;System Preferences&lt;/strong&gt; → &lt;strong&gt;Keyboard&lt;/strong&gt; → &lt;strong&gt;App Shortcuts&lt;/strong&gt;
2.  Click &lt;strong&gt;+&lt;/strong&gt; to add a new shortcut
3.  Type &lt;code&gt;Enter Full Screen&lt;/code&gt; in the box labeled as “Menu title”. This essentially is a like programming a macro. You need to type &lt;code&gt;Enter Full Screen&lt;/code&gt; (exactly as it is, including which letter is capitalized) because it is the title of the menu option in most applications responsible for entering full screen mode. If you mess up the spelling or the case of this entry, you won’t get the results you expect.&lt;/p&gt;

&lt;p&gt;Though most applications have the &lt;code&gt;Enter Full Screen&lt;/code&gt; menu title, some applications don’t follow this convention e.g. iTerm2. So you need to verify what is the exact text that an application uses to enter full screen and use what’s appropriate&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/fullscreen.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;h1 id=&quot;cli-music-player&quot;&gt;CLI music player&lt;/h1&gt;

&lt;h2 id=&quot;using-the-built-in-cli-player&quot;&gt;Using the built in CLI player&lt;/h2&gt;

&lt;p&gt;&lt;code&gt;afplay MusicFileName.ext&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;This command will block, it will wait until the track has finished playing before you get returned to the prompt.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;afplay&lt;/code&gt; comes with OSX, it is already there&lt;/p&gt;

&lt;h2 id=&quot;music-on-console-moc&quot;&gt;Music On Console (moc)&lt;/h2&gt;

&lt;p&gt;&lt;code&gt;brew update&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;brew install moc&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;moc will need the Jack Audio. If you pulled moc from brew, jackd may have been installed already. If not, just pull it from brew with &lt;code&gt;brew install jack&lt;/code&gt;.  The jack audio needs to run as daemon before you can use moc &lt;/p&gt;

&lt;p&gt;&lt;code&gt;jackd -d coreaudio&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Now you can run moc.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mocp Folder/&lt;/code&gt; will launch a console UI, you can navigate the tracks using the arrow keys. Press ENTER to play a track. Press &lt;strong&gt;p&lt;/strong&gt; to pause/play. &lt;strong&gt;q&lt;/strong&gt; will exit&lt;/p&gt;

&lt;h1 id=&quot;fixing-homebrew-after-upgrade-to-109&quot;&gt;Fixing homebrew after upgrade to 10.9&lt;/h1&gt;

&lt;p&gt;The &lt;code&gt;brew update&lt;/code&gt; command stopped working after upgrading to Mavericks. Fix it with&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ cd `brew --prefix` 
$ git fetch origin 
$ sudo git reset --hard origin/master
$ brew update
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;screen-shooting&quot;&gt;Screen shooting&lt;/h1&gt;

&lt;p&gt;&lt;strong&gt;shift+cmd+4&lt;/strong&gt; will turn the cursor to a cross hair. You can now click and drag the cross-hair to cover the region of the screen that you want to capture. &lt;/p&gt;

&lt;p&gt;By default, the screen shot will be placed the Desktop. You can change this location on the terminal&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ defaults write com.apple.screencapture location ~/Desktop/screenshots
$ killall SystemUIServer
&lt;/code&gt;&lt;/pre&gt;

</content>
 </entry>
 
 <entry>
   <title>Node Package Manager</title>
   <link href="http://tedhagos.com/nodejs/node-package-manager.html"/>
   <updated>2014-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/nodejs/node-package-manager</id>
   <content type="html">&lt;p&gt;The Debian and Ubuntu repos require that you pull npm separately from nodejs, maybe in the future this will change, but as far as &lt;em&gt;quantal&lt;/em&gt; (Ubuntu codename), I still had to do this&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install nodejs npm&lt;/code&gt; &lt;/p&gt;

&lt;p&gt;Another way to get npm is to do&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install curl&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;curl http://npmjs.org/install.sh | sudo sh&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;osx-install&quot;&gt;OSX install&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;brew update&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;brew instal node&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;npm should be part of the node formula. If the brew route fails, you can always download and install the binary package from &lt;a href=&quot;http://nodejs.org&quot;&gt;nodejs.org&lt;/a&gt;. &lt;/p&gt;

&lt;h1 id=&quot;how-to-use-npm&quot;&gt;How to use npm&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;npm info &amp;lt;name of package&amp;gt;&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The info argument spits out, well, info about a package — without installing it of course. It contains authors, versions, contributors, dependencies etc. It’s in JSON format&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo npm install -g &amp;lt;name of package&amp;gt;&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;If you don’t put the &lt;em&gt;-g&lt;/em&gt; (global) option, the package will be installed in the current directory. You may have to add that directory to the system path variable if you need to run the package from another directory. In global mode, the installed package will be runnable from any directory. Here are some examples of usage. &lt;/p&gt;

&lt;table border=&quot;2&quot; cellspacing=&quot;0&quot; cellpadding=&quot;6&quot; rules=&quot;groups&quot; frame=&quot;hsides&quot;&gt;


&lt;colgroup&gt;
&lt;col class=&quot;left&quot; /&gt;

&lt;col class=&quot;left&quot; /&gt;
&lt;/colgroup&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`sudo npm install -g http-server`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;installs a basic http server. Perfect for web projects&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`sudo npm install -g express`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;a web framework for NodeJS projects&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;There are lots of other stuff npm can do. Learn them further using&lt;/p&gt;

&lt;p&gt;&lt;code&gt;info npm&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;npm help install&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;registry-problems&quot;&gt;Registry problems&lt;/h1&gt;

&lt;p&gt;&lt;strong&gt;npm&lt;/strong&gt; is configured to pull from known registries. But from time to time, pulling software from the registries might fail. Repointing the registry to &lt;a href=&quot;http://registry.npmjs.org&quot;&gt;http://registry.npmjs.org&lt;/a&gt; fixes things &lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo npm config set registry http://registry.npmjs.org&lt;/code&gt; &lt;/p&gt;

&lt;div id=&quot;disqus_thread&quot;&gt;&lt;/div&gt;
&lt;script type=&quot;text/javascript&quot;&gt;
    /* * * CONFIGURATION VARIABLES: EDIT BEFORE PASTING INTO YOUR WEBPAGE * * */
    var disqus_shortname = &#39;thagos&#39;; // required: replace example with your forum shortname

    /* * * DON&#39;T EDIT BELOW THIS LINE * * */
    (function() {
        var dsq = document.createElement(&#39;script&#39;); dsq.type = &#39;text/javascript&#39;; dsq.async = true;
        dsq.src = &#39;//&#39; + disqus_shortname + &#39;.disqus.com/embed.js&#39;;
        (document.getElementsByTagName(&#39;head&#39;)[0] || document.getElementsByTagName(&#39;body&#39;)[0]).appendChild(dsq);
    })();
&lt;/script&gt;

&lt;noscript&gt;Please enable JavaScript to view the &lt;a href=&quot;http://disqus.com/?ref_noscript&quot;&gt;comments powered by Disqus.&lt;/a&gt;&lt;/noscript&gt;
</content>
 </entry>
 
 <entry>
   <title>Mac Mini Microphone Hack</title>
   <link href="http://tedhagos.com/osx/mac-mini-microphone-hack.html"/>
   <updated>2014-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/osx/mac-mini-microphone-hack</id>
   <content type="html">&lt;p&gt;The Mac Mini requires line level mic input. It needs to be powered. A mic that works on PC may not work on the mini.&lt;/p&gt;

&lt;p&gt;If you just want to have a mic for the mini and it’s easier to buy than to improvise, go ahead and shop around for mics, there isn’t a shortage for third party products for the mini. On the other hand, if you have an old voice recorder equipped with a stereo mini jack (3.5mm) and don’t mind shopping around for a stereo mini jack connector (3.5mm also, obviously), then hack away.&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;Find an old voice recorder. There could be one (or two) gathering dust in that sealed box of yesteryear-gadgets. I found an old Sony TCM-150. Any recorder with a built-in microphone should do it. I think even the more modern voice recorders with a 3.5mm socket should do the trick, but I don’t have a modern voice recorder.&lt;/li&gt;
  &lt;li&gt;A 3.5mm stereo jack (mini jack) with both ends as male connectors. This can be easily procured at any audio supplies store or hardware supply store. I got mine from ACE hardware.&lt;/li&gt;
  &lt;li&gt;Once you have procured the 2 items above. Connect the Mac Mini and the voice recorder using the stereo mini jack. You might want to check the batteries on that old voice recorder, I hope you did not forget to remove the batteries when you locked it away for keeping. If the dry cell batteries have leaked, the voice recorded may not be usable anymore.&lt;/li&gt;
  &lt;li&gt;Power the voice recorder. Put in record mode (you can press the pause button so that it actually doesn’t record, we just need the mic)&lt;/li&gt;
  &lt;li&gt;Test it. I tested mine by using it in a skype call. In fact, that was the only reason I needed a microphone for the Mac Mini&lt;/li&gt;
&lt;/ol&gt;

</content>
 </entry>
 
 <entry>
   <title>iTerm 256 Colors</title>
   <link href="http://tedhagos.com/osx/iterm-256-color.html"/>
   <updated>2014-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/osx/iterm-256-color</id>
   <content type="html">&lt;p&gt;To enable 256 colors in iTerm, go to &lt;em&gt;Preferences&lt;/em&gt;, &lt;em&gt;Profiles&lt;/em&gt; then click the &lt;em&gt;Terminal&lt;/em&gt; tab.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/iterm-256.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Change the value of “Report Terminal Type” to &lt;em&gt;xterm-256color&lt;/em&gt;&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>/usr/bin/env No Such File</title>
   <link href="http://tedhagos.com/linux/UsrBinEnVNodeNoSuchFile.html"/>
   <updated>2014-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/linux/UsrBinEnVNodeNoSuchFile</id>
   <content type="html">&lt;p&gt;If your platform is Debian, Ubuntu, Lubuntu or any of the Debian derivatives and you installed NodeJS via the &lt;code&gt;aptitude&lt;/code&gt; or &lt;code&gt;apt-get&lt;/code&gt; package manager, you may experience this error when you try to use NodeJS. Not only will this appear by using NodeJS itself but it may appear as well when you use any package gotten via &lt;strong&gt;npm&lt;/strong&gt; e.g. &lt;code&gt;http-server&lt;/code&gt; or &lt;code&gt;express&lt;/code&gt;. &lt;/p&gt;

&lt;p&gt;The error &lt;code&gt;/usr/bin/env node no such file or directory&lt;/code&gt; means it is looking for the node executable in &lt;code&gt;/usr/bin/node&lt;/code&gt; but it’s not there because what the package manager installed was &lt;code&gt;/usr/bin/nodejs&lt;/code&gt; intead. &lt;/p&gt;

&lt;h1 id=&quot;solution&quot;&gt;Solution&lt;/h1&gt;

&lt;p&gt;Either rename nodejs to node or just create a symlink. Better to just create the symlink&lt;/p&gt;

&lt;p&gt;&lt;code&gt;$ cd /usr/bin&lt;/code&gt; 
&lt;code&gt;$ sudo ln -s nodejs node&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Another way to solve this is get &lt;strong&gt;nodejs-legacy&lt;/strong&gt; instead &lt;strong&gt;node&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;$ sudo apt-get install -y nodejs-legacy&lt;/code&gt;&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>Send Receive SMS Using USB Modem</title>
   <link href="http://tedhagos.com/linux/UsbModemLinux-SMS.html"/>
   <updated>2014-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/linux/UsbModemLinux-SMS</id>
   <content type="html">&lt;h1 id=&quot;overview&quot;&gt;1 Overview&lt;/h1&gt;

&lt;p&gt;This guide uses gnokii-sms to send and receive SMS messages using Linux PC. The project I was working on did not require industrial grade throughput. That is the reason I cannot justify the cost of WaveComm iTegra type equipment. The SMS project was ran on a Debian Linux. If you are using a different distro, use the packaging mechanism of your distro.&lt;/p&gt;

&lt;p&gt;There were a couple of other solution approaches but I went for gnokii-sms. I didn’t have a lot of time, which ruled out whipping serial I/O codes and concocting SMS messages using Hayes AT command set.&lt;/p&gt;

&lt;p&gt;I could have used Kannel but it was so big. I only needed a simple mechanism to receve and send SMS message. I did not need a full gateway with all the bells and whistles.&lt;/p&gt;

&lt;p&gt;GNOKii-SMS and Gammu were mature solutions. I went eventually with gnokii-sms but Gammu would have been a fit as well.&lt;/p&gt;

&lt;p&gt;Hence, this mashup uses gnokii-sms. It is a stand-alone application with a daemon and it uses MySQL database as a datastore. I don’t need to learn any API in order to integrate it with my application. All I need to do is to write an application that will query the gnokii-sms backend. I won’t even alter the structure of the gnokii-sms database. My application will simply use gnokii for the SMS facility. No application logic will be added to gnokii-sms&lt;/p&gt;

&lt;p&gt;This little project requires the following&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;A Linux PC. Debian was used but I got to test it on Crunchbang as well, worked out fine. It should work fairly well on the Debian derivatives e.g. Ubuntu, Xubuntu, Mint etc&lt;/li&gt;
  &lt;li&gt;A USB modem. A post paid plan was used for the project, but a USB stick on pre-paid should work just as well&lt;/li&gt;
&lt;/ol&gt;

&lt;h1 id=&quot;installation&quot;&gt;2 Installation&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get update&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo apt-get install mysql-server&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo apt-get install gnokii-smsd-mysql&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;config&quot;&gt;3 Config&lt;/h1&gt;

&lt;p&gt;Connect the USB modem to PC and find out which port it is using&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo lsusb&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Another command to use could be&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo dmesg | grep USB&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;What you are looking for is the device name for the modem. Usually, the modem is at &lt;code&gt;/dev/tty0&lt;/code&gt;, but just to be sure, run either lsusb or the dmesg command as stated above. Remember the device name because you will need it when you edit &lt;code&gt;~/.config/gnokii/config&lt;/code&gt; — that is the configuration file for gnokii. &lt;/p&gt;

&lt;p&gt;On some Debian derivatives like Crunchbang, the gnokii config file was not created during the installation . If that is the case, just create the config file manually.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo mkdir -p ~/.config/gnokii&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo touch ~/.config/gnokii/config&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Edit the &lt;strong&gt;config&lt;/strong&gt; file. Set the port number and model to their appropriate values&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;port=/dev/tty0
model=AT
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;There might a default value for the model property, it could be 6510. That value corresponds to a Nokia phone. But we are not using a Nokia phone, we are using a USB modem, so set the model value to &lt;strong&gt;AT&lt;/strong&gt;.&lt;/p&gt;

&lt;p&gt;Once done with the configuration, just do a quick test so that we know things are working out fine. Gnokii has a command that can quickly tell us if we are doing things correctly.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo gnokii --identify&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;It should give us some diagnostics and a quick peek if we did something wrong on the configuration file.&lt;/p&gt;

&lt;h1 id=&quot;database-backend&quot;&gt;4 Database backend&lt;/h1&gt;

&lt;p&gt;The gnokii-smsd-msyql package contains the SQL scripts necessary to create the table structure, but the database itself still needs to be manually prepared. &lt;/p&gt;

&lt;p&gt;&lt;code&gt;mysql -u root -p&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;mysql&amp;gt; create database smsgw;&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;mysql&amp;gt; use smsgw;&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;mysql&amp;gt; \. /usr/share/doc/gnokii-smsd-mysql/sms.tables.mysql.sql;&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;sending-and-receiving-sms&quot;&gt;5 Sending and Receiving SMS&lt;/h1&gt;

&lt;p&gt;First, the daemon smsgw daemon process needs to run. Get another terminal window and run the daemon process from there&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo smsd -u root -p smsgw -c localhost -m mysql&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;To send an SMS message, the command is&lt;/p&gt;

&lt;p&gt;&lt;code&gt;echo &quot;Test message | gnokii --sendsmsd +MobileNumber&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;+MobileNumber is structured as &lt;span class=&quot;underline&quot;&gt;(country code) phone number&lt;/span&gt;&lt;/p&gt;

&lt;p&gt;If you send an SMS message to the number of the USB modem, all incoming messages are stored in the &lt;strong&gt;inbox&lt;/strong&gt; table of the database&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mysql -u root -p&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;mysql&amp;gt; use smsgw;&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;mysql&amp;gt; select * from inbox;&lt;/code&gt;&lt;/p&gt;

&lt;div id=&quot;footnotes&quot;&gt;
&lt;h2 class=&quot;footnotes&quot;&gt;Footnotes: &lt;/h2&gt;
&lt;div id=&quot;text-footnotes&quot;&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.1&quot; name=&quot;fn.1&quot; class=&quot;footnum&quot; href=&quot;#fnr.1&quot;&gt;1&lt;/a&gt;&lt;/sup&gt; There was a gnokii config file though at `/etc/xdg/gnokii/config`, this
was probably meant to be copied over to `~/.config/gnokii/config`&lt;/div&gt;


&lt;/div&gt;
&lt;/div&gt;
</content>
 </entry>
 
 <entry>
   <title>Lubuntu Trusty on Thinkpad Edge 130</title>
   <link href="http://tedhagos.com/linux/ThinkPadEdge130.html"/>
   <updated>2014-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/linux/ThinkPadEdge130</id>
   <content type="html">&lt;p&gt;The E130 is an entry level ThinkPad. It doesn’t have the same solid feel of the T420 or even the lower ends of the X series. It’s not a bad notebook though. For it’s price, it makes a pretty good type writer and secondary development machine.&lt;/p&gt;

&lt;h1 id=&quot;hardware&quot;&gt;Hardware&lt;/h1&gt;

&lt;table border=&quot;2&quot; cellspacing=&quot;0&quot; cellpadding=&quot;6&quot; rules=&quot;groups&quot; frame=&quot;hsides&quot;&gt;


&lt;colgroup&gt;
&lt;col class=&quot;left&quot; /&gt;

&lt;col class=&quot;left&quot; /&gt;
&lt;/colgroup&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;Processor&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;dual core i5, cpu MHz 774&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;RAM&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;4GB DDR3, expanded it to 8GB&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;HDD&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;500GB SATA2, 9mm slim type drive&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;h2 id=&quot;access-panel&quot;&gt;Access Panel&lt;/h2&gt;

&lt;p&gt;The ThinkPad Edge 130 has an access panel located at the bottom. It is secured by 3 Philip screws. Before you do any unscrewing and removal, make sure that
1.  It is not connected to the wall outlet. It’s powered down. The machine is off, not sleeping or stand by. Off
2.  The battery is removed&lt;/p&gt;

&lt;p&gt;Carefully remove the screws and lift up the access panel. Once the panel is removed, you can pretty much get to the hard drive and the memory modules. &lt;/p&gt;

&lt;h2 id=&quot;replace-the-drive-with-ssd&quot;&gt;Replace the drive with SSD&lt;/h2&gt;

&lt;p&gt;This notebook’s drive is slim type. Which means if you will replace the drive, you need a drive with a 2.5” form factor. It should be SATA2 (or 3 if you cannot find SATA 2 anymore). This notebook uses the slim type SSD. You need to get one that is 7mm high. This kind of drive is usually used by ultrabooks, so it is no longer exotic. The local computer hardware shop probably carries it already. If not, you can always order it online. &lt;/p&gt;

&lt;p&gt;In order to get to the hard drive chassis, you need to open the access panel at the bottom of the notebook. See the section above on how to do that. &lt;/p&gt;

&lt;p&gt;See the official Lenovo support page on how to replace the hard drive . You will need a philips screw driver to remove the drive from the chassis. Just return it the same way you removed it. The replacement exercise does not involve much risk. There isn’t much that will get in your way.&lt;/p&gt;

&lt;h2 id=&quot;increase-the-ram&quot;&gt;Increase the RAM&lt;/h2&gt;

&lt;p&gt;The ThinkPad Edge 130 comes in a lot of varieties. Writing the exact specification of the RAM module in this page is futile. This page will become a wikipage for the E130, I can’t possibly keep up with that amount of information. The best way to upgrade the RAM is to inspect the existing RAM module. The speed specifications should actually be somewhere on the sticker of the RAM. Make a note of that and buy the additional RAM accordingly.&lt;/p&gt;

&lt;p&gt;The second memory bay is usually empty. This notebook comes with 4GB out of the box. There are two clips on the side of the memory bay, you need to flex them a bit. You just need to be careful a bit, but there is no need to be nervous about it also. The official Lenovo support page for RAM replacement is here .  &lt;/p&gt;

&lt;h1 id=&quot;software&quot;&gt;Software&lt;/h1&gt;

&lt;p&gt;At the time of writing, Lubuntu’s version is 14.04. The Edge 130 should be able to handle the 64 bit version of Lubuntu. Why else would I put an 8GB RAM on this box if I wouldn’t use the 64 bit Linux. So head over to the  &lt;a href=&quot;http://lubuntu.net&quot;&gt;Lubuntu site&lt;/a&gt; and get the 64 bit installer. Don’t worry even if the installer says &lt;span class=&quot;underline&quot;&gt;AMD 64&lt;/span&gt;, that is the installer used, even on non-AMD processors. The Intel X86 is the 32 bit version, at the time of writing, there is no &lt;span class=&quot;underline&quot;&gt;Intel 64&lt;/span&gt; installer of Lubuntu. You really will use the AMD 64 installer.  &lt;/p&gt;

&lt;p&gt;Use your other PC to prepare the installer. If this is your first PC, then ask somebody’s help you prepare a Linux USB installer. When you buy most USB sticks, they are formatted in FAT32, don’t mess with that. There is usually no need to format those sticks.  &lt;/p&gt;

&lt;p&gt;There are plenty of ways to prepare a Linux USB installer. The geeky way is to use &lt;code&gt;dd&lt;/code&gt;, which assumes you already have a Linux machine. And since you are brave enough to try dd, you probably shouldn’t be reading this guide. Linux installation is elementary for you. Anyway, just in case you want to see how it’s done, there is a well maintained page over at Arch Linux detailing that installation technique. &lt;/p&gt;

&lt;p&gt;Another way to prepare the USB installer is by using &lt;a href=&quot;http://unetbootin.sourceforge.net&quot;&gt;UnetBootin&lt;/a&gt;. This software can run on Windows, Linux and OSX, so you shouldn’t have any problems getting it.&lt;/p&gt;

&lt;p&gt;Stick in the USB, fire up UnetBootIn. Select the ISO image (the Lubuntu image you downloaded) and make sure you actually see that UnetBootIn has recognized the USB drive. Follow the prompts and you should have a Lubuntu installer&lt;/p&gt;

&lt;h2 id=&quot;installation&quot;&gt;Installation&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;Plug the USB installer on one the ports&lt;/li&gt;
  &lt;li&gt;Power up the E130. Press RET (Enter key) quickly when you see the prompt&lt;/li&gt;
  &lt;li&gt;The UnetBootIn menu should be visible. Choose “Install Lubuntu”&lt;/li&gt;
  &lt;li&gt;There will be a series of guide questions, like choosing the language, keyboard etc. Do them accordingly&lt;/li&gt;
  &lt;li&gt;Make sure you are connected to the internet. Although you can install Lubuntu while offline, it will save you hours of tweaking and troubleshooting if you do the installation online. If you will use WiFi to connect to the net, make sure you’re joined to WiFi access point before proceeding. Lubuntu will inform you of WiFi access points at this point of the installation&lt;/li&gt;
  &lt;li&gt;Lubuntu will format the disk. You don’t have to do much. But you may need to select some action. Choose the one that says “Use the whole disk etc” or something like that. If there is an existing OS on the disk, the installer will ask you if you want to install Lubuntu side by side with this OS. You are on your own if you choose this route. This guide is about how to install Lubuntu and only Lubuntu on the E130 notebook. There are plenty of other resources on how to dual boot with Windows. This page is not one of them&lt;/li&gt;
  &lt;li&gt;After a while, Lubuntu will setup a user. Type your full name, edit the name you want to give to the machine and your password&lt;/li&gt;
  &lt;li&gt;Shortly thereafter, you will be asked to reboot. Wait for the screen that says “Remove the installation media and press ENTER”. Then, do as it says&lt;/li&gt;
&lt;/ol&gt;

&lt;h1 id=&quot;configuration&quot;&gt;Configuration&lt;/h1&gt;

&lt;p&gt;Login to your shiny and blazing fast Lubuntu. When I timed it, it booted under 12 seconds. That’s how fast Lubuntu boots on an dual core i5 with 8GB RAM and SSD. Next, configure it.&lt;/p&gt;

&lt;h2 id=&quot;the-root-password&quot;&gt;The root password&lt;/h2&gt;

&lt;p&gt;Lubuntu doesn’t want you to use the root credential. I don’t want you too either. There is just too much danger. But there are times that you do need to use it. So, change the root password to something you can actually remember&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo passwd root&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;It will ask you for a password. Type the same password you used to login. When you use &lt;code&gt;sudo&lt;/code&gt;, you are being challenged for your &lt;strong&gt;own&lt;/strong&gt; password, not root’s. The sudo command grants you elevated privileges only for the duration of the current command. It’s not forever. If you want to use an elevated privilege for a long time, use &lt;code&gt;su&lt;/code&gt;. Now that will require the root password. That is why we are changing it right now to something we can recall from memory in the future.&lt;/p&gt;

&lt;h2 id=&quot;update-the-system&quot;&gt;Update the system&lt;/h2&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get update&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo apt-get upgrade&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;After this, just install and get whatever software you need. You can refer to the &lt;a href=&quot;/linux/linux-cheatsheet.html&quot;&gt;Linux cheatsheet&lt;/a&gt;
 for some ideas on what you can do and install in Lubuntu.&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;Lenovo Hardware and maintenance manual for Edge 130 and 135&lt;/li&gt;
&lt;/ol&gt;

&lt;div id=&quot;footnotes&quot;&gt;
&lt;h2 class=&quot;footnotes&quot;&gt;Footnotes: &lt;/h2&gt;
&lt;div id=&quot;text-footnotes&quot;&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.1&quot; name=&quot;fn.1&quot; class=&quot;footnum&quot; href=&quot;#fnr.1&quot;&gt;1&lt;/a&gt;&lt;/sup&gt; Hard drive removal and replacement is detailed on this page &amp;lt;http://support.lenovo.com/en_PH/product-and-parts/detail.page?&amp;amp;DocID=PD024375&amp;gt;&lt;/div&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.2&quot; name=&quot;fn.2&quot; class=&quot;footnum&quot; href=&quot;#fnr.2&quot;&gt;2&lt;/a&gt;&lt;/sup&gt; This page has detailed steps, though a bit terse. But to compensate, they drew pictures, which helps a lot. The page is at &amp;lt;http://support.lenovo.com/en_PH/product-and-parts/detail.page?&amp;amp;DocID=PD024374&amp;gt;&lt;/div&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.3&quot; name=&quot;fn.3&quot; class=&quot;footnum&quot; href=&quot;#fnr.3&quot;&gt;3&lt;/a&gt;&lt;/sup&gt; &amp;lt;https://wiki.archlinux.org/index.php/USB_Flash_Installation_Media&amp;gt;&lt;/div&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.4&quot; name=&quot;fn.4&quot; class=&quot;footnum&quot; href=&quot;#fnr.4&quot;&gt;4&lt;/a&gt;&lt;/sup&gt; This link points to the hardware maintenance manual of the Edge 130 &amp;lt;http://www.manualowl.com/m/Lenovo/ThinkPad-Edge-E130/Manual/309636&amp;gt;&lt;/div&gt;


&lt;/div&gt;
&lt;/div&gt;
</content>
 </entry>
 
 <entry>
   <title>Markdown Documents</title>
   <link href="http://tedhagos.com/emacs/emacs-markdown.html"/>
   <updated>2014-09-25T00:00:00+08:00</updated>
   <id>http://tedhagos.com/emacs/emacs-markdown</id>
   <content type="html">&lt;p&gt;Get markdown-mode from elpa or whatever it is that you are using. You can enter markdown mode using the command &lt;strong&gt;M x markdown-mode&lt;/strong&gt;. But in order to automaticaly enter markdown mode when a markdown document is in the buffer, you need to the add the following to .init.el or .emacs file&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-lisp&quot; data-lang=&quot;lisp&quot;&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;autoload&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;&amp;#39;markdown-mode&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;markdown-mode&amp;quot;&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;Major mode for editing Markdown files&amp;quot;&lt;/span&gt; &lt;span class=&quot;no&quot;&gt;t&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; 
&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;add-to-list&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;&amp;#39;auto-mode-alist&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;\\.text\\&amp;#39;&amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;.&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;markdown-mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt; 
&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;add-to-list&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;&amp;#39;auto-mode-alist&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;\\.markdown\\&amp;#39;&amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;.&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;markdown-mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt; 
&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;add-to-list&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;&amp;#39;auto-mode-alist&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;\\.md\\&amp;#39;&amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;.&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;markdown-mode&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;
</content>
 </entry>
 
 <entry>
   <title>Object Oriented Programming</title>
   <link href="http://tedhagos.com/javascript/OOP-introduction.html"/>
   <updated>2014-09-01T00:00:00+08:00</updated>
   <id>http://tedhagos.com/javascript/OOP-introduction</id>
   <content type="html">&lt;h1 id=&quot;objects&quot;&gt;Objects&lt;/h1&gt;

&lt;p&gt;There are many other programming languages which use objects. JavaScript isn’t even the first language to do it. The concept of managing complexity using objects as abstraction mechanism has been going on for quite some time. &lt;/p&gt;

&lt;p&gt;What is it really&lt;/p&gt;

&lt;p&gt;Well, it’s a way to manage complexity. But then I should make a case for complexity. What is complexity in software and how does objects abstraction address those. What kinds of problems does OOP solve&lt;/p&gt;

&lt;h1 id=&quot;complexity&quot;&gt;Complexity&lt;/h1&gt;

&lt;h1 id=&quot;abstraction&quot;&gt;Abstraction&lt;/h1&gt;
</content>
 </entry>
 
 <entry>
   <title>Email</title>
   <link href="http://tedhagos.com/tools/Email.html"/>
   <updated>2014-08-07T00:00:00+08:00</updated>
   <id>http://tedhagos.com/tools/Email</id>
   <content type="html">&lt;ul&gt;
  &lt;li&gt;Mutt&lt;/li&gt;
  &lt;li&gt;msmtp&lt;/li&gt;
  &lt;li&gt;.netrc&lt;/li&gt;
  &lt;li&gt;muttrc&lt;/li&gt;
  &lt;li&gt;offlineimaprc&lt;/li&gt;
&lt;/ul&gt;
</content>
 </entry>
 
 <entry>
   <title>HTML5 Exercises</title>
   <link href="http://tedhagos.com/html5/html5-exercises.html"/>
   <updated>2014-08-05T00:00:00+08:00</updated>
   <id>http://tedhagos.com/html5/html5-exercises</id>
   <content type="html">&lt;p&gt;&lt;strong&gt;1. Resize the triangle&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;Create a 500px wide HTML5 canvas that allows a user to increase/decrease the size of an equilateral triangle as shown in the picture. If the user clicks the small triangle pointing downward (labeled 1), the height of the triangle decreases by 5px. If the user clicks the small triangle pointing upwards (labeld 2), the height of the triangle increases by 5px.&lt;/p&gt;

&lt;p&gt;&lt;a href=&quot;/images/triangle-exam.png&quot;&gt;See the picture&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;2. Move the circle&lt;/strong&gt; along the path of the arc&lt;/p&gt;

&lt;p&gt;Animate the small circle. Make it move from left to right. Start at the left-most part of the arc and finish at the right-most end. The movement of the circle will trace the curve of the arc. The arc is a half circle. The canvas is 500px wide.&lt;/p&gt;

&lt;p&gt;&lt;a href=&quot;/images/arc-exam.png&quot;&gt;See the picture&lt;/a&gt; &lt;/p&gt;

&lt;p&gt;&lt;strong&gt;3. Bouncing ball&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;Animate the ball inside the bounded region. The ball travels in a straight path. When it hits a side of the bounding region, it will bounce (like in a pool table). The angle of deflection does not need to physically accurate, you can use an arbitrary angle for the deflection (e.g. 45 deg). The canvas size is 500px wide by 500px tall&lt;/p&gt;

&lt;p&gt;&lt;a href=&quot;/images/bounce-exam.png&quot;&gt;See the picture&lt;/a&gt; &lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Program Flow</title>
   <link href="http://tedhagos.com/javascript/ProgramFlow.html"/>
   <updated>2014-08-04T00:00:00+08:00</updated>
   <id>http://tedhagos.com/javascript/ProgramFlow</id>
   <content type="html">&lt;p&gt;The JavaScript interpreter looks for the first statement in a program or script. It will read and execute that. If there is another statement after the first one, it will execute that too. It will keep on doing this until there are no more statments to execute. Think of program flow like a river or stream. It flows in one direction unless something alters that direction.&lt;/p&gt;

&lt;p&gt;Sometimes, the interpreter encounters an instruction that will divert the flow of execution. These instructions are called control structures. Some structures allows you to branch and some structures allows for looping.&lt;/p&gt;

&lt;p&gt;Branching happens when you divert program flow away from a linear path. Some instructions on the script are skipped or not performed when some conditions are true (or not). Branching is facilitated in JavaScript by using the keywords &lt;strong&gt;if&lt;/strong&gt; and &lt;strong&gt;switch&lt;/strong&gt;.&lt;/p&gt;

&lt;p&gt;Looping happens when you perform statements repeatedly. This kind of control flow can be done by using the keywords &lt;strong&gt;while&lt;/strong&gt; and &lt;strong&gt;for&lt;/strong&gt;.&lt;/p&gt;

&lt;h1 id=&quot;todo-rework-the-examples-so-it-doesnt-use-nodejs-use-the-browser&quot;&gt;TODO Rework the examples so it doesn’t use nodejs, use the browser&lt;/h1&gt;

&lt;h1 id=&quot;todo-reword-the-prose-is-weak&quot;&gt;TODO Reword. The prose is weak&lt;/h1&gt;

&lt;h1 id=&quot;if-statement&quot;&gt;If statement&lt;/h1&gt;

&lt;p&gt;Branching statements allows you to side step some statements when some conditions are true; &lt;strong&gt;if-else&lt;/strong&gt; statement is one way to branch. When the expression inside the &lt;strong&gt;if()&lt;/strong&gt; construct yields true, the statement inside the if block is executed. &lt;/p&gt;

&lt;p&gt;The &lt;strong&gt;if&lt;/strong&gt; statement can be used when you want to conditionally skip some statements. It forks the program flow. The basic construction of an &lt;strong&gt;if&lt;/strong&gt; statement is as follows&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;if (&amp;lt;boolean expresion&amp;gt;) {
  // statements;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The boolean expression (condition) is an argument to the &lt;strong&gt;if&lt;/strong&gt; reserved word. It needs to evaluate to either true or false. The pair of curly braces is optional. If you only have one statement to execute when the condition is true, the braces can be omitted. &lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-java&quot; data-lang=&quot;java&quot;&gt;&lt;span class=&quot;n&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;==&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;20&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;console&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;log&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;true&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;else&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;console&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;log&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;nah!&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt; 

&lt;span class=&quot;c1&quot;&gt;// RETURNS nah!&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;Be careful when writing your comparisson operator. Here’s a slightly modified version of the code above&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-javascript&quot; data-lang=&quot;javascript&quot;&gt;&lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;if&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;20&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;nx&quot;&gt;console&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;log&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&amp;quot;true&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;else&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;nx&quot;&gt;console&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;log&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&amp;quot;nah!&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt; 

&lt;span class=&quot;c1&quot;&gt;// RETURNS true&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;Can you spot the difference? The expression inside the if() construct is improperly written as an assignment rather than a test for equality–but JavaScript went to evaluate it anyway. Be careful with this one because this mistake is so easy to make. The reason this returned &lt;strong&gt;**true&lt;/strong&gt;** is because an arithmetic assignment operation returns the zero value, and if you use a number where a boolean expression is expected, the number is convered to the &lt;strong&gt;true&lt;/strong&gt; value. &lt;/p&gt;

&lt;h1 id=&quot;switch-statement&quot;&gt;Switch statement&lt;/h1&gt;

&lt;p&gt;is another way of branching. It is a bit more concise than the if-else statement, it allows for a different kind of construction. &lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-javascript&quot; data-lang=&quot;javascript&quot;&gt;&lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;s&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;hello&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;kd&quot;&gt;var&lt;/span&gt; &lt;span class=&quot;nx&quot;&gt;ret&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;k&quot;&gt;switch&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;s&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;

  &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;he&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;nx&quot;&gt;ret&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;he&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;break&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;nx&quot;&gt;ret&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;break&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;kc&quot;&gt;true&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;nx&quot;&gt;ret&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;true&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;break&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;case&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;Hello&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;toLowerCase&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;()&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;nx&quot;&gt;ret&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;Hello&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
    &lt;span class=&quot;k&quot;&gt;break&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;default&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;:&lt;/span&gt;
    &lt;span class=&quot;nx&quot;&gt;ret&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s2&quot;&gt;&amp;quot;nothing&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;

&lt;span class=&quot;nx&quot;&gt;console&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;log&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nx&quot;&gt;ret&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;


&lt;span class=&quot;c1&quot;&gt;// RETURNS &amp;quot;Hello&amp;quot;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;The values listed in &lt;strong&gt;case&lt;/strong&gt; statements can be Strings, booleans or numbers (a bit of a relief if you are coming the Java language where the case is restricted to integer values). Always add the &lt;strong&gt;break&lt;/strong&gt; keyword inside the case statement lest you will have unintended consequences–without the break keyword, the program will overflow to the remaining statements of the &lt;strong&gt;switch construct&lt;/strong&gt; even if a match has already been found. &lt;/p&gt;

&lt;h1 id=&quot;while-loop&quot;&gt;While loop&lt;/h1&gt;

&lt;p&gt;Looping or iteration statements allows you to perform some statements repeatedly while some conditions are true.&lt;/p&gt;

&lt;p&gt;The &lt;strong&gt;while&lt;/strong&gt; statement is a looping mechanism. It requires a boolean value (or an expression that yields a boolean value) inside the &lt;strong&gt;**while()&lt;/strong&gt;** construct. The statements inside the while block will be executed repeatedly as long as the expression inside while() is true. When the expression is no longer true, the loop terminates. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;var timetoexit = false;
var counter = 0;

while (!timetoexit) {
  console.log(&quot;counter = &quot; + counter);
  if (counter++ == 10) timetoexit = true;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You can use the &lt;strong&gt;break&lt;/strong&gt; keyword to break out of loops, like this&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;var timetoexit = false;
var counter = 0;

while (!timetoexit) {
  console.log(&quot;counter = &quot; + counter);
  if (counter++ == 10) break;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The break keyword causes the program flow to ignore all the statements (if there are any) immediately following the &lt;strong&gt;break&lt;/strong&gt; keyword and fall right outside the end of the while block where the program flow will resume.  &lt;/p&gt;

&lt;p&gt;Another keyword related to loop structures is the &lt;strong&gt;**continue&lt;/strong&gt;** keyword. It causes program flow to jump also&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;var timetoexit = false;
var counter = 0;

while (!timetoexit) {

  console.log(&quot;counter = &quot; + counter);
  if (++counter == 10) {
    timetoexit = true;
    continue; 
  }
  console.log(&quot;*&quot;);
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Unlike &lt;strong&gt;break&lt;/strong&gt; though, &lt;strong&gt;**continue&lt;/strong&gt;** causes program flow to ignore all the remaining statements until the end of the while block and jump right away to the beginning of the while loop—forcing the condition to get re-evaluated; this is the reason why our last asterisk never got printed. &lt;/p&gt;

&lt;h1 id=&quot;for-loop&quot;&gt;For loop&lt;/h1&gt;

&lt;p&gt;The &lt;strong&gt;for&lt;/strong&gt; statement is another looping mechanism. The for loop like the while loop allows you to iterate and perform a group of statements over and over again. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;for ([where to begin] ; [where to stop] ; [step]) {

}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;A key concept in using the for loop is the counter (the step). Loop will perform all the statements inside the block for a finite and determined number of times. Each time that the whole block is evaluated, the step value is incremented. The incremented step value is then compared to the ending value, and when the ending value is reached, the for loop will then terminate. The program control will fall over to first statement immediately after the for loop block.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;for (i = 1 ; i &amp;lt; 10 ; i++) {

  console.log(i);

}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The code sample above defines the starting point at 1 (i = 1), it will stop the loop execution when &lt;strong&gt;i &amp;lt; 10&lt;/strong&gt;.  Each time the &lt;strong&gt;for&lt;/strong&gt; loop is evaluated, the i variable will be incremented by 1 (i++).&lt;/p&gt;

&lt;p&gt;Here is a another sample of the for loop.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;var util = require(&#39;util&#39;)

var i = 1;
var j = 1;

for (i = 1 ; i &amp;lt; 10; i++) {

    for (j =1 ; j &amp;lt; 10; j++) {

        util.print(i * j + &quot;\t&quot;);

    }

    util.print(&quot;\n&quot;);
}


/*

RESULT

1   2   3   4   5   6   7   8   9   
2   4   6   8   10  12  14  16  18  
3   6   9   12  15  18  21  24  27  
4   8   12  16  20  24  28  32  36  
5   10  15  20  25  30  35  40  45  
6   12  18  24  30  36  42  48  54  
7   14  21  28  35  42  49  56  63  
8   16  24  32  40  48  56  64  72  
9   18  27  36  45  54  63  72  81

*/
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;This code requires the node.js library &lt;strong&gt;util&lt;/strong&gt;. We can’t use the STDIO (console.log) here because it automatically adds a CRLF (carriage return-line feed) while &lt;strong&gt;util.print()&lt;/strong&gt; library does not. Node.JS has many libraries for convenient use, you just need to do the &lt;strong&gt;require(libname)&lt;/strong&gt; statement if you want to use it.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Linux Cheatsheet</title>
   <link href="http://tedhagos.com/linux/linux-cheatsheet.html"/>
   <updated>2014-07-21T00:00:00+08:00</updated>
   <id>http://tedhagos.com/linux/linux-cheatsheet</id>
   <content type="html">&lt;h1 id=&quot;copy-pasting-clipboard-tofrom-terminal&quot;&gt;Copy pasting clipboard to/from terminal&lt;/h1&gt;

&lt;p&gt;Can be done either via &lt;em&gt;xsel&lt;/em&gt; or &lt;em&gt;xclip&lt;/em&gt;. Xclip is part of most modern installations, so you it may be there in your installation already. The common usages are:&lt;/p&gt;

&lt;p&gt;You copied something in an X window (browser or editor etc), you want to paste the content to the terminal. Unfortunately the terminal doesn’t have access to the clipboard. But you can do this on the terminal using &lt;/p&gt;

&lt;p&gt;&lt;code&gt;echo `xclip -selection clipboard -o`&lt;/code&gt; &lt;/p&gt;

&lt;p&gt;That command will echo the contents of clipboard to the console. If you wanted to append the clipboard contents to a file, you can do &lt;/p&gt;

&lt;p&gt;&lt;code&gt;echo `xclip -selection clipboard -o` &amp;gt;&amp;gt; somefile.txt&lt;/code&gt; &lt;/p&gt;

&lt;p&gt;and that will do the trick&lt;/p&gt;

&lt;p&gt;If you  need to copy the &lt;strong&gt;contents of a text file to the clipboard&lt;/strong&gt;. This can be done by &lt;/p&gt;

&lt;p&gt;&lt;code&gt;cat somefile.txt | xclip -selection clipboard&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Now you can paste that content using &lt;em&gt;CTRL V&lt;/em&gt; &lt;/p&gt;

&lt;p&gt;Typing &lt;code&gt;xclip -selection clipboard&lt;/code&gt; is just way too many keystrokes. Put an alias to it. In ~/.bashrc, add the following&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;alias pbpaste=&quot;xclip -selection clipboard -o&quot;
alias pbcopy=&quot;xclip -selection clipboard&quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Now you can&lt;/p&gt;

&lt;p&gt;&lt;code&gt;echo `pbpaste` &amp;gt;&amp;gt; somefile.txt&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;cat somefile.txt | pbcopy&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;find-the-version-name&quot;&gt;Find the version name&lt;/h1&gt;

&lt;p&gt;Find version name — the wonky names, not the kernel number — of Ubuntu, Lubuntu, Mint or any of its many derivatives.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;lsb_release -a&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;is-my-linux-64-or-32-bit&quot;&gt;Is my Linux 64 or 32 bit&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;uname -a&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Does it contain &lt;em&gt;X86_64&lt;/em&gt;? If it does, it is 64 bit&lt;/p&gt;

&lt;h1 id=&quot;find-out-how-much-disk-space-is-left&quot;&gt;Find out how much disk space is left&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;df -h&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;change-the-password-for-root&quot;&gt;Change the password for root&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo passwd root&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;If you have an admin, ask for permission first if it is okay to do this. If it is just your own machine, go ahead and knock yourself off. You don’t need to do things as root — always. For that there is &lt;em&gt;sudo&lt;/em&gt;&lt;/p&gt;

&lt;h1 id=&quot;divide-your-terminal-screen-and-multiplex&quot;&gt;Divide your terminal screen and multiplex&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install tmux&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;You still need to configure tmux, but the basic idea is to launch tmux and do all your windowing there
&lt;code&gt;tmux new -s &amp;lt;session-name&amp;gt;&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;secure-copy-file-from-one-machine-to-another&quot;&gt;Secure Copy file from one machine to another*&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;scp username@remotemachine:somefile.txt ~/somefile.txt&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;This will copy &lt;em&gt;somefile.txt&lt;/em&gt; on &lt;em&gt;remotemachine&lt;/em&gt; to the local machine&lt;/p&gt;

&lt;p&gt;&lt;code&gt;scp somefile.txt username@remotemachine:/somefile.txt&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;It will copy &lt;em&gt;somefile.txt&lt;/em&gt; from the local machine to &lt;em&gt;remotemachine&lt;/em&gt;&lt;/p&gt;

&lt;h1 id=&quot;see-if-there-is-a-cups-configured-printer&quot;&gt;See if there is a CUPS configured printer&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;lynx http://localhost:631&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;You can use Chrome or Firefox if it better suits you&lt;/p&gt;

&lt;h1 id=&quot;find-out-ram-and-cpu-info&quot;&gt;Find out RAM and CPU info&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;cat /proc/meminfo&lt;/code&gt; to find out how much RAM&lt;br /&gt;
&lt;code&gt;cat /proc/cpuinfo&lt;/code&gt; to find out CPU information&lt;/p&gt;

&lt;h1 id=&quot;mess-around-with-network-interfaces&quot;&gt;Mess around with network interfaces&lt;/h1&gt;

&lt;p&gt;The file you want is in &lt;code&gt;/etc/network/interfaces/&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;cli-browsing&quot;&gt;CLI browsing&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install lynx&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;lynx http://tldp.org&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;display-calendar&quot;&gt;Display calendar&lt;/h1&gt;

&lt;p&gt;With week numbers etc
-   &lt;strong&gt;cal     :&lt;/strong&gt; displays the current month
-   &lt;strong&gt;cal 2014:&lt;/strong&gt; displays the whole year
-   &lt;strong&gt;ncal -w :&lt;/strong&gt; shows the week numbers
-   &lt;strong&gt;ncal -e:&lt;/strong&gt; shows when Easter is&lt;/p&gt;

&lt;h1 id=&quot;rip-youtube-videos&quot;&gt;Rip YouTube videos&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install youtube-dl ffmpeg lame&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;cd ~/Videos&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;youtube-dl http:/youtubevideoaddress&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The downloaded video is in &lt;em&gt;.webm&lt;/em&gt; format, to convert to wav, do this&lt;/p&gt;

&lt;p&gt;&lt;code&gt;ffmpeg -i filename.webm filename.wav&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;To convert to mp3, do this further&lt;/p&gt;

&lt;p&gt;&lt;code&gt;lame filename.wav filename.mp3&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;rdp-from-windows-to-linux&quot;&gt;RDP from Windows to Linux&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install xnest&lt;/code&gt;  &lt;/p&gt;

&lt;p&gt;Edit &lt;em&gt;etc/gdm/gdm.conf&lt;/em&gt; then uncomment &lt;em&gt;RemoteGreeter&lt;/em&gt; in the daemon section. Just delete the pound sign. Next, find the &lt;em&gt;xdmcp&lt;/em&gt; section and change the value of the &lt;em&gt;Enable Key&lt;/em&gt;, set it to &lt;em&gt;true&lt;/em&gt;.&lt;/p&gt;

&lt;p&gt;Log out so you can restart the GDM or you can execute this command&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo /etc/init.d/gdm restart&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;After that, the box is ready to accept remote desktop connections from Windows clients&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Get a download accelerator&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install axel&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;cd ~/Downloads&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;axel -n 10 http://wordpress.org/latest.zip&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The int value of &lt;em&gt;-n&lt;/em&gt; means the number of threads it will use to pull the file. Go easy on this one, don’t try to punish the web server by setting a very high number. 10 seems a bit excessive already.&lt;/p&gt;

&lt;h1 id=&quot;backup-a-mysql-database&quot;&gt;Backup a MySQL database&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo mysqldump -u YourUserName -p --all-databases &amp;gt; /path/to/dump/file.backup&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The dumped file is basically text. They are SQL commands that has both structure and data of the dumped database. To restore the db from the dumped file, do this&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mysql -u YourUserName -p&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mysql&amp;gt; create database nameOfNewDatabase&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;mysql&amp;gt; use nameOfNewDatabase&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;mysql&amp;gt; .\file.backup&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;manage-the-run-level-configuration&quot;&gt;Manage the run level configuration&lt;/h1&gt;

&lt;p&gt;If you don’t want to always type &lt;code&gt;/etc/init.d/processname restart&lt;/code&gt; get the run level config manager&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install sysv-rc-conf&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sysv-rc-conf&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Use the minus sign(-) to stop processes and plus(+) sign to restart them. &lt;/p&gt;

&lt;h1 id=&quot;ssh-to-your-linux-box&quot;&gt;SSH to your Linux box&lt;/h1&gt;

&lt;p&gt;First, you need to make it an SSH server&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install ssh&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Then from a remote host, connect to it&lt;/p&gt;

&lt;p&gt;&lt;code&gt;ssh yourusername@remotehost&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;&lt;em&gt;yourusername&lt;/em&gt; has to be a defined account in the remotehost computer. You will be asked, the first time around, to store an entry inside ~/.ssh/known_hosts, say yes to this question&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;Connect Windows to a CUPS printer&lt;/strong&gt;&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;Ping the CUPS printer, make sure there are no network connectivity issues&lt;/li&gt;
  &lt;li&gt;Go to &lt;em&gt;Add Printers&lt;/em&gt; dialog window&lt;/li&gt;
  &lt;li&gt;Choose &lt;em&gt;Add network printer&lt;/em&gt;&lt;/li&gt;
  &lt;li&gt;Choose &lt;em&gt;My printer is not listed&lt;/em&gt;, because it might not be&lt;/li&gt;
  &lt;li&gt;There will be a box provided, type &lt;code&gt;http://ipofCUPSprinter:631/nameofQueue&lt;/code&gt;. The &lt;em&gt;nameofQueue&lt;/em&gt; is something your sys admin would know, so ask him. Or you could simple open up a browser and go to &lt;a href=&quot;http://ipofCUPSprinter:631&quot;&gt;http://ipofCUPSprinter:631&lt;/a&gt;, the name of the queue is usually listed in there under the &lt;em&gt;Printers&lt;/em&gt; section&lt;/li&gt;
  &lt;li&gt;Select a driver for the printer, most Windows 7/8 boxes can simply pull this from the internet&lt;/li&gt;
&lt;/ol&gt;

&lt;h1 id=&quot;install-a-print-server&quot;&gt;Install a print server&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install cupsys&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Configure /etc/cups/cupsd.conf&lt;/p&gt;

&lt;h1 id=&quot;install-java-re-and-sdk-from-oracle&quot;&gt;Install Java RE and SDK from Oracle&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo add-apt-repository ppa:webupd8team/java&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo apt-get update&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo apt-get install oracle-java8-installer&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo update-alternatives --config java&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;screen-capture&quot;&gt;Screen capture&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install imagemagick&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;cd ~/Pictures&lt;/code&gt;
-   &lt;strong&gt;&lt;code&gt;import -window root Image1.png&lt;/code&gt;:&lt;/strong&gt; Take the screenshot of the whole screen
-   &lt;strong&gt;&lt;code&gt;import Image2.png&lt;/code&gt;:&lt;/strong&gt; The mouse pointer will turn to a crosshair, click and drag the screen region you want to capture
-   &lt;strong&gt;&lt;code&gt;import -frame Image3.png&lt;/code&gt;:&lt;/strong&gt; Just like the command above but this one captures the frame of the window region&lt;/p&gt;

&lt;h2 id=&quot;other-options&quot;&gt;Other options&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;code&gt;sudo apt-get install shutter&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;sudo apt-get install gimp&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;sudo apt-get install gnome-screenshot&lt;/code&gt; if you don’t mind using gnome&lt;/li&gt;
  &lt;li&gt;Just press PRT SC to get the whole screen&lt;/li&gt;
  &lt;li&gt;Shift PRT SC to get just a portion of the screen&lt;/li&gt;
  &lt;li&gt;&lt;code&gt;scrot&lt;/code&gt; This will silently take a screenshot of the whole screen, and will create a file on the current directory&lt;/li&gt;
&lt;/ol&gt;

&lt;h1 id=&quot;view-an-image&quot;&gt;View an image&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;gpicview Image01.png&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;mount-an-smb-or-cifs-share&quot;&gt;Mount an SMB or CIFS share&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install smbclient&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo apt-get install cifs-utils&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo mount -t cifs //servername/folder ~/mountFolder&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;If you don’t know the name of the folder that is shared, use the smbclient to discover it&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo smbclient -L servername&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;If you don’t know the name of the server, although, you should, try to&lt;/p&gt;

&lt;p&gt;&lt;code&gt;arp -n&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;It should give you the list of servers that your machine already talked to. The problem is, what if you haven’t talked to that machine yet? You could try to&lt;/p&gt;

&lt;p&gt;&lt;code&gt;ping 255.255.255.255&lt;/code&gt;  &lt;/p&gt;

&lt;p&gt;Ping the whole network, try to see if you can recognize some servers in there&lt;/p&gt;

&lt;h1 id=&quot;system-time-is-wrong-all-the-time&quot;&gt;System time is wrong all the time&lt;/h1&gt;

&lt;p&gt;Edit &lt;code&gt;/etc/default/rcS&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;UTC is enabled by default, set it to no&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;UTC=no
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Reboot&lt;/p&gt;

&lt;h1 id=&quot;check-battery&quot;&gt;Check battery&lt;/h1&gt;

&lt;p&gt;The &lt;code&gt;ibam&lt;/code&gt; doesn’t seem to be applicable anymore. But instead there are two commands that might do the trick&lt;/p&gt;

&lt;p&gt;&lt;code&gt;upower -e&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;upower -i &amp;lt;power source&amp;gt;&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The -e option gives only the power sources, namely the battery and plugged power. The -i option gives a detailed info about the power source. So use the commands like this&lt;/p&gt;

&lt;p&gt;&lt;code&gt;upower -i `upower -e`&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;terminal-color&quot;&gt;Terminal color&lt;/h1&gt;

&lt;p&gt;Find out first how many colors does the terminal already have. The &lt;code&gt;tput colors&lt;/code&gt; command will tell you the current number of colors&lt;/p&gt;

&lt;p&gt;On the &lt;strong&gt;~/.bashrc&lt;/strong&gt; file, set the number of colors to 256&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;# ~/.bashrc

export TERM=&quot;xterm-256color&quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If you do this, remove any setting from any tool e.g. tmux, emacs that will interfer with setting the TERM color.&lt;/p&gt;

&lt;h1 id=&quot;hp-laserjet-p1102w&quot;&gt;HP LaserJet P1102W&lt;/h1&gt;

&lt;p&gt;You will need quite a couple of things. This printer requires the hplip package. That is not all, it also requires a proprietary plugin from HP for it to function properly. &lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install hplip hplip-gui&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Launch the HPLip Toolbox. If you are using Lubuntu with the default LXDE, it will be in the &lt;strong&gt;Preferences&lt;/strong&gt; menu. If you are using &lt;strong&gt;gnome-do&lt;/strong&gt;, it is so much easier to find, press the superkey + spacebar&lt;/p&gt;

&lt;p&gt;After installation, if you have configured the P1102W properly, your workstation is on the same subnet as the printer, the HPLip Toolbox should have installed it already. In fact, if you check the line printer status, &lt;code&gt;lpstat&lt;/code&gt;, you should be able to see the printer installed already.  &lt;/p&gt;

&lt;p&gt;You need to set a default printer. This can be done using &lt;code&gt;lpoptions -d PrinterName&lt;/code&gt;. If the LinuxMachine is equipped with GUI, it can also be done there. In Lubuntu it’s under &lt;strong&gt;System tools&lt;/strong&gt; - &lt;strong&gt;Printers&lt;/strong&gt;. If you don’t set a default printer, you might not be able to use commands such as &lt;code&gt;lpstat, lpr, lprm&lt;/code&gt; etc.&lt;/p&gt;

&lt;h1 id=&quot;format-external-drive-to-fat32&quot;&gt;Format external drive to FAT32&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo apt-install dosfstools&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Find out the name of the actual device&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo fdisk -l&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;If you simpy use fdisk without the sudo, you might not see all the disk&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo mkdosfs -F -I /dev/sdb1&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Substitute the actual device name to &lt;strong&gt;sdb1&lt;/strong&gt;. The primary disk will be sda — sda1 is the first partition on the primary disk, sda2 is the second partition etc. If you have a second disk it will be &lt;strong&gt;sdb&lt;/strong&gt;. If you have a USB external disk attached, it could be &lt;strong&gt;sdc&lt;/strong&gt;. The disk has to be properly partitioned before you format it to FAT32. &lt;/p&gt;

&lt;p&gt;You can use &lt;strong&gt;fdisk&lt;/strong&gt; or &lt;strong&gt;cfdisk&lt;/strong&gt; to created partitions. There are GUI tools to do this as well like &lt;strong&gt;gparted&lt;/strong&gt;. Actually, it’s a lot easier to use gparted, so just go use that.&lt;/p&gt;

&lt;h1 id=&quot;hash-sum-mismatch-error&quot;&gt;Hash Sum Mismatch Error&lt;/h1&gt;

&lt;p&gt;During a software update, you might encounter this error. Just remove the contents of &lt;strong&gt;apt/lists&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo rm -fr /var/lib/apt/lists/*&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo apt-get update&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;sudo apt-get upgrade&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;nvpy&quot;&gt;nvPY&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install nvpy&lt;/code&gt;  &lt;/p&gt;

&lt;p&gt;Create a config file for nvpy &lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo touch ~/.nvpy.cfg&lt;/code&gt;&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;# .nvpy.cfg

[nvpy]
notes_as_txt = 1
txt_path = /home/thagos/Dropbox/notes

#disable simplenote syncing, use 1 to enable it
simplenote_sync = 0

# 0 value will sort in alphanumeric mode
sort_mode = 1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;There is a detailed example and explanation of the cfg file in &lt;a href=&quot;https://github.com/cpbotha/nvpy&quot;&gt;https://github.com/cpbotha/nvpy&lt;/a&gt;&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>JavaScript Learning Objectives</title>
   <link href="http://tedhagos.com/javascript/LearningObjectives.html"/>
   <updated>2014-07-17T00:00:00+08:00</updated>
   <id>http://tedhagos.com/javascript/LearningObjectives</id>
   <content type="html">&lt;h1 id=&quot;introduction&quot;&gt;1 Introduction&lt;/h1&gt;

&lt;h1 id=&quot;programming-concepts&quot;&gt;2 Programming Concepts&lt;/h1&gt;

&lt;h1 id=&quot;code-structure&quot;&gt;3 Code Structure&lt;/h1&gt;

&lt;h1 id=&quot;types&quot;&gt;4 Types&lt;/h1&gt;

&lt;h1 id=&quot;expressions-statements-and-functions&quot;&gt;5 Expressions, Statements and Functions&lt;/h1&gt;

&lt;h1 id=&quot;keywords&quot;&gt;6 Keywords&lt;/h1&gt;

&lt;h1 id=&quot;program-flow&quot;&gt;7 Program Flow&lt;/h1&gt;

&lt;h1 id=&quot;complex-types&quot;&gt;8 Complex Types&lt;/h1&gt;

&lt;h1 id=&quot;objects-and-functions&quot;&gt;9 Objects and Functions&lt;/h1&gt;

&lt;h1 id=&quot;arrays&quot;&gt;10 Arrays&lt;/h1&gt;

&lt;h1 id=&quot;object-oriented-programming&quot;&gt;11 Object Oriented Programming&lt;/h1&gt;

&lt;h1 id=&quot;browser-side-javascript&quot;&gt;12 Browser side JavaScript&lt;/h1&gt;

&lt;h1 id=&quot;the-window-object&quot;&gt;13 The Window Object&lt;/h1&gt;

&lt;h1 id=&quot;document-object-model&quot;&gt;14 Document Object Model&lt;/h1&gt;

&lt;h1 id=&quot;event-handling&quot;&gt;15 Event Handling&lt;/h1&gt;

&lt;h1 id=&quot;jquery&quot;&gt;16 JQuery&lt;/h1&gt;

&lt;h1 id=&quot;scripting-css&quot;&gt;17 Scripting CSS&lt;/h1&gt;

&lt;h1 id=&quot;client-side-storage&quot;&gt;18 Client Side Storage&lt;/h1&gt;
</content>
 </entry>
 
 <entry>
   <title>Screencasting Setup</title>
   <link href="http://tedhagos.com/tools/ScreenCastHardware.html"/>
   <updated>2014-07-13T00:00:00+08:00</updated>
   <id>http://tedhagos.com/tools/ScreenCastHardware</id>
   <content type="html">&lt;ol&gt;
  &lt;li&gt;Shure PG42 USB Mic&lt;/li&gt;
  &lt;li&gt;A mixer with phantom voltage, needs to be USB&lt;/li&gt;
  &lt;li&gt;Camtasia 2 Mac - to Record the screen&lt;/li&gt;
  &lt;li&gt;Audacity — surely, I cannot talk while typing. So I need to have the ability to record the voice on a separate track.&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;The condenser mic and the mixer is necessary to achieve an acceptably clean audio. So there is no hiss and hum on the audio.&lt;/p&gt;

&lt;p&gt;Possible source of these equipment in MNL is &lt;a href=&quot;http://www.jbmusic.com.ph/product/details/alesis-mm4usb-4-channel-usb-mixer&quot;&gt;http://www.jbmusic.com.ph/product/details/alesis-mm4usb-4-channel-usb-mixer&lt;/a&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/shure-mic.jpg&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/mixer.jpg&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Types</title>
   <link href="http://tedhagos.com/java/Types.html"/>
   <updated>2014-07-10T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/Types</id>
   <content type="html">&lt;p&gt;Programmers create things (data). We manipulate them by adding, substracting, dividing or multiplying. Sometimes we mash them up together (concatenation), then we create more things. Sometimes, we store these things for later use, and other times we don’t, we simply discard them. Sometimes we dig up things that we have stored in the past (retrieval) and other times we show the things we’ve tucked away to other people (reports). This is an oversimplification of what programmers do, but I would guess that it is not very far from it. &lt;/p&gt;

&lt;p&gt;In order to create things, we need to know what kinds of things you can create. Will you be counting these things (whole numbers) or will you measure them (real numbers)? If we need to compare one thing to another thing, how will we  remember the result of that comparisson? Can we store the result of that comparisson inside another kind of thing, perhaps one that is aware of truth and falsity? &lt;/p&gt;

&lt;p&gt;If we were using a language that is pretty close to the metal, like assembly, you may need to know quite a bit about how things are stored and organized in a very low-level fashion before you can get very far.  We are, however, using Java and it will not require us to work close to the metal. &lt;/p&gt;

&lt;p&gt;You will work with &lt;strong&gt;types&lt;/strong&gt; rather than bits (zeroes and ones). Java has defined some useful abstractions for us already. These abstractions allow us to work with relative ease because we can represent familiar concepts with ready-made &lt;strong&gt;types&lt;/strong&gt;. For example, if you need to work with real numbers, Java has defined the &lt;strong&gt;float&lt;/strong&gt; and &lt;strong&gt;double&lt;/strong&gt;  data types. If you need to work with words and letters, Java has defined for us the &lt;strong&gt;java.lang.String&lt;/strong&gt; type.  These abstractions are high enough that we don’t get bogged down by the details of how they are stored and structured on the disk, we can worry about other things like the logic of our application. &lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;byte:&lt;/strong&gt; signed 8-bit integer (-128 to 127)&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;short :&lt;/strong&gt; signed 32-bit integer (-2,147,483,648 to 2,147,483,647)&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;long:&lt;/strong&gt; signed 64-bit integer (-9,223,372,036,854,775,808 to 9,223,372,036,854,807)&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;float:&lt;/strong&gt; signed 32-bit floating point (32 bit IEEE 754 floating point)&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;double:&lt;/strong&gt; signed 64-bit floating point (64 bit IEEE 754 floating point)&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;char :&lt;/strong&gt; 16-bit Unicode character (0 to 65535 or \u0000 to \uFFFF)&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;boolean:&lt;/strong&gt; either &lt;strong&gt;true&lt;/strong&gt; or &lt;strong&gt;false&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;variables&quot;&gt;Variables&lt;/h2&gt;

&lt;p&gt;Variables are created by declaring them, then defining them. For example, &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;int i;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The code above is a statement that &lt;strong&gt;declares&lt;/strong&gt; the variable named &lt;strong&gt;i&lt;/strong&gt; that is of type &lt;strong&gt;int&lt;/strong&gt; (integer). It does not &lt;strong&gt;define&lt;/strong&gt; the value of i though.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;int i;
i = 0;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;In the code sample above, the variable &lt;strong&gt;i&lt;/strong&gt; was &lt;strong&gt;declared&lt;/strong&gt; on one line and then &lt;strong&gt;defined&lt;/strong&gt; on the following line. Declaration and definition of variables does not necessarily need 2 separate statements. The next sample code declares and defines a variable in one statement.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;int i = 0;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Why are we specifying that &lt;strong&gt;i&lt;/strong&gt; is an int? That is because Java is a statically typed language. The compiler will not guess what kind of data you are creating, you have to tell it explicitly before it gets compiled. &lt;/p&gt;

&lt;h2 id=&quot;rules-on-types&quot;&gt;Rules on types&lt;/h2&gt;

&lt;p&gt;The numerical types of Java follows a certain hierarchy. A &lt;strong&gt;double&lt;/strong&gt; type is bigger than a &lt;strong&gt;float&lt;/strong&gt; which is bigger than a &lt;strong&gt;long&lt;/strong&gt; which is bigger than an &lt;strong&gt;int&lt;/strong&gt; which is bigger than a &lt;strong&gt;short&lt;/strong&gt; which is bigger than a &lt;strong&gt;byte&lt;/strong&gt;. It is best to keep this hierarchy in mind because we will be working with types quite a lot. There are times that we need to work with a combination of types in one statement. But for now, we need to remember that values of a smaller type can be assigned to a variable that has a larger type.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;int a = 10;
int b = a;
long c = b;
double d = c;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;We can assign &lt;strong&gt;b to a&lt;/strong&gt; because they are both integers. We can assign &lt;strong&gt;b to c&lt;/strong&gt; because a long is bigger than an integer. And &lt;strong&gt;c to d&lt;/strong&gt; because a double is bigger than a long. When you assign a value that is of a smaller type to one of a larger type, it is referred to as &lt;strong&gt;upcasting&lt;/strong&gt;. Upcasting is automatically performed for you when you do operations like the one in the example above. &lt;/p&gt;

&lt;p&gt;Let’s look at another example.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;byte a = 1;
byte b = 2;
byte c = a + b;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;It looks innocent and deceitfully correct, but it will result to an error. It can be reasoned that because we added two byte values, &lt;strong&gt;a + b&lt;/strong&gt;, we can assign the arithmetic result to variable &lt;strong&gt;c&lt;/strong&gt;, which is declared as a byte. That would have been correct if the resulting type of adding two &lt;strong&gt;byte&lt;/strong&gt; values is also a byte. That is not the case. One apple added to another another apple doesn’t make two apples in this case. It looks counter-intutive and scaringly frustrating to a beginning Java programmer. But the frustration, hopefully, should go away given a bit more practice and familiarity with the rules governing Java programming.&lt;/p&gt;

&lt;p&gt;For the most part, we can work with variables of differing types e.g. add an int to a double, subtract a byte from an int etc, multiply a float with a long. But what kind of type will the resultant value have? To answer that, we will need to know another rule.&lt;/p&gt;

&lt;p&gt;When a &lt;strong&gt;double&lt;/strong&gt; is added to something smaller (float, long, int etc) the resulting type is a double. When a &lt;strong&gt;float&lt;/strong&gt; is added to something smaller (long, int, short etc), the result is float. When a &lt;strong&gt;long&lt;/strong&gt; added to either an &lt;strong&gt;int, short,  byte or char&lt;/strong&gt;, the result is a long. For everything else the result of the operation is an &lt;strong&gt;int&lt;/strong&gt;.&lt;/p&gt;

&lt;p&gt;I simply chose the arithmetic &lt;strong&gt;add&lt;/strong&gt; operation for the explanation for convenience. It is a lot easier to say (and write), but the rule holds up for subtraction, multiplication, division and even modulus. No arithmetic operation will ever result into &lt;strong&gt;short&lt;/strong&gt; or &lt;strong&gt;byte&lt;/strong&gt;. There is a very interesting reason for that rule, it sounds really clever too but that is out of the scope of this material. If you want to pursue that reason, dig up on “how Java does arithmetic internally”. &lt;/p&gt;

&lt;p&gt;Let’s continue to work on our small byte addition example earlier, let’s fix that.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;class ByteAdd {
  public static void main(String args[]) {
    byte a = 1;
    byte b = 2;
    int c = a + b;
  }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;And now, all is well. &lt;/p&gt;

&lt;h2 id=&quot;todo-reference-and-footnote-java-internal-arithmetic&quot;&gt;TODO Reference and footnote Java internal arithmetic&lt;/h2&gt;

&lt;p&gt;Look at the JLS&lt;/p&gt;

&lt;div id=&quot;footnotes&quot;&gt;
&lt;h2 class=&quot;footnotes&quot;&gt;Footnotes: &lt;/h2&gt;
&lt;div id=&quot;text-footnotes&quot;&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.1&quot; name=&quot;fn.1&quot; class=&quot;footnum&quot; href=&quot;#fnr.1&quot;&gt;1&lt;/a&gt;&lt;/sup&gt; A statically typed language requires that that all variable types should be known before compilation. Static typing gives the compiler information about the data. This is why Java can prevent you from doing silly things like declaring a variable as String and then using them in an arithmetic operation which may not be appropriate for Strings.&lt;/div&gt;


&lt;/div&gt;
&lt;/div&gt;
</content>
 </entry>
 
 <entry>
   <title>Program Flow</title>
   <link href="http://tedhagos.com/java/ProgramFlow.html"/>
   <updated>2014-07-10T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/ProgramFlow</id>
   <content type="html">&lt;p&gt;Your ability to direct and command the flow of control in your program is one of the defining skills for a programmer. There are basically 3 ways that you can arrange the commands in your program code. You can execute commands one after another, &lt;strong&gt;sequencing&lt;/strong&gt;.
You can execute certain commands when certain conditions are met, &lt;strong&gt;branching&lt;/strong&gt;; and you can keep on repeating certain commands while certain conditions are still true, &lt;strong&gt;looping or iteration&lt;/strong&gt;&lt;/p&gt;

&lt;h2 id=&quot;if-statement&quot;&gt;If statement&lt;/h2&gt;

&lt;pre&gt;&lt;code&gt;if(condition) {
  statement 1;
  statement 2;
  statement n;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The &lt;strong&gt;if&lt;/strong&gt; statement allows you to conditionally perform some commands. It allows you side-step the main flow of your code. If you remember your flowcharting concepts or logic formumation. When you see the diamond shape, it means you have a fork in the road. When a condition is met, you either go left or right. That is what the &lt;strong&gt;if&lt;/strong&gt; statement does. It allows you to test for conditions. When the condition is true, you go to the beginning of the if block (the opening curly brace) and begin executing all the statements there until all the statements are exhausted. When you have reached the end of the block (the closing curly brace), then there are no more commands to execute. The flow of control will resume immediately after the end of the curly brace. &lt;/p&gt;

&lt;p&gt;The condition in the &lt;strong&gt;if&lt;/strong&gt; statement can be written as expressions that will evaluate to either true or false. This is the most common use you will have of the if statement. On some occassions, you may see the condition written as the literals &lt;strong&gt;true&lt;/strong&gt; or &lt;strong&gt;false&lt;/strong&gt; – this is rare, but it will still be a legal code. Looks easy, right. &lt;/p&gt;

&lt;p&gt;Okay, a little ice-breaker. Can you spot what’s wrong in the next code. Try not to read the explanation and figure it out yourself first.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;int i = 0;

if(i = 1) {
 System.out.println(&quot;but i is equal to one&quot;);
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Did you catch it? If you did not, the Java compiler will. That’s a good thing. People with programming experience in &lt;strong&gt;C Language&lt;/strong&gt; would be familiar with this, because the expression &lt;code&gt;if (i = 1)&lt;/code&gt; would have been valid in &lt;strong&gt;C&lt;/strong&gt;. Thankfully not in Java, the compiler would have warned you of “Incompatible type error”.&lt;/p&gt;

&lt;p&gt;The &lt;strong&gt;**if&lt;/strong&gt;** statement requires a boolean expession in the argument. The expression &lt;code&gt;(i = 1)&lt;/code&gt; will not result to a boolean expression, rather it will yield a integer expression; zero if the operation succeeds and non-zero if it fails.&lt;/p&gt;

&lt;h2 id=&quot;nested-structures&quot;&gt;Nested structures&lt;/h2&gt;

&lt;p&gt;You can put an if statement inside another if statement. You can in fact nest them up pretty deeply. While there is no set limit on how deep you can nest if statements, practical considerations and code readability should knock some sense into your code. &lt;/p&gt;

&lt;p&gt;From my experience, whenever I am treading dangerously deep into a web of if-else-if-else, that is usually a signal to take a step back and look if there are other ways of going about controlling program logic. It could be a design or analysis problem already and that using the if-else statement is not the right tool to solve the problem at hand. &lt;/p&gt;

&lt;h2 id=&quot;switch-statement&quot;&gt;Switch statement&lt;/h2&gt;

&lt;p&gt;When the nature of the decision you need to make is a bit on the fuzzy side, it could be impractical to use the if-statement, for those kinds of decisions, you will use the switch-statement.&lt;/p&gt;

&lt;p&gt;The syntax of the switch statement is;&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;switch(expression) {
  case CONSTANTEXPR:
  case ENUMCONSTANTNAME:
  default:
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Where expression can either be a &lt;strong&gt;char, byte, short, int&lt;/strong&gt;. It can also be &lt;strong&gt;Character, Byte, Short, Integer&lt;/strong&gt; OR an &lt;strong&gt;enum&lt;/strong&gt; type. By the way, Character is not the same as char, that wasn’t a typo. Character is a Java class which is a wrapper class for the char primitive. As of Java 7, you can now use String expressions on the &lt;strong&gt;switch&lt;/strong&gt; statement&lt;/p&gt;

&lt;p&gt;The following code sample  extracts the day-of-week value from a &lt;strong&gt;Calendar&lt;/strong&gt; object. The day-of-week is returned as integer, which makes it perfect to filter using a &lt;strong&gt;switch&lt;/strong&gt; statement rather than nested &lt;strong&gt;if&lt;/strong&gt;&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;import java.util.Calendar;

class SwitchSample {

  public static void main(String args[]){

    Calendar now = Calendar.getInstance();
    int dow = now.get(Calendar.DAY_OF_WEEK);

    switch(dow) {
      case 0:
        System.out.println(&quot;Sunday&quot;);
        break;
      case 1:
        System.out.println(&quot;Monday&quot;);
        break;
      case 2:
        System.out.println(&quot;Tuesday&quot;);
        break;
      case 3:
        System.out.println(&quot;Wednesday&quot;);
        break;
      case 4:
        System.out.println(&quot;Thursday&quot;);
        break;
      case 5:
        System.out.println(&quot;Friday&quot;);
        break;
      case 6:
        System.out.println(&quot;Saturday&quot;);
        break;
      default:
        System.out.println(&quot;What the value really is = &quot; + dow);
      }
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The switch is really simple to use, just put the expression which you’d like to test as an argument to the switch statement, then have a series of case statements inside the switch body.&lt;/p&gt;

&lt;p&gt;Each case statement corresponds to a value, if the value of the expression is equivalent to a case value, then instructions immediately right after the colon of the case statement will be executed/evaluated, in fact the rest of the statements will be evaluated, all the way down, until it gets to the last instruction at the bottom of the switch statement; that’s not what your intention might be; that is the reason why I put a break statement. Once a case statement evaluates to true, Java blindly executes all the statements after that case, it won’t even bother checking the other cases – so note this, and be careful.&lt;/p&gt;

&lt;h2 id=&quot;while-statement&quot;&gt;While statement&lt;/h2&gt;

&lt;p&gt;The while statement is used when you would like to perform some commands repeatedly, like in a loop. There is another statement in Java that does looping, that is the &lt;strong&gt;for&lt;/strong&gt; statement.&lt;/p&gt;

&lt;p&gt;The &lt;strong&gt;**while&lt;/strong&gt;** and &lt;strong&gt;**for&lt;/strong&gt;** loop may overlap at times because they have a similar function. For a beginner, it may not be quite obvious when to use one and not the other. The basic difference of the two statements though is that;&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;Use the while loop if you do not know how many times you need to loop&lt;/li&gt;
  &lt;li&gt;Use the for loop if you actually know how many times you will need to go through the loop&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;A while loop looks like the following&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;while(condition) {
  statement 1;
  statement 2;
  statement n;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The condition is either a literal (the keywords true or false) or an expression that will evaluate to either true or false (boolean values).&lt;/p&gt;

&lt;p&gt;The basic idea in a &lt;strong&gt;while&lt;/strong&gt; loop is this&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;The condition is evaluated for the first time, if the condition is true or resolves to true, then;&lt;/li&gt;
  &lt;li&gt;Perform statement 1, then;&lt;/li&gt;
  &lt;li&gt;Perform statement 2, then;&lt;/li&gt;
  &lt;li&gt;Perform statement n, then;&lt;/li&gt;
  &lt;li&gt;The program detects that we are at the end of while block (the closing curly brace is the end of the while block)&lt;/li&gt;
  &lt;li&gt;The program will loop back to the condition and re-evaluate it again, if it still true, then;&lt;/li&gt;
  &lt;li&gt;Perform statement (again)&lt;/li&gt;
  &lt;li&gt;rinse repeat until you get the end of the end of the block again and until you get to re-evaluate the condition (again).&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;You will only drop off end of the while block if and when the condition evaluates to false. This is the reason it is best to remember that you need write code inside the while block that will make the condition false at some point in time, lest you end up in a perpetual loop.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;import static java.lang.System.out;

class While {
  public static void main (String [] args){
    
    boolean condition = true;
    int counter = 0;
    
    while(condition) {
      out.println(counter);
      counter = counter + 1;
      
      if(counter &amp;gt;10) {
        condition = false;
      }
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The sample code above shows the use of a while statement. Always remember to put something inside your while loop that will make the conditon false at some point in time. A perpetual loop can be very difficult for a beginning programmer to spot.&lt;/p&gt;

&lt;h2 id=&quot;for-statement&quot;&gt;For statement&lt;/h2&gt;

&lt;pre&gt;&lt;code&gt;for(beginning value; ending value; step) {
  statement 1;
  statement 2;
  statement 3;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The &lt;strong&gt;for loop&lt;/strong&gt; like the &lt;strong&gt;while&lt;/strong&gt; allows you to iterate and perform a group of statements over and over again. The for loop though, allows for more control.  &lt;/p&gt;

&lt;p&gt;A key concept in using the for loop is the automatic counter, the &lt;strong&gt;step&lt;/strong&gt;. The code will perform all the statements inside the block for a finite and determined number of times.&lt;/p&gt;

&lt;p&gt;Each time that the whole block is evaluated, the &lt;strong&gt;step&lt;/strong&gt; value is incremented. The incremented step value is then compared to the &lt;strong&gt;ending value&lt;/strong&gt;, and when the ending value is reached, the for loop will then terminate. The program control will fall over to first statement immediately after the for loop block. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;class for_sample {
  public static void main (String [] args) {
    for(int i=0; i&amp;lt;=10;i++) {
      System.out.println(i);
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;In the preceding example, we print some numeric values from zero (0) to ten (10). This is the exact thing that our &lt;strong&gt;while&lt;/strong&gt; example was doing earlier.  &lt;/p&gt;

&lt;h2 id=&quot;break-and-continue&quot;&gt;Break and Continue&lt;/h2&gt;

&lt;p&gt;The &lt;strong&gt;**break&lt;/strong&gt;** and &lt;strong&gt;**continue&lt;/strong&gt;** keywords disrupts program flow inside loops, whether the for-loop or while-loop. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;while(condition) {
  statement 1;
  statement 2;
  break;
  statement 3;
  statement 4;
}

statement 5;
statement n;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The break statement caused the program flow inside a block to forcibly exit. In the sample code (Fig 7), as soon as the break statement is encountered, the program control ignored statements 3 and 4. The while loop will go out of scope and program control will be transferred to the first executable statement immediately after the while block – in this case, statement 5.  &lt;/p&gt;

&lt;p&gt;You normally will not use the break statement in this fashion because it doesn’t make sense. This statement is usually deployed with more logical finesse. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;while(condition) {
  statement 1;
  statement 2;

  if(someCondition) {
    break;
  } 

  statement 3;
  statement 4;
}

statement 5;
statement n;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;This is a more likely use of the &lt;strong&gt;break&lt;/strong&gt; statement. I should warn you though that this is a frowned upon practice. A truly structured programming should have only one entry point and one exit point. Because of the introduction the &lt;strong&gt;break&lt;/strong&gt; statement, our control structure now has one entry point and two exit points.  &lt;/p&gt;

&lt;p&gt;While this code is innocent enough right now, it could get very hairy and complicated. You will appreciate following the 1-entry-1-exit rule when you have had your fair share of debugging someone else’s code and you are wading through a maze of nested structures with lots of breaks peppered into the source code — and by then you will understand why nobody wants to work on a code they did not write themselves. &lt;/p&gt;

&lt;p&gt;Next is the &lt;strong&gt;**continue&lt;/strong&gt;** keyword. Here is how it behaves.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;while(condition) {
  statement 1;
  statement 2;
  continue;
  statement 3;
  statement 4;
}

statement 5;
statement n;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;When the &lt;strong&gt;continue&lt;/strong&gt; statement is encountered, statements 3 and 4 will be ignored, just like how it was with the break. Unlike the case in &lt;strong&gt;**break&lt;/strong&gt;** though, the loop will not go out of scope and will not be immediately terminated.  &lt;/p&gt;

&lt;p&gt;The &lt;strong&gt;continue&lt;/strong&gt; statement will go back to the beginning of the loop and forcibly re-evaluate condition. If the condition is still true, then the loop continues normally.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>How to Print Stuff</title>
   <link href="http://tedhagos.com/java/Printing.html"/>
   <updated>2014-07-10T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/Printing</id>
   <content type="html">&lt;p&gt;You’ve already seen the &lt;strong&gt;println&lt;/strong&gt; method previous. We used it before. It is one the easiest built-in functions to utilize. All one needs to do to print is to pass something as a parameter to the &lt;strong&gt;println&lt;/strong&gt; method, and it will do the rest.  The println can handle a variety of data types. You don’t need to worry what kind of data to use, just throw it to println and let it do its work.  &lt;/p&gt;

&lt;p&gt;To send a bunch of Strings to STDOUT (standard output oir the&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-java&quot; data-lang=&quot;java&quot;&gt;&lt;span class=&quot;kd&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;PrintOne&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  
  &lt;span class=&quot;kd&quot;&gt;public&lt;/span&gt; &lt;span class=&quot;kd&quot;&gt;static&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;void&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;String&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;[]&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;args&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    
    &lt;span class=&quot;n&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;Line number one&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;Line number two&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
    
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;No surprises here. You have seen this code many times before.  It prints “Line number one”, then adds new line before it prints out “Line number two” then adds another new line. &lt;/p&gt;

&lt;p&gt;You can put expressions inside the println.  Like this&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-java&quot; data-lang=&quot;java&quot;&gt;&lt;span class=&quot;n&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;*&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;4&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
&lt;span class=&quot;n&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;err&quot;&gt;“&lt;/span&gt;&lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;err&quot;&gt;“&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;&lt;img src=&quot;../images/expressions-println.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Expressions inside the println method will be evaluated first before it gets printed. Any expression that results to a value can be printed inside the println — whether it is arithmetic, logical or simply combining Strings. &lt;/p&gt;

&lt;p&gt;The second line in the second example is worth pointing out.  The first plus sign after the enclosing double quote is not meant to perform an arithmetic addition — the second plus sign was meant to do that. The first plus sign was meant to perform String combination. &lt;/p&gt;

&lt;p&gt;Once the arithmetic sum of 1 and 2 have been resolved, the result will be combined with the String “1 + 2 = “.  In Java, if you add a String to anything, anything at all, whether it be a native or reference type, it gets converted into a String type.  Hence, the quickest way to convert any number to String is to add an empty String to it.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;10 + &quot;&quot;;&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;systemoutprint&quot;&gt;System.out.print&lt;/h1&gt;

&lt;p&gt;The &lt;strong&gt;println&lt;/strong&gt; has a variant form called &lt;strong&gt;print&lt;/strong&gt; — without the &lt;strong&gt;ln&lt;/strong&gt;. It does basically the same thing but it does not add a new line to the output.  Try this one out.&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-java&quot; data-lang=&quot;java&quot;&gt;&lt;span class=&quot;kd&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;PrintTwo&lt;/span&gt;  &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;kd&quot;&gt;public&lt;/span&gt; &lt;span class=&quot;kd&quot;&gt;static&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;void&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;String&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;[]&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;args&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;Line number one&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;Line number two&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;This sample prints “Line number oneLine number two”. It was mashed up.  This behavior can come in handy depending on what you are doing. If you need to print things without adding a new line or carriage return, use the &lt;strong&gt;print&lt;/strong&gt; method, if you need the line feed then use &lt;strong&gt;println&lt;/strong&gt;. &lt;/p&gt;

&lt;p&gt;&lt;strong&gt;*&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;There are things you can embed on Strings that can alter the printing behavior. They are called escape sequences or escape characters.  The most common escape sequence you may encounter is the &lt;strong&gt;**\n&lt;/strong&gt;**.  &lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-java&quot; data-lang=&quot;java&quot;&gt;&lt;span class=&quot;kd&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Escape&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  
  &lt;span class=&quot;kd&quot;&gt;public&lt;/span&gt; &lt;span class=&quot;kd&quot;&gt;static&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;void&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;String&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;[]&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;args&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;print&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;The quick brown\n fox jumped over the lazy\n dog&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;Escape characters are written as part of the String literal. These characters start with a back slash and they affect the way the String is printed. In the sample code above, the \n escape character will add a line feed or carriage return then it will resume printing on the next line. &lt;/p&gt;

&lt;h1 id=&quot;formatting-the-print&quot;&gt;Formatting the print&lt;/h1&gt;

&lt;p&gt;There might be times when you will need to use a combination of variables and String literals  during printing. Since we know that adding String to another data type produces a String, we can always write expressions like this.&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-java&quot; data-lang=&quot;java&quot;&gt;&lt;span class=&quot;kd&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Positional&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  
  &lt;span class=&quot;kd&quot;&gt;public&lt;/span&gt; &lt;span class=&quot;kd&quot;&gt;static&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;void&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;String&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;[]&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;args&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    
    &lt;span class=&quot;kt&quot;&gt;int&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;
    &lt;span class=&quot;kt&quot;&gt;int&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;
    &lt;span class=&quot;kt&quot;&gt;int&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;c&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;
    
    &lt;span class=&quot;n&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;a = &amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot; b = &amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot; c = &amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
    
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;There is nothing wrong with this technique except that it’s cumbersome and prone to error. You can almost get cross-eyed just looking at it and checking whether the the pairs of double quotes are correct.  Fortunately, there are other ways to handle printing. These are &lt;strong&gt;printf&lt;/strong&gt; and &lt;strong&gt;format&lt;/strong&gt;.  The &lt;strong&gt;printf&lt;/strong&gt; and &lt;strong&gt;format&lt;/strong&gt;  method are equivalent in functionality, you can use either of one them to replace &lt;strong&gt;println&lt;/strong&gt; or &lt;strong&gt;print&lt;/strong&gt;.&lt;/p&gt;

&lt;p&gt;Printf behaves exactly the same as its brothers &lt;strong&gt;println&lt;/strong&gt; and &lt;strong&gt;print&lt;/strong&gt;. All of them can handle whatever variable, literal or expression you throw to them. They will simply send it to STDOUT (your terminal).  But there are two things that &lt;strong&gt;printf&lt;/strong&gt; does exceptionally well. It can handle positional parameters and formatting.  &lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-java&quot; data-lang=&quot;java&quot;&gt;&lt;span class=&quot;kd&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;Positional&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  
  &lt;span class=&quot;kd&quot;&gt;public&lt;/span&gt; &lt;span class=&quot;kd&quot;&gt;static&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;void&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;String&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;[]&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;args&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    
    &lt;span class=&quot;kt&quot;&gt;int&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;1&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;
    &lt;span class=&quot;kt&quot;&gt;int&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;2&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;
    &lt;span class=&quot;kt&quot;&gt;int&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;c&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;3&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;
    
    &lt;span class=&quot;n&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;a = &amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot; b = &amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot; c = &amp;quot;&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;+&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
    
    &lt;span class=&quot;n&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;printf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;a = %d b = %d c = %d \n&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;a&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;b&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;c&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
    
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;Printf lets you insert placeholders or format specifiers inside the String literal. These placeholders will be filled up by the actual values which are specified also inside the &lt;strong&gt;printf&lt;/strong&gt; method.  In the example, &lt;strong&gt;**%d&lt;/strong&gt;** is a placeholder. There were three of them because there were three variables also to the right of the String literal.  The first %d corresponds to the variable &lt;strong&gt;a&lt;/strong&gt;. The second %d is for the variable &lt;strong&gt;b&lt;/strong&gt; and the last %d corresponds to variable c.  &lt;/p&gt;

&lt;p&gt;The format specifier we used  in the example was written as %d because it needed to handle &lt;strong&gt;int&lt;/strong&gt; values.  If we wanted it to deal with another type of data, we would have used a different specifier.  You can format quite a few things with &lt;strong&gt;printf&lt;/strong&gt;. It can deal with dates, &lt;/p&gt;

&lt;h1 id=&quot;activity&quot;&gt;Activity&lt;/h1&gt;

&lt;h1 id=&quot;glossary&quot;&gt;Glossary&lt;/h1&gt;

&lt;h1 id=&quot;exercise&quot;&gt;Exercise&lt;/h1&gt;
</content>
 </entry>
 
 <entry>
   <title>Simple Servlet Using Maven</title>
   <link href="http://tedhagos.com/java/MavenServlet.html"/>
   <updated>2014-07-10T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/MavenServlet</id>
   <content type="html">&lt;p&gt;Maven can automate some of the tasks involved in setting up a development environment for a Java web project. While you could use some established archetypes of Maven, it can also be managed by using a very simple Maven project structure.&lt;/p&gt;

&lt;p&gt;A Java web application requires a servlet container. There are many, but Apache Tomcat is probably the most common. A close second choice would probably Jetty, which is what we will use. Jetty is very lightweight, much lighter than Tomcat. &lt;/p&gt;

&lt;p&gt;In this sample project, we won’t even have to download Jetty manually. We simply need to add it as a dependency on the POM file and Maven will do the rest. It will pull the Jetty files from the repositories and get it installed. We don’t even have to worry of updating our system path.&lt;/p&gt;

&lt;h1 id=&quot;web-app-directory-structure&quot;&gt;web app directory structure&lt;/h1&gt;

&lt;p&gt;and why do we have to structure our maven project like this&lt;/p&gt;

&lt;p&gt;Directory Structure&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;├── pom.xml
├── src
│   └── main
│       ├── java
│       │   └── HelloWeb.java
│       └── webapp
│           └── WEB-INF
│               └── web.xml
└── target
    ├── classes
    │   └── HelloWeb.class
    └── tmp
        └── jsp
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Create the toplevel dir
&lt;code&gt;mkdir -p src/main/java&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;mkdir -p src/main/webapp/WEB-INF&lt;/code&gt;  &lt;/p&gt;

&lt;p&gt;Take the sample of 
Get the sample pom sample of 
web.xml&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF8&quot;?&amp;gt;
&amp;lt;web-app&amp;gt;
  &amp;lt;servlet&amp;gt;
    &amp;lt;servlet-name&amp;gt;Hello&amp;lt;/servlet-name&amp;gt;
    &amp;lt;servlet-class&amp;gt;HelloWeb&amp;lt;/servlet-class&amp;gt;
  &amp;lt;/servlet&amp;gt;
  &amp;lt;servlet-mapping&amp;gt;
    &amp;lt;servlet-name&amp;gt;Hello&amp;lt;/servlet-name&amp;gt;
    &amp;lt;url-pattern&amp;gt;/hello/*&amp;lt;/url-pattern&amp;gt;
  &amp;lt;/servlet-mapping&amp;gt;
&amp;lt;/web-app&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;pom.xml&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;project&amp;gt;
  &amp;lt;modelVersion&amp;gt;4.0.0&amp;lt;/modelVersion&amp;gt;
  &amp;lt;groupId&amp;gt;com.hagos&amp;lt;/groupId&amp;gt;
  &amp;lt;artifactId&amp;gt;CookieSample&amp;lt;/artifactId&amp;gt;
  &amp;lt;version&amp;gt;1.0-SNAPSHOT&amp;lt;/version&amp;gt;
  &amp;lt;packaging&amp;gt;jar&amp;lt;/packaging&amp;gt;
  
  &amp;lt;properties&amp;gt;
    &amp;lt;jettyVersion&amp;gt;9.1.2.v20140210&amp;lt;/jettyVersion&amp;gt;
  &amp;lt;/properties&amp;gt;
  
  &amp;lt;dependencies&amp;gt;
    &amp;lt;dependency&amp;gt;
      &amp;lt;groupId&amp;gt;org.eclipse.jetty&amp;lt;/groupId&amp;gt;
      &amp;lt;artifactId&amp;gt;jetty-server&amp;lt;/artifactId&amp;gt;
      &amp;lt;version&amp;gt;${jettyVersion}&amp;lt;/version&amp;gt;
    &amp;lt;/dependency&amp;gt;
  &amp;lt;/dependencies&amp;gt;
  
  &amp;lt;build&amp;gt;
    &amp;lt;plugins&amp;gt;
      &amp;lt;plugin&amp;gt;
        &amp;lt;groupId&amp;gt;org.eclipse.jetty&amp;lt;/groupId&amp;gt;
        &amp;lt;artifactId&amp;gt;jetty-maven-plugin&amp;lt;/artifactId&amp;gt;
        &amp;lt;version&amp;gt;${jettyVersion}&amp;lt;/version&amp;gt;

        &amp;lt;configuration&amp;gt;
          &amp;lt;mainClass&amp;gt;CookieSample&amp;lt;/mainClass&amp;gt;
          &amp;lt;scanTarget&amp;gt;src/main/java/&amp;lt;/scanTarget&amp;gt;
          &amp;lt;scanIntervalSeconds&amp;gt;1&amp;lt;/scanIntervalSeconds&amp;gt;
        &amp;lt;/configuration&amp;gt;
      &amp;lt;/plugin&amp;gt;
      
      &amp;lt;plugin&amp;gt;
        &amp;lt;groupId&amp;gt;org.codehaus.mojo&amp;lt;/groupId&amp;gt;
        &amp;lt;artifactId&amp;gt;exec-maven-plugin&amp;lt;/artifactId&amp;gt;
        &amp;lt;version&amp;gt;1.2.1&amp;lt;/version&amp;gt;
      &amp;lt;/plugin&amp;gt;

    &amp;lt;/plugins&amp;gt;
  &amp;lt;/build&amp;gt;

&amp;lt;/project&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You still have to mvn compile, to see the changes on the servlet&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>JDBC Programming</title>
   <link href="http://tedhagos.com/java/database-programming-jdbc.html"/>
   <updated>2014-07-09T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/database-programming-jdbc</id>
   <content type="html">&lt;p&gt;A  database is a store of information. Logically, it is place where you tuck away any data that you intend to retrieve or manipulate any time in the future. It is a durable storage. Any information that you store in  a database is guaranteed to be saved even if the application that created the data is no longer running. A database outlives your application. &lt;/p&gt;

&lt;p&gt;All data will ultimately end up as a file. That is the physical destination of all data stores. These files can vary widely when it comes to their physical representation. The simplest one is a text file and the information is written in a way that humans can read it when they open the file using a basic text editor. It can also be very elaborate where you split the information across many files. These files may not be human readable. You will need some intervening software process in order for the information to make sense. Our current subject of discussion is the latter one. Most mainstream databases organize information using a software process. &lt;/p&gt;

&lt;h1 id=&quot;database-programs&quot;&gt;Database programs&lt;/h1&gt;

&lt;p&gt;There are many software packages that offer database management e.g. Oracle, Microsoft SQL Server, MySQL, PostgreSQL and SQLite, to name a few. Each one has characteristics that set it apart from the others. Their merits and differences are beyond the scope of the current discussion. For our purposes, we will use SQLite. &lt;/p&gt;

&lt;p&gt;SQLite has been chosen because it is fairly easy to install, configure and use. &lt;/p&gt;

&lt;h1 id=&quot;structured-query-language&quot;&gt;Structured Query Language&lt;/h1&gt;

&lt;p&gt;Brief explanation, types of database. What is SQL&lt;/p&gt;

&lt;p&gt;Why we are using SQLite&lt;/p&gt;

&lt;p&gt;Some practice codes on SQLite. Prep code on SQLite, populate our database&lt;/p&gt;

&lt;h1 id=&quot;jdbc&quot;&gt;JDBC&lt;/h1&gt;

&lt;p&gt;&lt;img src=&quot;../images/jdbc.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/jdbc-architecture-2.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;h1 id=&quot;reading-records-from-a-databse&quot;&gt;Reading records from a databse&lt;/h1&gt;

&lt;h1 id=&quot;other-types-of-queries&quot;&gt;Other types of queries&lt;/h1&gt;

&lt;p&gt;There are other types of queries.&lt;/p&gt;

&lt;h1 id=&quot;metadata&quot;&gt;Metadata&lt;/h1&gt;

&lt;p&gt;no. yes. no&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Reference Types</title>
   <link href="http://tedhagos.com/java/ReferenceTypes.html"/>
   <updated>2014-07-09T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/ReferenceTypes</id>
   <content type="html">&lt;p&gt;By far and thus far, the data types you have seen are called &lt;strong&gt;primitive types&lt;/strong&gt;. The Java Type System has another kind of type. It is called &lt;strong&gt;reference types&lt;/strong&gt;. Let’s defer the discussion for reference types at least until after we’ve gone through the OOP topic. &lt;/p&gt;

&lt;p&gt;For now, what we need to remember is that, if it’s not primitive (double, float, long, int, short, byte, char or boolean) then it must be a reference type. It’ not all scary, some reference types are actually pretty to use, for example, the &lt;strong&gt;String&lt;/strong&gt; type which you have been using all  along since the Hello World program.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;String a = &quot;Hello&quot;;
String b = &quot;World&quot;;
String c = a + b;
int d = 10;
String e = a + d; // attention here
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;I called your attention to the last line because I thought this might be a good opportunity to explain why the addition operator works for a String type, what happened to our rule before — when a &lt;strong&gt;double&lt;/strong&gt; is added to something else it becomes a double.&lt;/p&gt;

&lt;p&gt;First, let’s put an addendum to our rule. &lt;strong&gt;When something is added to a String&lt;/strong&gt; the whole expression becomes a String. The String type rules them all.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Operators</title>
   <link href="http://tedhagos.com/java/Operators.html"/>
   <updated>2014-07-09T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/Operators</id>
   <content type="html">&lt;p&gt;This should not not be a long section. If you have programmed before using another language, chances are these operators are exactly the same thing in that other language that you used. So it’s a not a big deal. There is not much to see here and to be honest, quite boring. So in this section, we will simply list the operators that you can use in Java. No gotcha’s nor clever examples, just a list. &lt;/p&gt;

&lt;h2 id=&quot;arithmetic&quot;&gt;Arithmetic&lt;/h2&gt;

&lt;p&gt;The &lt;strong&gt;+ - * / and %&lt;/strong&gt; functions as you might expect. They work on numbers, both real and integral — with the exception of the &lt;strong&gt;+&lt;/strong&gt; which also works on &lt;strong&gt;Strings&lt;/strong&gt; as you’ve seen earlier.&lt;/p&gt;

&lt;h2 id=&quot;comparisson&quot;&gt;Comparisson&lt;/h2&gt;

&lt;p&gt;The &lt;strong&gt;&amp;gt; &amp;gt;=  &amp;lt; &amp;lt;= and ==&lt;/strong&gt; are read, more than, more than or equal to,less than and less than or equal to, respectively. You know how to use them, you don’t need me to explain.&lt;/p&gt;

&lt;h2 id=&quot;logical&quot;&gt;Logical&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;&amp;amp;&lt;/strong&gt; This is the logical &lt;strong&gt;and&lt;/strong&gt;. The expression will only be true if both operands are true&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;&amp;amp;&amp;amp;&lt;/strong&gt; This is the short circuit &lt;strong&gt;and&lt;/strong&gt;, more or less the same as the logical &lt;strong&gt;and&lt;/strong&gt; but it’s short circuit. If the first operand is already false, it will not even bother testing the second operand, it will immediately return &lt;strong&gt;false&lt;/strong&gt;, hence it’s name, short-circuit.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;|&lt;/strong&gt; This is the logical &lt;strong&gt;or&lt;/strong&gt;. The expression will return true as long as one of the operand is &lt;strong&gt;true&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;||&lt;/strong&gt; This is the short-circuit &lt;strong&gt;or&lt;/strong&gt;. It follows the same prinnciple as &lt;strong&gt;&amp;amp;&amp;amp;&lt;/strong&gt; only it performs it on an &lt;strong&gt;or&lt;/strong&gt; condition — can you figure it out? That’s your homework.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;There are other operators in Java, but we won’t list them here. It makes sense to tackle them when there is more context. So we will leave the discussions of these other operators like &lt;code&gt;++ and --&lt;/code&gt; for later.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Casting</title>
   <link href="http://tedhagos.com/java/Casting.html"/>
   <updated>2014-07-09T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/Casting</id>
   <content type="html">&lt;p&gt;You already know what &lt;strong&gt;upcasting&lt;/strong&gt; is, we discussed that not too long ago. Upcasting happens when you assign a narrow type to a wider type e.g. an &lt;strong&gt;int&lt;/strong&gt; value assigned to a &lt;strong&gt;long&lt;/strong&gt;. Java does for you automatically, no intervention needed. The opposite of &lt;strong&gt;upcasting&lt;/strong&gt; is of course, well, &lt;strong&gt;downcasting&lt;/strong&gt; — not very creative, I know, but it makes the two terms easier to remember. &lt;/p&gt;

&lt;p&gt;Downcasting happens when you force a wider type into a narrow type e.g. try to assign an &lt;strong&gt;int&lt;/strong&gt; to a &lt;strong&gt;byte&lt;/strong&gt;, the compiler won’t let you. Unlike upcasting, downcasting is an explicit activity. It needs action from the programmer. &lt;/p&gt;

&lt;p&gt;Let’s go back to the byte arithmetic example of Fig 4. We solved that by making variable &lt;strong&gt;c&lt;/strong&gt; a int. We also could have solved that problem using downcasting.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;class ByteAdd {
  public static void main(String args[]) {
    byte a = 1;
    byte b = 2;
    byte c = (byte)(a + b);
  }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The cast operator is the pair of parentheses. We are forcing the value to the right of the cast operator, in this case &lt;code&gt;(a + b)&lt;/code&gt; which is a &lt;strong&gt;int&lt;/strong&gt; to become a &lt;strong&gt;byte&lt;/strong&gt;. You have to group expression &lt;strong&gt;a + b&lt;/strong&gt; because if you did not, only the value &lt;strong&gt;a&lt;/strong&gt; would have been casted to a byte, then the addition operation would be performed (which results to an int) then it will be assigned to &lt;strong&gt;c&lt;/strong&gt; — the cast operator has a higher precedence than the addition operation, that is why you need to group.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Reserved Words</title>
   <link href="http://tedhagos.com/java/ReservedWords.html"/>
   <updated>2014-07-08T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/ReservedWords</id>
   <content type="html">&lt;p&gt;abstract continue for new switch assert default goto package synchronized boolean do if private this break double implements protected throw byte else import public throws case enum instanceof return transient catch extends int short try char final interface static void class finally long strictfp volatile const float native super while&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Jekyll Setup</title>
   <link href="http://tedhagos.com/ruby/jekyll.html"/>
   <updated>2014-07-01T00:00:00+08:00</updated>
   <id>http://tedhagos.com/ruby/jekyll</id>
   <content type="html">&lt;h1 id=&quot;setup&quot;&gt;Setup&lt;/h1&gt;

&lt;p&gt;Get RVM. This is optional but it’s always good to know that Jekyll can be ran on a fixed version of Ruby and not dependent on whatever the OS currently have&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mkdir jekyll-sandbox&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;cd jekyll-sandbox&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;rvm use 1.9.3&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;gem update --system&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;gem install jekyll&lt;/code&gt;  &lt;/p&gt;

&lt;h1 id=&quot;jekyll-folder-structure-and-key-files&quot;&gt;Jekyll Folder Structure and Key files&lt;/h1&gt;

&lt;p&gt;The top level folders of Jekyll are found below. Some are optional and some are mandatory
-   &lt;strong&gt;root folder:&lt;/strong&gt; The folder you created above (jekyll-sandbox) that’s the one. When you cd to jekyll-sandbox, that top-level is called a root folder. The file index.html should be located in the root folder. Mandatory&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;strong&gt;_posts:&lt;/strong&gt; This is the blogging part of Jekyll, if you add a file in this folder, that will be read, parsed and converted by Jekyll into HTML. With the use the &lt;em&gt;Liquid&lt;/em&gt; templating system, it becomes a breeze to simply loop through all &lt;em&gt;site.post&lt;/em&gt; and do your formatting. Files inside this folder should have the format &lt;strong&gt;**YYYY-MM-DD-name-of-post.md&lt;/strong&gt;**. Jekyll uses the date information. Mandatory&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;strong&gt;_layouts:&lt;/strong&gt; This should contain template files; These files are the cookie cutters of each post (and each page). The template file(s) simply contain the HTML plumbing work and you plug the content of the post using &lt;em&gt;Liquid’s&lt;/em&gt; # Setup&lt;/p&gt;
  &lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Get RVM. This is optional but it’s always good to know that Jekyll can be ran on a fixed version of Ruby and not dependent on whatever the OS currently have&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mkdir jekyll-sandbox&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;cd jekyll-sandbox&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;rvm use 1.9.3&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;gem update --system&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;gem install jekyll&lt;/code&gt;  &lt;/p&gt;

&lt;h1 id=&quot;jekyll-folder-structure-and-key-files-1&quot;&gt;Jekyll Folder Structure and Key files&lt;/h1&gt;

&lt;p&gt;The top level folders of Jekyll are found below. Some are optional and some are mandatory
-   &lt;strong&gt;root folder:&lt;/strong&gt; The folder you created above (jekyll-sandbox) that’s the one. When you cd to jekyll-sandbox, that top-level is called a root folder. The file index.html should be located in the root folder. Mandatory&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;strong&gt;_posts:&lt;/strong&gt; This is the blogging part of Jekyll, if you add a file in this folder, that will be read, parsed and converted by Jekyll into HTML. With the use the &lt;em&gt;Liquid&lt;/em&gt; templating system, it becomes a breeze to simply loop through all &lt;em&gt;site.post&lt;/em&gt; and do your formatting. Files inside this folder should have the format &lt;strong&gt;**YYYY-MM-DD-name-of-post.md&lt;/strong&gt;**. Jekyll uses the date information. Mandatory&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;strong&gt;_layouts:&lt;/strong&gt; This should contain template files; These files are the cookie cutters of each post (and each page). The template file(s) simply contain the HTML plumbing work and you plug the content of the post using &lt;em&gt;Liquid’s&lt;/em&gt; {{ page.content }} directive, that will inject the post’s content into the template. Mandatory&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;strong&gt;_includes:&lt;/strong&gt; If you want to use &lt;strong&gt;include directive&lt;/strong&gt; in jekyll, it will look for the files to include in this folder. Optional&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;strong&gt;_site:&lt;/strong&gt; When you run &lt;code&gt;jekyll --server&lt;/code&gt; or simply &lt;code&gt;jekyll&lt;/code&gt;, this folder will be generated. This is in fact, the resulting HTML files(s) that you can upload to your website. You don’t need to create this folder, this will be created automatically when you run the jekyll server&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;css:&lt;/strong&gt; Just for organization, obviously this is where CSS files go. Optional&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;feed:&lt;/strong&gt; Only if you want to create feeds for the posts. Optional.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Get the &lt;a href=&quot;https://github.com/tedhagos/jekyll-base&quot;&gt;basic jekyll project at GitHub&lt;/a&gt;. &lt;/p&gt;

&lt;h1 id=&quot;local-testing&quot;&gt;Local testing&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;jekyll --serve&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;A development server will run and listen on port 4000
 directive, that will inject the post’s content into the template. Mandatory&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;strong&gt;_includes:&lt;/strong&gt; If you want to use &lt;strong&gt;include directive&lt;/strong&gt; in jekyll, it will look for the files to include in this folder. Optional&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;strong&gt;_site:&lt;/strong&gt; When you run &lt;code&gt;jekyll --server&lt;/code&gt; or simply &lt;code&gt;jekyll&lt;/code&gt;, this folder will be generated. This is in fact, the resulting HTML files(s) that you can upload to your website. You don’t need to create this folder, this will be created automatically when you run the jekyll server&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;css:&lt;/strong&gt; Just for organization, obviously this is where CSS files go. Optional&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;feed:&lt;/strong&gt; Only if you want to create feeds for the posts. Optional.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Get the &lt;a href=&quot;https://github.com/tedhagos/jekyll-base&quot;&gt;basic jekyll project at GitHub&lt;/a&gt;. &lt;/p&gt;

&lt;h1 id=&quot;local-testing-1&quot;&gt;Local testing&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;jekyll --serve&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;A development server will run and listen on port 4000&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Mounting Drives in fstab</title>
   <link href="http://tedhagos.com/linux/fstab.html"/>
   <updated>2014-06-30T00:00:00+08:00</updated>
   <id>http://tedhagos.com/linux/fstab</id>
   <content type="html">&lt;p&gt;The fstab gets read during the boot sequence. If you have a second hard drive that you would like to mount during boot up, put an entry to it in the fstab. Add the following line to fstab to mount a second drive&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;/dev/sdb1   /home/username/pathToMount  ext4  defaults 0 0
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;How do you know that it’s &lt;strong&gt;sdb1&lt;/strong&gt; or &lt;strong&gt;sdb2&lt;/strong&gt; or something else. Use any one of these commands to find out the block devices&lt;/p&gt;

&lt;p&gt;&lt;code&gt;lsblk&lt;/code&gt;
&lt;code&gt;hwinfo&lt;/code&gt;
&lt;code&gt;fdisk&lt;/code&gt;
&lt;code&gt;hdparm&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Here is a &lt;a href=&quot;../resources/linux/fstab.sample.txt&quot;&gt;sample fstab&lt;/a&gt;&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>CLI Printing</title>
   <link href="http://tedhagos.com/linux/PrintingCommandLine.html"/>
   <updated>2014-06-30T00:00:00+08:00</updated>
   <id>http://tedhagos.com/linux/PrintingCommandLine</id>
   <content type="html">&lt;h2 id=&quot;general-commands&quot;&gt;General commands&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;lpstat -d:&lt;/strong&gt; Find out if there is default printer configured&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;lpstat -p:&lt;/strong&gt; See all the printers configured&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;lpstat -p -d:&lt;/strong&gt; See all of the printers and the default printer&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;lpr &amp;lt;filename&amp;gt;:&lt;/strong&gt; Send file to the default printer&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;cat &amp;lt;filename&amp;gt; | lpr:&lt;/strong&gt; does the same thing as &lt;code&gt;lpr \&amp;lt;filename\&amp;gt;&lt;/code&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;lpadmin:&lt;/strong&gt; CLI command to configure cups printers, but it is really much easier to use the web interface. Use the browser and go to &lt;code&gt;http://localhost:631&lt;/code&gt;. You can pretty much do everything there and without memorizing all the command line flags needed by lpadmin. If you have no choice but to do the admin on the command line, the &lt;strong&gt;lpadmin&lt;/strong&gt; command is well documented in &lt;a href=&quot;http://www.cups.org/documentation.php/man-lpadmin.html&quot;&gt;CUPS page for lpadmin&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;lpoptions -d printername:&lt;/strong&gt; Set a default printer&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;lpq:&lt;/strong&gt; See what print jobs are in progress and if there are ones on the queue&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;lprm &amp;lt;job id&amp;gt;:&lt;/strong&gt; Cancel a print job. You need to know the job id, that is why you need to &lt;code&gt;lpq&lt;/code&gt; first&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;lpoptions -p &amp;lt;printerName&amp;gt; -l:&lt;/strong&gt; Show the settings of the printer&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;todo-printing-options&quot;&gt;TODO Printing Options&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;[ ] Why is OSX using the Manual feed always? Try out the 1102w on Linux&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Either the &lt;code&gt;lpr&lt;/code&gt; and &lt;code&gt;lp&lt;/code&gt; command will send contents of a file to the configured printer. But before you can print, you need a properly configured printer. Use either &lt;code&gt;lpadmin&lt;/code&gt; for that, or configure the printer via the web gui admin at &lt;em&gt;&lt;a href=&quot;http://printserverNameOrIpAddress:631&quot;&gt;http://printserverNameOrIpAddress:631&lt;/a&gt;&lt;/em&gt; &lt;/p&gt;

&lt;p&gt;The most basic usage is to send the contents of a file to the configured printer&lt;/p&gt;

&lt;p&gt;&lt;code&gt;lpr filename.ext&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Some options to use&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;**lpr -o landscape &lt;filename&gt;:** Print in landscape mode&lt;/filename&gt;&lt;/li&gt;
  &lt;li&gt;**lpr -o fit-to-page &lt;filename&gt;:** Fit everything in 1 page&lt;/filename&gt;&lt;/li&gt;
  &lt;li&gt;**lpr -o media=A4 &lt;filename&gt;:** Use A4 as paper&lt;/filename&gt;&lt;/li&gt;
  &lt;li&gt;**lpr -o media=Letter &lt;filename&gt;:** Use letter size&lt;/filename&gt;&lt;/li&gt;
  &lt;li&gt;**lpr -n 2 &lt;filename&gt;:** Print 2 copies&lt;/filename&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;lpr -n 2 Collate=True:&lt;/strong&gt; Print 2 copies and collate&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;references&quot;&gt;References&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;CUPS.org, “Command Line Printing and options”, &lt;a href=&quot;http://www.cups.org/documentation.php/options.html&quot;&gt;http://www.cups.org/documentation.php/options.html&lt;/a&gt;, June 30, 2014&lt;/li&gt;
  &lt;li&gt;CUPS.org, “lpadmin(8)”, &lt;a href=&quot;http://www.cups.org/documentation.php/man-lpadmin.html&quot;&gt;http://www.cups.org/documentation.php/man-lpadmin.html&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;
</content>
 </entry>
 
 <entry>
   <title>Gnus Email</title>
   <link href="http://tedhagos.com/emacs/GnusEmail.html"/>
   <updated>2014-06-30T00:00:00+08:00</updated>
   <id>http://tedhagos.com/emacs/GnusEmail</id>
   <content type="html">&lt;h1 id=&quot;goal&quot;&gt;Goal&lt;/h1&gt;

&lt;ol&gt;
  &lt;li&gt;Get a local copy of Gmail on a notebook/workstation, &lt;strong&gt;offlineimap&lt;/strong&gt; seems like a good choice&lt;/li&gt;
  &lt;li&gt;Use Gnus in Emacs to read the mail&lt;/li&gt;
  &lt;li&gt;Search the email using &lt;strong&gt;notmuch&lt;/strong&gt;&lt;/li&gt;
&lt;/ol&gt;

&lt;h1 id=&quot;config&quot;&gt;Config&lt;/h1&gt;

&lt;p&gt;Make sure that IMAP is enabled in your Gmail account. Verify your settings in Gmail first, then come back here to continue the config.&lt;/p&gt;

&lt;p&gt;&lt;code&gt;touch ~/.gnus&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Put this entry in the .gnus file, replace &lt;strong&gt;emailaddress@gmail.com&lt;/strong&gt; with the real email address&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-lisp&quot; data-lang=&quot;lisp&quot;&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;setq&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;gnus-select-method&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;nnmaildir&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;GmailDiary&amp;quot;&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;directory&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;~/.GMailDiary&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;directory-files&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;nnheader-directory-files-safe&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
     &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;get-new-mail&lt;/span&gt; &lt;span class=&quot;no&quot;&gt;nil&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;

&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;setq&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;message-send-mail-function&lt;/span&gt; &lt;span class=&quot;ss&quot;&gt;&amp;#39;smtpmail-send-it&lt;/span&gt; 
      &lt;span class=&quot;nv&quot;&gt;smtpmail-starttls-credentials&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;((&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;smtp.gmail.com&amp;quot;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;587&lt;/span&gt; &lt;span class=&quot;no&quot;&gt;nil&lt;/span&gt; &lt;span class=&quot;no&quot;&gt;nil&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt; 
      &lt;span class=&quot;nv&quot;&gt;smtpmail-auth-credentials&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;((&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;smtp.gmail.com&amp;quot;&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;587&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;somebody@gmail.com&amp;quot;&lt;/span&gt; &lt;span class=&quot;no&quot;&gt;nil&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt; 
      &lt;span class=&quot;nv&quot;&gt;smtpmail-default-smtp-server&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;smtp.gmail.com&amp;quot;&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;smtpmail-smtp-server&lt;/span&gt; 
      &lt;span class=&quot;s&quot;&gt;&amp;quot;smtp.gmail.com&amp;quot;&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;smtpmail-smtp-service&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;587&lt;/span&gt; 
      &lt;span class=&quot;nv&quot;&gt;gnus-ignored-newsgroups&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;^to\\.\\|^[0-9. ]+\\( \\|$\\)\\|^[\&amp;quot;]\&amp;quot;[#&amp;#39;()]&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;

&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;setq&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;gnus-secondary-select-methods&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;((&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;nnml&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)))&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;setq&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;user-mail-address&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;diaryofted@gmail.com&amp;quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;&lt;code&gt;touch ~/.authinfo&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Contents of .authinfo is &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;machine imap.gmail.com login somebody@gmail.com password secretpassword  port 993 
machine smtp.gmail.com login somebody@gmail.com password secretpassword  port 587
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Replace &lt;strong&gt;secretpassword&lt;/strong&gt; with the real password&lt;/p&gt;

&lt;h2 id=&quot;done-offlineimap&quot;&gt;DONE OffLineImap&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;[X] Clean this up&lt;/li&gt;
  &lt;li&gt;[X] Remove specific usernames and password&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Now for OffLineImap&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;[general]
accounts = PHMail
accounts = MyGmailAccount

[Account PHMail]
localrepository = Local
remoterepository = Remote
autorefresh = 5
quick = 10

[Repository Local]
type = Maildir
localfolders = ~/MailPH

[Repository Remote]
type = IMAP
remotehost = TheHostName
remoteuser = someone@somedomain.com
keepalive = 60
holdconnectionopen = yes

[mbnames]
enabled = yes
filename = ~/.mutt/mailboxes
header = &quot;mailboxes &quot;
peritem = &quot;+%(accountname)s/%(foldername)s&quot;
sep = &quot; &quot;
footer = &quot;\n&quot;

[Account MyGmailAccount]
localrepository = MyGMailLocal
remoterepository = MyGmailRemote

[Repository MyGMailLocal]
type = Maildir
remotehost = localhost
remoteuser = linuxUserName
localfolders=~/.MyGMail

[Repository MyGMailRemote]
type = IMAP
remotehost = imap.gmail.com
remoteuser = someone@gmail.com
remotepass = secret
ssl = yes
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
maxconnections = 1
realdelete = no
folderfilter = lambda foldername: foldername in [&#39;INBOX&#39;]
holdconnectionopen = true
keepalive = 60
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;reading-email&quot;&gt;Reading email&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;M x gnus&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;It will take a while to pull all the emails, so be patient. It looks like ncurses, so basically just navigate to the folders using up and down arrow keys, then press enter on either the INBOX, All Mail etc. &lt;/p&gt;

&lt;p&gt;To go up the previous screen, press &lt;strong&gt;p&lt;/strong&gt;&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Lesson 1</title>
   <link href="http://tedhagos.com/c/1.html"/>
   <updated>2014-06-29T00:00:00+08:00</updated>
   <id>http://tedhagos.com/c/1</id>
   <content type="html">&lt;h1 id=&quot;compiler&quot;&gt;Compiler&lt;/h1&gt;

&lt;p&gt;Make sure you have a modern C compiler. Linux, UNIX and OSX comes with compilers for C and  C++. Give it a quick test if you really have  a compiler. Get a terminal window and type&lt;/p&gt;

&lt;p&gt;&lt;code&gt;gcc&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;or &lt;/p&gt;

&lt;p&gt;&lt;code&gt;clang&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Either of these two compilers will do.&lt;/p&gt;

&lt;h2 id=&quot;osx&quot;&gt;OSX&lt;/h2&gt;

&lt;p&gt;You need to download XCode before you can get the C compilers. So click the &lt;strong&gt;App Store&lt;/strong&gt;, login and get XCode. When XCode is done downloading, open it and Go to Preferences. Download the command line tools&lt;/p&gt;

&lt;h2 id=&quot;linux&quot;&gt;Linux&lt;/h2&gt;

&lt;p&gt;If the C compiler is not yet installed, just get it via package manager&lt;/p&gt;

&lt;p&gt;&lt;code&gt;sudo apt-get install gcc&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Obviously this package manager is for Debian and the Ubuntus. Use the package manager of your distro if you are using anything other than Debian.&lt;/p&gt;

&lt;h1 id=&quot;hello-world&quot;&gt;Hello World&lt;/h1&gt;

&lt;p&gt;C is a compiled language. Which means you create your source programs by writing it in a source file, usually ending in &lt;strong&gt;.c&lt;/strong&gt;, then you convert it to object code or compiled code. This is where you need the &lt;strong&gt;gcc&lt;/strong&gt;. &lt;/p&gt;

&lt;p&gt;Some textbooks may still refer to the compiler as &lt;strong&gt;cc&lt;/strong&gt; (C Compiler). Just substitute it with &lt;strong&gt;gcc&lt;/strong&gt;. GCC stands for Gnu C Compiler. &lt;/p&gt;

&lt;p&gt;Many beginning programmers create the Hello World code as their first program. Don’t break tradition. That tradition started sometime in 1969 when Dennis Ritchie and Brian Kernighan used it in their book, &lt;em&gt;The C Programming Language&lt;/em&gt;. To manage the Hello World exercise, create source file named &lt;em&gt;hello.c&lt;/em&gt; then put the following code in it&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;//filename: hello.c

#include&amp;lt;stdio.h&amp;gt;
#include&amp;lt;stdlib.h&amp;gt;

int main() {
  printf(&quot;Hello World\n&quot;);
  return EXIT_SUCCESS;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;compile the above code using gcc. On  a terminal window, type&lt;/p&gt;

&lt;p&gt;&lt;code&gt;gcc hello.c&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;If you managed to type example code above without errors, you should compile without errors too. The compiler will produce the file &lt;em&gt;a.out&lt;/em&gt;. Run the program with the command&lt;/p&gt;

&lt;p&gt;&lt;code&gt;./a.out&lt;/code&gt;&lt;/p&gt;

&lt;h2 id=&quot;compiler-options&quot;&gt;Compiler options&lt;/h2&gt;

&lt;p&gt;GCC has a lot of compiler options. One of them allows you to specify the output filename so that it reads something other than &lt;em&gt;a.out&lt;/em&gt;. So compile your code again, this time using the command below&lt;/p&gt;

&lt;p&gt;&lt;code&gt;gcc hello.c -o hello&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;hello&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The  &lt;code&gt;-o&lt;/code&gt; flag of gcc means &lt;em&gt;output filename&lt;/em&gt;&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Apache Tomcat</title>
   <link href="http://tedhagos.com/java/tomcat-install.html"/>
   <updated>2014-06-25T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/tomcat-install</id>
   <content type="html">&lt;p&gt;Apache Tomcat is a very popular choice for beginning programmers when learning how to build JSP and Servlets. Many teachers, instructors and mentors favor this tool, and for good reasons. It has been the reference implementation of the Servlet specification for many years. Even now in the age of GlassFish, the JEE server from Oracle, Apache Tomcat continues to be a choice for many developers as they explore servlet and JSP programming. &lt;/p&gt;

&lt;p&gt;Tomcat is a servlet container, it is also a web server because it can handle static html pages like many web servers such as Apache or IIS. &lt;/p&gt;

&lt;h2 id=&quot;todo-setup&quot;&gt;TODO Setup&lt;/h2&gt;

&lt;p&gt;Download Apache Tomcat from the official site &lt;a href=&quot;http://tomcat.apache.org&quot;&gt;tomcat.apache.org&lt;/a&gt;, choose the latest version. At the time of writing, the latest version was v8.0. &lt;/p&gt;

&lt;p&gt;Unzip or untar the binary installer anywhere in your workstation drive. It will be wise to install it in a location where you will not need elevated or admin privileges. Pick a location where you have read, write and execute privileges. When starting to learn how to work with Tomcat, it’s best not to have administrative challenges. You will encounter plenty of admin restrictions later on when you start deploying your apps. But for now, keep it sweet and simple. The safest location will be somewhere in your home folder. In Windows this maybe &lt;code&gt;C:\Users\yourname&lt;/code&gt;. In OSX and Linux it will be &lt;code&gt;~/&lt;/code&gt;. &lt;/p&gt;

&lt;h2 id=&quot;todo-configuration&quot;&gt;TODO Configuration&lt;/h2&gt;

&lt;p&gt;Let’s say I will put the Tomcat installation on &lt;code&gt;~/progtools/&lt;/code&gt;  &lt;/p&gt;

&lt;h1 id=&quot;todo-ide-integration&quot;&gt;TODO IDE Integration&lt;/h1&gt;

&lt;h2 id=&quot;todo-eclipse&quot;&gt;TODO Eclipse&lt;/h2&gt;

&lt;h2 id=&quot;todo-netbeans&quot;&gt;TODO Netbeans&lt;/h2&gt;
</content>
 </entry>
 
 <entry>
   <title>Terminal Copy and Paste</title>
   <link href="http://tedhagos.com/linux/clipboard-copy-paste-terminal.html"/>
   <updated>2014-06-25T00:00:00+08:00</updated>
   <id>http://tedhagos.com/linux/clipboard-copy-paste-terminal</id>
   <content type="html">&lt;p&gt;The terminal has no access to X clipboard. That is the general situation. Grabbing text from a GUI app e.g. text from a web page opened in a web browser cannot be simply pasted to xterm, or lxterminal or whatever terminal console you are using. Copying and pasting using CTRL c and CTRL v won’t work. At least not without the aid of some extra programs.&lt;/p&gt;

&lt;p&gt;Xclip can help during these kind of situations. This program mostly comes automatic with the installation of a modern Linux desktop. My Lubuntu 12 came with xclip, installed already. But just in case you need to install it, you can grab it from the software repositories, a simple &lt;code&gt;sudo apt-get install xclip&lt;/code&gt; should do the trick. Obviously this is the command to use for Debian or Debian derivatives such as Ubuntu, Lubuntu etc. For other systems. Use your package manager e.g. yum (for Fedora and CentOS)&lt;/p&gt;

&lt;p&gt;Once xclip is installed, you can use to do two things. First is to echo the contents of the clipboard to a terminal console. &lt;/p&gt;

&lt;p&gt;&lt;code&gt;xclip -selection clipboard -o&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;If the clipboard has some contents, the command above will dump the textual content of clipboard on the terminal. Try to open some web pages, copy some text and then try out the above command on a terminal console.&lt;/p&gt;

&lt;p&gt;A typical usage of this command is when you grab some text from document open in a word processor or a browser window, you can append it to a text file like this&lt;/p&gt;

&lt;p&gt;&lt;code&gt;xclip -selection clipboard -o &amp;gt;&amp;gt; somefile.txt&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The second thing you can do with xclip is move the contents of a text file to the clipboard. This will then allow you to use CTRL v keyboard shortcut to paste the content (obviously to a word processor or program that uses X windows). &lt;/p&gt;

&lt;p&gt;&lt;code&gt;cat somefile.txt | xclip -selection clipboard&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The command above should do it. If you have, say, GEdit or LibreOffice open, you can already use CTRL v to paste the clipboard content into your document.&lt;/p&gt;

&lt;h1 id=&quot;pbpaste-and-pbcopy-hack&quot;&gt;pbpaste and pbcopy hack&lt;/h1&gt;

&lt;p&gt;OSX has command line tools to copy and paste contents to/from the clipboard. These commands are &lt;code&gt;pbcopy&lt;/code&gt; and &lt;code&gt;pbpaste&lt;/code&gt;. You can define pbcopy and pbpaste aliases in the startup files so you can use these commands like you would in OSX. This hack benefits people who use both OSX and Linux. In ~/.bashrc, add the following&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;alias pbcopy=&#39;xclip -selection clipboard&#39;
alias pbpaste=&#39;xclip -selection clipboard -o&#39;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Of course you need to quit your current terminal and restart it so the new settings will take effect. Or you can issue the command &lt;code&gt;source ~/.bashrc&lt;/code&gt;.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Inheritance</title>
   <link href="http://tedhagos.com/java/inheritance.html"/>
   <updated>2014-06-21T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/inheritance</id>
   <content type="html">&lt;p&gt;Derive new classes from existing ones. Build class hierarchies and leverage existing codes. It’s a cheap way to reuse codes, you don’t have to do anything, just extend&lt;/p&gt;

&lt;p&gt;&lt;strong&gt;What is it&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;Inheritance is a language capability. It is a form of reuse — functionality reuse. Instead of copying and pasting code, which is also a form of reuse, you can intead extend an existing code and add or modify its behavior.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Polymorphism</title>
   <link href="http://tedhagos.com/java/polymorphism.html"/>
   <updated>2014-06-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/polymorphism</id>
   <content type="html">&lt;p&gt;Overriding - run time polymorphism Oveloading - compile time polymorphism&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>Packages</title>
   <link href="http://tedhagos.com/java/packages.html"/>
   <updated>2014-06-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/packages</id>
   <content type="html">&lt;p&gt;Modularity is a big deal with modern languages. Some experts actually include this feature on the list of things a language must have before it can considered an OO language.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Maven</title>
   <link href="http://tedhagos.com/java/maven.html"/>
   <updated>2014-06-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/maven</id>
   <content type="html">&lt;h2 id=&quot;installation&quot;&gt;Installation&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;Download the installer from  &lt;a href=&quot;http://maven.apache.org/download.cgi&quot;&gt;maven site&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;Unzip or untar downloaded file, put it somewhere in your file system, preferably a location where you have read/write/execute permissions&lt;/li&gt;
  &lt;li&gt;Update the SYSTEM PATH to include the folder where you unzipped the maven installation&lt;/li&gt;
  &lt;li&gt;Make sure Maven is working on the command line. Get a terminal window and invoke the maven command — &lt;code&gt;mvn&lt;/code&gt;. If you did not get a &lt;em&gt;bad command or filename&lt;/em&gt; error, that’s a good sign.&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;folder-structure&quot;&gt;Folder structure&lt;/h2&gt;

&lt;p&gt;Maven expects a folder structure for a project. You can choose not to follow what it expects, but that means you need to do some configuration on your own. If you prefer not to monkey around the config, follow Maven’s world view on directory structure unless you have a really compelling reason not to.&lt;/p&gt;

&lt;p&gt;The next set of terminal commands prepares a basic Maven project&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;
$ cd ~/
$ mkdir BasicProject
$ cd BasicProject
$ mkdir -p src/main/java
$ touch src/main/java/HelloWorld.java
$ touch pom.xml

&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&quot;pom-file&quot;&gt;POM file&lt;/h3&gt;

&lt;p&gt;Maven expects a pom.xml file on the top level directory of the project. This xml file drives the behavior of the build. There are a couple of things that Maven expects to find inside the pom file. It expects to find the modelVersion, groupId, artifactId and the version.&lt;/p&gt;

&lt;p&gt;The pom file, short for Project Object Model, is an xml file. It contains information and configuration details about the project. Maven uses these details to do the build.&lt;/p&gt;

&lt;p&gt;A simple POM file is shown below.&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-xml&quot; data-lang=&quot;xml&quot;&gt;&lt;span class=&quot;nt&quot;&gt;&amp;lt;project&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;modelVersion&amp;gt;&lt;/span&gt;4.0.0&lt;span class=&quot;nt&quot;&gt;&amp;lt;/modelVersion&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;groupId&amp;gt;&lt;/span&gt;com.hagos&lt;span class=&quot;nt&quot;&gt;&amp;lt;/groupId&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;artifactId&amp;gt;&lt;/span&gt;HelloWorld&lt;span class=&quot;nt&quot;&gt;&amp;lt;/artifactId&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;version&amp;gt;&lt;/span&gt;1.0-SNAPSHOT&lt;span class=&quot;nt&quot;&gt;&amp;lt;/version&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;packaging&amp;gt;&lt;/span&gt;jar&lt;span class=&quot;nt&quot;&gt;&amp;lt;/packaging&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;/project&amp;gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;The &lt;strong&gt;modelVersion&lt;/strong&gt; is always set to 4.0 because it is currently the only supported model for Maven versions 2 and 3. This may change in the future. If Maven 4 comes out and there is another model, you should use the prescribed model and update the modelVersion value appropriately. Where can you find the modelVersion value? The &lt;a href=&quot;https://maven.apache.org/pom.html&quot;&gt;maven site&lt;/a&gt; of course. &lt;/p&gt;

&lt;p&gt;The &lt;strong&gt;groupId&lt;/strong&gt; element is the same as the package name, it is that reverse domain name convention you use to organize your java source files, so just use that. The &lt;strong&gt;version&lt;/strong&gt; element is self explanatory, you get to choose that. It simply indicates what version your application is already in.&lt;/p&gt;

&lt;h3 id=&quot;test&quot;&gt;Test&lt;/h3&gt;

&lt;p&gt;Create a rudimentary HelloWorld.java source file and save it in &lt;strong&gt;/src/main/java&lt;/strong&gt; folder then try to compile the project. On a terminal window, make sure you are at the top level directory of the &lt;em&gt;BasicProject&lt;/em&gt; folder, run the command&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mvn compile&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;This will compile the project. There is only one source file in the project so far so it may not seem to be a big deal right now. A build tools like Maven shines better when working with multi file projects. &lt;/p&gt;

&lt;p&gt;All the compiled codes will be placed in the &lt;strong&gt;&lt;em&gt;target/classes&lt;/em&gt;&lt;/strong&gt; folder. Those were created by Maven during compilation. The BasicProject folder should now resemble the following&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;.
├── pom.xml
├── src
│   └── main
│       └── java
│           └── HelloWorld.java
└── target
    └── classes
        └── com
            └── hagos
                └── HelloWorld.class
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&quot;running-the-application&quot;&gt;Running the application&lt;/h3&gt;

&lt;p&gt;On the same terminal window that you have been using, cd to the target/classes folder then run the Java app&lt;/p&gt;

&lt;p&gt;&lt;code&gt;cd target/classes&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;java com.hagos.HelloWorld&lt;/code&gt;&lt;/p&gt;

&lt;h2 id=&quot;java-execute&quot;&gt;Java Execute&lt;/h2&gt;

&lt;p&gt;Maven can do lots of stuff via plugins. One of the things it can do is to execute a Java app from maven. It’s not a big deal to cd to target/classes then run the Java app from there. But if you do this lots of times in a day, that can get annoying. &lt;/p&gt;

&lt;h3 id=&quot;maven-exec-plugin&quot;&gt;Maven Exec Plugin&lt;/h3&gt;

&lt;p&gt;To make maven run your programs, you need to get the mvn-exec plugin. Using this plugin in your app requires not much else than declaring the plugin in the POM file. Add the plugin in the build section. &lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-xml&quot; data-lang=&quot;xml&quot;&gt;&lt;span class=&quot;nt&quot;&gt;&amp;lt;project&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;modelVersion&amp;gt;&lt;/span&gt;4.0.0&lt;span class=&quot;nt&quot;&gt;&amp;lt;/modelVersion&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;groupId&amp;gt;&lt;/span&gt;com.hagos&lt;span class=&quot;nt&quot;&gt;&amp;lt;/groupId&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;artifactId&amp;gt;&lt;/span&gt;HelloWorld&lt;span class=&quot;nt&quot;&gt;&amp;lt;/artifactId&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;version&amp;gt;&lt;/span&gt;1.0-SNAPSHOT&lt;span class=&quot;nt&quot;&gt;&amp;lt;/version&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;packaging&amp;gt;&lt;/span&gt;jar&lt;span class=&quot;nt&quot;&gt;&amp;lt;/packaging&amp;gt;&lt;/span&gt;

  &lt;span class=&quot;nt&quot;&gt;&amp;lt;build&amp;gt;&lt;/span&gt;
    &lt;span class=&quot;nt&quot;&gt;&amp;lt;plugins&amp;gt;&lt;/span&gt;
      &lt;span class=&quot;nt&quot;&gt;&amp;lt;plugin&amp;gt;&lt;/span&gt;
        &lt;span class=&quot;nt&quot;&gt;&amp;lt;groupId&amp;gt;&lt;/span&gt;org.codehaus.mojo&lt;span class=&quot;nt&quot;&gt;&amp;lt;/groupId&amp;gt;&lt;/span&gt;
        &lt;span class=&quot;nt&quot;&gt;&amp;lt;artifactId&amp;gt;&lt;/span&gt;exec-maven-plugin&lt;span class=&quot;nt&quot;&gt;&amp;lt;/artifactId&amp;gt;&lt;/span&gt;
        &lt;span class=&quot;nt&quot;&gt;&amp;lt;version&amp;gt;&lt;/span&gt;1.2.1&lt;span class=&quot;nt&quot;&gt;&amp;lt;/version&amp;gt;&lt;/span&gt;
      &lt;span class=&quot;nt&quot;&gt;&amp;lt;/plugin&amp;gt;&lt;/span&gt;
    &lt;span class=&quot;nt&quot;&gt;&amp;lt;/plugins&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;/build&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;/project&amp;gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;The build node of the POM contains the plugins node. Each plugin, like the mvn-exec, needs to be inside the plugins node. &lt;/p&gt;

&lt;p&gt;Now you can do the following command&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mvn compile&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;mvn exec:java -DmainClass=com.hagos.HelloWorld&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;If you want to get rid of that pesky -D argument, you can declare the main class inside the POM file.&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-xml&quot; data-lang=&quot;xml&quot;&gt;&lt;span class=&quot;nt&quot;&gt;&amp;lt;plugin&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;groupId&amp;gt;&lt;/span&gt;org.codehaus.mojo&lt;span class=&quot;nt&quot;&gt;&amp;lt;/groupId&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;artifactId&amp;gt;&lt;/span&gt;exec-maven-plugin&lt;span class=&quot;nt&quot;&gt;&amp;lt;/artifactId&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;version&amp;gt;&lt;/span&gt;1.2.1&lt;span class=&quot;nt&quot;&gt;&amp;lt;/version&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;configuration&amp;gt;&lt;/span&gt;
    &lt;span class=&quot;nt&quot;&gt;&amp;lt;mainClass&amp;gt;&lt;/span&gt;com.hagos.HelloWorld&lt;span class=&quot;nt&quot;&gt;&amp;lt;/mainClass&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;/configuration&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;/plugin&amp;gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;Now that maven knows what the main class is, there is no need to pass the -D argument when you want to execute the Java app from maven&lt;/p&gt;

&lt;p&gt;&lt;code&gt;mvn compile&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;mvn exec:java&lt;/code&gt;  &lt;/p&gt;

&lt;h2 id=&quot;review-of-the-maven-commands&quot;&gt;Review of the maven commands&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;mvn compile:&lt;/strong&gt; compiles a maven project&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;mvn clean:&lt;/strong&gt; removes the target folder and all of its contentes. It will remove also any jar file packages that may have been created&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;mvn package:&lt;/strong&gt; creates a jar package if you specified &lt;em&gt;packaging&lt;/em&gt; in the POM file&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;mvn exec:java:&lt;/strong&gt; executes a Java app, you need to pass the -DmainClass argument for this command, or you can configure the mainClass in the POM file&lt;/li&gt;
&lt;/ul&gt;
</content>
 </entry>
 
 <entry>
   <title>Common Java Errors</title>
   <link href="http://tedhagos.com/java/java-errors.html"/>
   <updated>2014-06-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/java-errors</id>
   <content type="html">&lt;h1 id=&quot;forgetting-the-semicolon&quot;&gt;Forgetting the semicolon&lt;/h1&gt;

&lt;p&gt;The Java compiler ignores white space. The only way for it to know that your statement is complete or finished is if you put tample   &lt;br /&gt;
int i = 0
xi = i + 10;
\#+end_example  &lt;/p&gt;

&lt;p&gt;The first line was supposed to be complete at the assignment of the literal value zero to i, but because you did not properly terminate it, the compiler thinks the statement reads this way&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;int i = 0 i = i = 10;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Which of course doesn’t make sense. These kinds of errors are easy enough to spot when Java spits out a compilation error, but it may not always be as simple as this. Some IDEs may even help you out to quickly spot mistakes such as this one.&lt;/p&gt;

&lt;h1 id=&quot;running-the-byte-code&quot;&gt;Running the byte code&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;java Hello.class&lt;/code&gt; &lt;/p&gt;

&lt;p&gt;Results to &lt;em&gt;NoClassDefFoundError&lt;/em&gt;. Just drop the &lt;em&gt;.class&lt;/em&gt; extension when you run java programs, like this&lt;/p&gt;

&lt;p&gt;&lt;code&gt;java Hello&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;running-a-class-without-a-main-method&quot;&gt;Running a class without a main method&lt;/h1&gt;

&lt;p&gt;Java, like most other languages requires an entry point to properly run. So if you define a class like this and then try to run it, it will compile but will not run&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;// Hello.java
class Hello {

}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;javac Hello.java&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;java Hello&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The Java runtime will point it out to you, so it’s easy to spot this error. What’s not easy to spot are mispellings on the &lt;em&gt;main&lt;/em&gt; method&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;public static void Main(String args[]) // wrong 
public static void Main() // wrong
public static void main() // wrong
public static void main(String args) // wrong
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;All the examples above will compile, but won’t run. They all define a method called either &lt;em&gt;Main&lt;/em&gt; or &lt;em&gt;main&lt;/em&gt; but none of them complied with the prescribed signature for a Java entry point. The Java main needs to meet the following conditions&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;publicly accessible, hence the keyword &lt;em&gt;public&lt;/em&gt;. It means other classes can call it from any package&lt;/li&gt;
  &lt;li&gt;statically scoped, hence the &lt;em&gt;static&lt;/em&gt; keyword. It means there is no need to create an instance of the enclosing class in order to invoke the method. The method belongs to the class and not its instance&lt;/li&gt;
  &lt;li&gt;It does not return anything, hence the &lt;em&gt;void&lt;/em&gt; keyword&lt;/li&gt;
  &lt;li&gt;It accepts an array of java.lang.String type.&lt;/li&gt;
&lt;/ol&gt;

&lt;p&gt;A prooper main is written like this&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;class Hello {
  public static void main(String args[]) {

  }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;comparing-strings&quot;&gt;Comparing Strings&lt;/h1&gt;

&lt;pre&gt;&lt;code&gt;// filename: xCompareString.java

class CompareString {
  public static void main(String []args) {
    String a = &quot;abc&quot;;
    String b = &quot;abc&quot;;
    
    if(a == b) {System.out.println(&quot;a == b&quot;);}
    else {System.out.println(&quot;a &amp;lt;&amp;gt; b&quot;);}

  }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;There are two kinds of data values in Java. The first kind is the primitive data type. Java defines eight of them — &lt;em&gt;byte, short, int, long, char, float, double&lt;/em&gt; and &lt;em&gt;boolean&lt;/em&gt;. Use the double equal operator to determine equality for these types. The other kind of data in Java is the reference type. Reference types are objects. String is a reference type. &lt;/p&gt;

&lt;p&gt;The example ab ove is a tricky one because the expression &lt;code&gt;(a == b)&lt;/code&gt; will actually evaluate to true. But that only happened because of Java has a concept of a String literal pool . The variables &lt;em&gt;a&lt;/em&gt; and &lt;em&gt;b&lt;/em&gt; truly are pointing to exactly the same instance of “abc” in the pool. &lt;/p&gt;

&lt;p&gt;Comparisson of one String to another — or for that matter, one reference type to another — use the &lt;em&gt;.equals()&lt;/em&gt; method. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;// filename: CompareString.java

class CompareString {
  public static void main(String []args) {
    String a = &quot;abc&quot;;
    String b = &quot;abc&quot;;
    
    if(a.equals(b)) {System.out.println(&quot;a == b&quot;);}
    else {System.out.println(&quot;a &amp;lt;&amp;gt; b&quot;);}

  }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;arrays-start-with-zero-not-one&quot;&gt;Arrays start with zero, not one&lt;/h1&gt;

&lt;pre&gt;&lt;code&gt;String basket[] = {&quot;apple&quot;, &quot;orange&quot;, &quot;banana&quot;};

basket[0] -&amp;gt; &quot;apple&quot;
basket[1] -&amp;gt; &quot;orange&quot;
basket[2] -&amp;gt; &quot;banana&quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;So if you get the length of array, it will be &lt;em&gt;3&lt;/em&gt;, naturally, because it has 3 elements. But there is no &lt;code&gt;basket[3]&lt;/code&gt; because the first element of the array numbered as zero, not 1. If you try to access &lt;code&gt;basket[3]&lt;/code&gt;, you will see the ArrayIndexOutOfBoundsException. &lt;/p&gt;

&lt;p&gt;This thinng will bite when constructing loops that are intended to “walk” through the array, like this example below&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;class ZeroBase {
  public static void main(String []args) {
    String basket[] = {&quot;apple&quot;,&quot;orange&quot;,&quot;banana&quot;};
    for(int i=0;i &amp;lt;= basket.length; i++) {
      System.out.println(basket[i]);
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;This will compile, but will run into problems during execution. Just remember that the last element of the array is numbered as &lt;code&gt;basket.length -1&lt;/code&gt;, always account for the fact that it starts with zero. So, to fix the code sample above, just rewrite the for loop to read either&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;for(int i=0;i &amp;lt; basket.length; i++)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;or&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;for(int i=0;i &amp;lt;= (basket.length - 1); i++)
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;bibliography&quot;&gt;Bibliography&lt;/h1&gt;

&lt;p&gt;&lt;a href=&quot;http://www.javacoffeebreak.com/articles/toptenerrors.html&quot;&gt;http://www.javacoffeebreak.com/articles/toptenerrors.html&lt;/a&gt;
&lt;a href=&quot;http://docs.oracle.com/javase/tutorial/getStarted/problems/&quot;&gt;http://docs.oracle.com/javase/tutorial/getStarted/problems/&lt;/a&gt;
&lt;a href=&quot;http://www.open.ac.uk/StudentWeb/m874/!synterr.htm&quot;&gt;http://www.open.ac.uk/StudentWeb/m874/!synterr.htm&lt;/a&gt;
&lt;a href=&quot;http://cs-people.bu.edu/dgs/courses/cs111/assignments/errors.html&quot;&gt;http://cs-people.bu.edu/dgs/courses/cs111/assignments/errors.html&lt;/a&gt;&lt;/p&gt;

&lt;div id=&quot;footnotes&quot;&gt;
&lt;h2 class=&quot;footnotes&quot;&gt;Footnotes: &lt;/h2&gt;
&lt;div id=&quot;text-footnotes&quot;&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.1&quot; name=&quot;fn.1&quot; class=&quot;footnum&quot; href=&quot;#fnr.1&quot;&gt;1&lt;/a&gt;&lt;/sup&gt; The String literal or constant pool is a special area in memory. Strings that are created by assigning a literal or constant expression to a variable e.g. String s = &quot;Hello&quot;
will be kept inside the literal pool. A String created using String s = new String(&quot;Hello&quot;) will not be part of this pool. Read [String literal pool](java-string-literal-pool) for more details&lt;/div&gt;


&lt;/div&gt;
&lt;/div&gt;
</content>
 </entry>
 
 <entry>
   <title>Generic Programming</title>
   <link href="http://tedhagos.com/java/generics.html"/>
   <updated>2014-06-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/generics</id>
   <content type="html">
</content>
 </entry>
 
 <entry>
   <title>Encapsulation</title>
   <link href="http://tedhagos.com/java/encapsulation.html"/>
   <updated>2014-06-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/encapsulation</id>
   <content type="html">&lt;p&gt;Encapsulation combine behavior (method) and data (variables or states) into one. The atomic unit of code in Java is the class. You cannot write anything smaller than that. That is why even if you just want to print Hello World, you have to enclose it in a class.&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>Emacs Cheatsheet</title>
   <link href="http://tedhagos.com/emacs/emacs-cheatsheet.html"/>
   <updated>2014-06-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/emacs/emacs-cheatsheet</id>
   <content type="html">&lt;p&gt;Capital &lt;strong&gt;C&lt;/strong&gt; means press the CTRL key. Capital &lt;strong&gt;M&lt;/strong&gt; means meta key, it depends how you configured your meta key, typically this is &lt;em&gt;ALT&lt;/em&gt; key or the &lt;em&gt;ESC&lt;/em&gt; key. &lt;/p&gt;

&lt;p&gt;So when you see this — &lt;code&gt;C x C c&lt;/code&gt; — it means press the CTRL key then x, after that, press the CTRL key again then c.  &lt;/p&gt;

&lt;p&gt;When you see this — &lt;code&gt;C x b&lt;/code&gt;, press the CTRL key then x then b. &lt;/p&gt;

&lt;p&gt;When you see this — &lt;code&gt;M x package-install RET org RET&lt;/code&gt;, it means press the meta key (ALT or ESC), then x then type the command &lt;code&gt;package-install&lt;/code&gt;, press RETURN key then type &lt;em&gt;org&lt;/em&gt; then press the RETURN key again. &lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;C x C f:&lt;/strong&gt; Open a file. You can also open a file during start up e.g. &lt;em&gt;emacs filename.ext&lt;/em&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;C x k:&lt;/strong&gt; Close a file&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;C k:&lt;/strong&gt; Delete the current line, starting from cursor position&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;C x C s:&lt;/strong&gt; Save current buffer to a file&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;C s:&lt;/strong&gt; Lets you type a search string. The cursor position is immediately taken to the first occurence of the search string. This one seaches forward only. It means from the current cursor position up to the end of the document. If the cursor position is at the end of the document and the string you are looking for is somewhere at the top, this search technique won’t find the string. For that, you need to search backwards&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;C r:&lt;/strong&gt; How to search backwards. See C s&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;C x C c:&lt;/strong&gt; Exit emacs&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;C v:&lt;/strong&gt; Page down&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;M v:&lt;/strong&gt; Page up&lt;/li&gt;
  &lt;li&gt;**M % &lt;string&gt; RET &lt;new string=&quot;&quot;&gt;:** Search and replace &lt;string&gt; with &lt;new string=&quot;&quot;&gt;&lt;/new&gt;&lt;/string&gt;&lt;/new&gt;&lt;/string&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;C x b:&lt;/strong&gt; Select a different buffer&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;C x 1:&lt;/strong&gt; Kill the help window&lt;/li&gt;
  &lt;li&gt;
    &lt;p&gt;&lt;strong&gt;M x RET visual-line-mode:&lt;/strong&gt; Line wraps at the end of the window. The default setting of emacs is to just keep on typing horizontally. It will shoot through the edge of window. There are no line wraps. This command helps to keep your sanity a little bit when typing docs. If you want this setting to always be turned on, put this line in the emacs init file &lt;code&gt;(global-visual-line-mode 1)&lt;/code&gt;. 1 means on, 0 means off&lt;/p&gt;
  &lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;M x RET adaptive-wrap-prefix-mode:&lt;/strong&gt; Nice looking smart wrap, useful when working with lists.&lt;/li&gt;
&lt;/ul&gt;
</content>
 </entry>
 
 <entry>
   <title>Org problems and errors</title>
   <link href="http://tedhagos.com/emacs/troubleshooting-org.html"/>
   <updated>2014-06-19T00:00:00+08:00</updated>
   <id>http://tedhagos.com/emacs/troubleshooting-org</id>
   <content type="html">&lt;p&gt;&lt;code&gt;M x org-version&lt;/code&gt; shows 7.x.x when you expect it to be 8.x. This is a version conflict problem. This happened to my installation when I copied an .emacs init file, with ‘org’ references to it, and used it to load up a fresh install of emacs. Once I grabbed the org package from elpa, it resulted into tons of errors. &lt;/p&gt;

&lt;p&gt;The solution is to do the following:&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;Remove the org folder inside ~/.emacs.d/elpa&lt;/li&gt;
  &lt;li&gt;Edit the ~/.emacs init files, remove any references to ‘org’&lt;/li&gt;
  &lt;li&gt;Load up emacs using the clean .emacs init file&lt;/li&gt;
  &lt;li&gt;Reinstall org from either elpa or orgmode source&lt;/li&gt;
  &lt;li&gt;Exit and restart emacs. That solved the problem&lt;/li&gt;
&lt;/ol&gt;
</content>
 </entry>
 
 <entry>
   <title>Org Note Taking</title>
   <link href="http://tedhagos.com/emacs/setting-up-org.html"/>
   <updated>2014-06-19T00:00:00+08:00</updated>
   <id>http://tedhagos.com/emacs/setting-up-org</id>
   <content type="html">&lt;h1 id=&quot;use-elpa-or-org-mode-packages&quot;&gt;Use elpa or org-mode packages&lt;/h1&gt;

&lt;p&gt;Emacs 24.x use the GNU elpa repositories, so if you issue the command &lt;code&gt;M x list-packages&lt;/code&gt;, you will see all the available (and installed) packages in Emacs. You need to be connected to the internet when you do this. Emacs will make a connection to elpa repository and grab a list of available software packages. &lt;/p&gt;

&lt;p&gt;Since you already know that you want to install &lt;em&gt;org&lt;/em&gt;, you can take a shorter route to the installation by issuing the command &lt;code&gt;M x package install RET org RET&lt;/code&gt;. Wait for a while because that package will be grabbed from elpa and will be compiled afterwards.&lt;/p&gt;

&lt;p&gt;Exit emacs, &lt;code&gt;C x C c&lt;/code&gt; and restart it. You should now have the org package. You can check which version of org has been installed using the command &lt;code&gt;M x org-version RET&lt;/code&gt;. You might see something similar to &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;Org-mode version 8.2.6 (8.2.6-22-gb11b4a-elpa @ /home/thagos/.emacs.d/elpa/org-20140519/)/
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;problems-with-version&quot;&gt;Problems with version&lt;/h1&gt;

&lt;p&gt;Sometimes you expect your version of org to be one thing, and then see another e.g., I expected my verion of org to be &lt;code&gt;8.x&lt;/code&gt; but right after I grabbed org from the repos, it still showed 7.x. It appeared to be reverting back to the version of org built-in within Emacs. A telling sign that there was a problem on installation was that it showed errors during the compilation of the &lt;code&gt;org&lt;/code&gt; package. It’s okay to see warnings, but not compilation errors. To solve this, follow the steps below.
1.  Exit emacs
2.  Sanitize the init files &lt;em&gt;~.emacs&lt;/em&gt; or &lt;em&gt;~&lt;/em&gt;.emacs.d/init.el/. Make sure there isn’t any reference to &lt;em&gt;org&lt;/em&gt; in the startup up files
3.  Remove the &lt;em&gt;org&lt;/em&gt; directories inside ~/.emacs.d/elpa/ folder.
4.  Start emacs using the sanitized init files
5.  Install the package again using &lt;code&gt;M x package-install RET org&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;The compilation errors should disappear and you should have an updated version of org&lt;/p&gt;

&lt;h1 id=&quot;how-to-trigger-publishing&quot;&gt;How to trigger publishing&lt;/h1&gt;

&lt;table border=&quot;2&quot; cellspacing=&quot;0&quot; cellpadding=&quot;6&quot; rules=&quot;groups&quot; frame=&quot;hsides&quot;&gt;


&lt;colgroup&gt;
&lt;col class=&quot;left&quot; /&gt;

&lt;col class=&quot;left&quot; /&gt;
&lt;/colgroup&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`C c C e P x`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Will prompt for the name of the project to be published.&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;&amp;#xa0;&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;This is the same as `M x org-publish`&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`C c C e P p`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;It will publish the current project. Equal to `M x org-publish-current-project`&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;`C c C e P a`&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Will publish all the defined projects in *org-project-alist*&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;Org uses timestamps to trigger publishing. That means it won’t republish documents that have not changed. To override this behavior, you need to change the default behavior of Org when publishing. One way to do this is to customize the variable &lt;em&gt;org-publish-use-timestamps-flag&lt;/em&gt;. This can be done within an emacs session. You can also achieve the same result by adding an entry to the the emacs init file, this could be the the ~/.emacs file. Add this entry &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(setq org-publish-use-timestamps-flag nil)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;When this variable is set to &lt;em&gt;nil&lt;/em&gt;, org will republish all of the files defined inside org-publish-project-alist.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Org HTML Postamble</title>
   <link href="http://tedhagos.com/emacs/org-postamble.html"/>
   <updated>2014-06-19T00:00:00+08:00</updated>
   <id>http://tedhagos.com/emacs/org-postamble</id>
   <content type="html">&lt;p&gt;Org can export the postamble to HTML, the default postamble exports the author name, date of creation, creator information (which shows the Org and Emacs version) and a link to validate the XHTML document. It usually is at the foot part of generated html document. &lt;/p&gt;

&lt;p&gt;The default postamble can be overidden by specifying a value of postamble. Org exports certain variables which you can override and customize to your liking. The variable &lt;code&gt;org-html-postamble&lt;/code&gt; is exported. &lt;/p&gt;

&lt;h1 id=&quot;inspecting-the-current-value-of-the-postamble&quot;&gt;Inspecting the current value of the postamble&lt;/h1&gt;

&lt;p&gt;Before you monkey around the postamble value, you might want to check the original/default values of the postamble, even just reference purposes. You can inspect the current value of the postamble by issuing the command&lt;/p&gt;

&lt;p&gt;&lt;code&gt;C h v org-html-postamble RET&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Shows the current value of the postamble. &lt;/p&gt;

&lt;p&gt;&lt;code&gt;C h v org-html-postamble-format RET&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Tells you a bit more information of what you can put inside the postamble. Below is excerpt from that last command&lt;/p&gt;

&lt;table border=&quot;2&quot; cellspacing=&quot;0&quot; cellpadding=&quot;6&quot; rules=&quot;groups&quot; frame=&quot;hsides&quot;&gt;


&lt;colgroup&gt;
&lt;col class=&quot;left&quot; /&gt;

&lt;col class=&quot;left&quot; /&gt;
&lt;/colgroup&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;%a&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;author&#39;s name&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;%e&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;author&#39;s email, if specified&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;%d&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;timestamp, date created&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;%c&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;creator string (Org and Emacs&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;%v&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;The link for XHTML validation&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;%T&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;The time of exporting&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;%C&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Time of last modification&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;One way to set the postamble is to set the value of &lt;code&gt;org-html-postamble&lt;/code&gt; in the init files, e.g. &lt;em&gt;~&lt;/em&gt;.emacs/. You can add the following line&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(setq org-html-postamble &quot;whatever you want, make sure its html though&quot;)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If you have more than one &lt;code&gt;org&lt;/code&gt; project in your alist, this may not be ideal. You can set the value of the postamble for each invidual project, e.g.&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-lisp&quot; data-lang=&quot;lisp&quot;&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;setq&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;org-publish-project-alist&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;&amp;#39;&amp;#39;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;project1&amp;quot;&lt;/span&gt;
   &lt;span class=&quot;ss&quot;&gt;:author&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;Your name&amp;quot;&lt;/span&gt;
   &lt;span class=&quot;ss&quot;&gt;:html-postamble&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;postamble for project 1&amp;quot;&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;project2&amp;quot;&lt;/span&gt;
   &lt;span class=&quot;ss&quot;&gt;:author&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;Author of project 2&amp;quot;&lt;/span&gt;
   &lt;span class=&quot;ss&quot;&gt;:html-postamble&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;&amp;quot;postamble for project 2&amp;quot;&lt;/span&gt;
  &lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;You can put HTML elements in the postamble.&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Org HTML Publishing Cheatsheet</title>
   <link href="http://tedhagos.com/emacs/org-html-publishing-cheatsheet.html"/>
   <updated>2014-06-19T00:00:00+08:00</updated>
   <id>http://tedhagos.com/emacs/org-html-publishing-cheatsheet</id>
   <content type="html">&lt;h1 id=&quot;inclusion-of-table-of-contents&quot;&gt;Inclusion of table of contents&lt;/h1&gt;

&lt;p&gt;Two ways of doing this. First one is in the &lt;em&gt;org-publish-project-alist&lt;/em&gt; declaration. Actually, you don’t have to do anything in there. TOC is included by default, if the &lt;em&gt;:with-toc&lt;/em&gt; directive is not &lt;em&gt;nil&lt;/em&gt; the exported HTML will always contain a table of contents unless otherwise overidden by &lt;em&gt;OPTIONS&lt;/em&gt; variable in the org document itself.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;(setq org-publish-alist
&#39;((
  &quot;mynotes&quot;
  :with-toc nil
  :base-directory &quot;/somedir/&quot;
))
)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The above example will supress the generation of TOC on every exported document. If you want to generated the TOC, remove the &lt;em&gt;:with-toc nil&lt;/em&gt; statement&lt;/p&gt;

&lt;p&gt;The second way of generating TOC is to put statement in the export options of each org document e.g. if you have a document named &lt;em&gt;somedocument.org&lt;/em&gt;, it might look like this&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;#+TITLE: My awesome document
#+TOC: headlines 1

Lorem ipsum dolor sit amet /consectitur/ alet *si do*
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Example document above will contain a table a TOC when exported. The value &lt;em&gt;1&lt;/em&gt; means heading 1 will be exported. If you use the value &lt;em&gt;2&lt;/em&gt; for headlines, TOC will contain until second level headings etc. The export options written on the org document itself will override whatever setting you have on the &lt;em&gt;project-alist&lt;/em&gt; &lt;/p&gt;

&lt;h1 id=&quot;document-navigation&quot;&gt;Document navigation&lt;/h1&gt;

&lt;table&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;The *UP&lt;/td&gt;
      &lt;td&gt;HOME* navigation links can be exported on each document if you use the :html_link_up and :html_link_home settings on &lt;em&gt;project-alist&lt;/em&gt; e.g.&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;pre&gt;&lt;code&gt;(setq org-publish-alist
&#39;((
  &quot;mynotes&quot;
  :with-toc nil
  :base-directory &quot;/somedir/&quot;
  :html_link_up &quot;index.html&quot;
  :html_link_home &quot;../index.html&quot;
))
)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;If you put the link up/home directives in the alist, all exported HTML documents will contain a link to both home and up. This can be overriden on each org document using the OPTIONS template, e.g.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;#+TITLE: My awesome document
#+TOC: headlines 1

#+HTML_LINK_UP: index.html
#+HTML_LINK_HOME: somepage.html 

Lorem ipsum dolor sit amet /consectitur/ alet *si do*
&lt;/code&gt;&lt;/pre&gt;

&lt;h1 id=&quot;embed-an-iframe&quot;&gt;Embed an iframe&lt;/h1&gt;

&lt;p&gt;Use &lt;code&gt;#+begin_html .. #+end_html&lt;/code&gt; to embed the iframe. Make sure that in the document, you have turned off the subscript/superscript setting. In the org doc itself, just add the options line to turn off the subscript action, then you can use the begin..end blocks to insert HTML codes&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;#+options: ^:nil

...

#+begin_html
&amp;lt;iframe&amp;gt; 

&amp;lt;/iframe&amp;gt;
#+end_html
&lt;/code&gt;&lt;/pre&gt;
</content>
 </entry>
 
 <entry>
   <title>Org Cheatsheet</title>
   <link href="http://tedhagos.com/emacs/org-cheatsheet.html"/>
   <updated>2014-06-19T00:00:00+08:00</updated>
   <id>http://tedhagos.com/emacs/org-cheatsheet</id>
   <content type="html">&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;C u C u         :&lt;/strong&gt; Collapse or expand all headings. It cycles visibility&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;C u C u C u     :&lt;/strong&gt; Expand all headings&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;M x org-publish :&lt;/strong&gt; Publish an org project (defined in alist)&lt;/li&gt;
&lt;/ul&gt;
</content>
 </entry>
 
 <entry>
   <title>Object Life cycle</title>
   <link href="http://tedhagos.com/java/object-lifecycle.html"/>
   <updated>2014-06-19T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/object-lifecycle</id>
   <content type="html">&lt;p&gt;There are things that happen inside a Java object from the time you create it using &lt;strong&gt;new&lt;/strong&gt; up until you call its first method. The Java runtime makes certain calls to specific blocks of code in your class definition.&lt;/p&gt;

&lt;p&gt;A Java object goes through several stages before you can call any of its instance methods. The static initializer kicks in first, if one is defined. And after that, the instance initializer and  then the constructor kicks in. The code below illustrates the construction of such initializers. &lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-java&quot; data-lang=&quot;java&quot;&gt;&lt;span class=&quot;c1&quot;&gt;// LifeCycle.java&lt;/span&gt;
&lt;span class=&quot;kn&quot;&gt;import&lt;/span&gt; &lt;span class=&quot;nn&quot;&gt;static&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;java&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;lang&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;System&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;

&lt;span class=&quot;kd&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;LifeCycle&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;

  &lt;span class=&quot;kd&quot;&gt;static&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
   &lt;span class=&quot;n&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;1&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;

  &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;2&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;

  &lt;span class=&quot;n&quot;&gt;LifeCycle&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;3&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;

  &lt;span class=&quot;kd&quot;&gt;protected&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;void&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;finalize&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;println&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;4&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;

  &lt;span class=&quot;kd&quot;&gt;public&lt;/span&gt; &lt;span class=&quot;kd&quot;&gt;static&lt;/span&gt; &lt;span class=&quot;kt&quot;&gt;void&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;main&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;String&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;[]&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;args&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;LifeCycle&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;lc&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;new&lt;/span&gt; &lt;span class=&quot;nf&quot;&gt;LifeCycle&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;();&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;lc&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;kc&quot;&gt;null&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;Initializers are still just code blocks. When you define variables inside them, they will still be local to the block. So you can’t do things like this&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-java&quot; data-lang=&quot;java&quot;&gt;&lt;span class=&quot;kd&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;nc&quot;&gt;LifeCycle&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;kt&quot;&gt;int&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;10&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;;&lt;/span&gt;  &lt;span class=&quot;c1&quot;&gt;// i still local to the block&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;

  &lt;span class=&quot;n&quot;&gt;LifeCycle&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;()&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;{&lt;/span&gt;
    &lt;span class=&quot;n&quot;&gt;out&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;na&quot;&gt;printf&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;i = %d&amp;quot;&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;i&lt;/span&gt;&lt;span class=&quot;o&quot;&gt;);&lt;/span&gt; &lt;span class=&quot;c1&quot;&gt;// cannot access i from here&lt;/span&gt;
  &lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;
&lt;span class=&quot;o&quot;&gt;}&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;h1 id=&quot;static-initializers&quot;&gt;Static initializers&lt;/h1&gt;

&lt;h1 id=&quot;instance-initializer&quot;&gt;instance initializer&lt;/h1&gt;

&lt;h1 id=&quot;constructors&quot;&gt;Constructors&lt;/h1&gt;

&lt;p&gt;Q: Are variables defined in the initializer blocks local to the blocks, or like instance members also
Q: Are blocks inherited
Q: Can I call methods inside the blocks
Q: Why bother with blocks in the first place&lt;/p&gt;

&lt;p&gt;&lt;a href=&quot;http://docs.oracle.com/javase/tutorial/java/javaOO/initial.html&quot;&gt;http://docs.oracle.com/javase/tutorial/java/javaOO/initial.html&lt;/a&gt;&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Tools</title>
   <link href="http://tedhagos.com/javascript/tools.html"/>
   <updated>2014-06-15T00:00:00+08:00</updated>
   <id>http://tedhagos.com/javascript/tools</id>
   <content type="html">&lt;h1 id=&quot;simple-web-server&quot;&gt;Simple web server&lt;/h1&gt;

&lt;p&gt;If you just need to serve up HTML, JS and CSS files, there is no need to setup Apache or NGINX. For quick and dirty testing of a static website, use anyone of these tools.&lt;/p&gt;

&lt;h2 id=&quot;http-server-npm&quot;&gt;http-server, npm&lt;/h2&gt;

&lt;p&gt;&lt;code&gt;sudo npm install -g http-server&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;cd /folder/path/to/html/files/&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Then point your browser to &lt;a href=&quot;http://localhost:8080&quot;&gt;http://localhost:8080&lt;/a&gt;&lt;/p&gt;

&lt;h2 id=&quot;simplehttpserver-python&quot;&gt;SimpleHTTPServer, python&lt;/h2&gt;

&lt;p&gt;&lt;code&gt;cd /path/to/html/files/&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;python -m SimpleHTTPServer&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Point the browser to to &lt;a href=&quot;http://localhost:8000&quot;&gt;http://localhost:8000&lt;/a&gt;&lt;/p&gt;

&lt;h2 id=&quot;php-standalone-server&quot;&gt;PHP standalone server&lt;/h2&gt;

&lt;p&gt;This tool requires a PHP version 5.4 or higher&lt;/p&gt;

&lt;p&gt;&lt;code&gt;cd /path/to/html/files/&lt;/code&gt;&lt;br /&gt;
&lt;code&gt;php -S localhost:8080&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;Obviously, the server is listening on port 8080, so point your browser to it&lt;/p&gt;

&lt;h1 id=&quot;restful-endpoints&quot;&gt;RESTful endpoints&lt;/h1&gt;

&lt;h2 id=&quot;sinatra&quot;&gt;Sinatra&lt;/h2&gt;

&lt;h2 id=&quot;nodejs&quot;&gt;NodeJS&lt;/h2&gt;

&lt;h2 id=&quot;java&quot;&gt;Java&lt;/h2&gt;
</content>
 </entry>
 
 <entry>
   <title>String Literal Pool</title>
   <link href="http://tedhagos.com/java/java-string-literal-pool.html"/>
   <updated>2014-06-13T00:00:00+08:00</updated>
   <id>http://tedhagos.com/java/java-string-literal-pool</id>
   <content type="html">
</content>
 </entry>
 
 <entry>
   <title>Database Programming</title>
   <link href="http://tedhagos.com/android/android-database-programming.html"/>
   <updated>2012-12-12T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/android-database-programming</id>
   <content type="html">&lt;h1 id=&quot;todo-rewrite-the-example&quot;&gt;TODO Rewrite the example&lt;/h1&gt;
</content>
 </entry>
 
 <entry>
   <title>CLI Development Environment</title>
   <link href="http://tedhagos.com/android/CLI-development-environment.html"/>
   <updated>2012-10-28T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/CLI-development-environment</id>
   <content type="html">&lt;p&gt;The Android SDK is a collection of tools and libraries to help you build Android projects. Before you can create projects, you must install the SDK. The SDK also installs, among other things, the Android Virtual Device or the AVD. The AVD emulates a physical android device. You can use this for testing your applications.&lt;/p&gt;

&lt;p&gt;The SDK depends on other software. You need to make sure that you have the following installed on your system.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;A Java Development Kit. You will need the full JDK not just the JRE. The JRE is good only for running Java software, not for developing them. The prescribed versions of JDK is either 1.5 or 1.6 but I have experience running them on Java 7 and 8 without problems&lt;/li&gt;
  &lt;li&gt;Apache Ant will also be needed. Android uses Ant as a build tool&lt;/li&gt;
&lt;/ul&gt;

&lt;h1 id=&quot;download&quot;&gt;Download&lt;/h1&gt;

&lt;p&gt;The SDK can be downloaded from &lt;a href=&quot;https://developer.android.com/sdk&quot;&gt;https://developer.android.com/sdk&lt;/a&gt;. Click the link which says “View all downloads and sizes”. The SDK is available for Mac, Linux and Windows, find the right installer for your platform and download it.&lt;/p&gt;

&lt;h1 id=&quot;configure&quot;&gt;Configure&lt;/h1&gt;

&lt;p&gt;At the time of writing, the SDK is packaged as a zipped file.  Unzip the downloaded file somewhere your disk. Unzip it preferably to a directory where you have read,write and execute privileges so you do not have to worry about tweaking permissions later. The name of directory where you unzipped the SDK files will be known as ANDROID_HOME. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;~/android-sdk-linux/

├── add-ons
├── build-tools
├── docs
├── platforms
├── platform-tools
├── samples
├── sources
├── system-images
├── temp
└── tools
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The diagram above shows the folder structure of the unzipped SDK files on a Linux box. If you are on Windows, this could be &lt;strong&gt;c:\&amp;gt;Users\yourname\android-sdk-windows&lt;/strong&gt;. On a Mac it could read &lt;strong&gt;~/android-sdk-mac&lt;/strong&gt;.&lt;/p&gt;

&lt;p&gt;There are two specific folders in the ANDROID_HOME that you must include in your system path, the tools folder and the platform-tools folder.  The common utilities and commands that you will use in Android Development are found on those folders. &lt;/p&gt;

&lt;h2 id=&quot;mac&quot;&gt;Mac&lt;/h2&gt;

&lt;p&gt;You can edit your &lt;strong&gt;~/.bash_profile&lt;/strong&gt; and include the following&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;export ANDROID_HOME=/&amp;lt;location of android home&amp;gt;
export PATH=$PATH:ANDROID_HOME/tools:$ANDROID_HOME/platform-tools:.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Replace &lt;location of=&quot;&quot; android=&quot;&quot; home=&quot;&quot;&gt; with the actual directory location of your ANDROID_HOME&lt;/location&gt;&lt;/p&gt;

&lt;h2 id=&quot;linux&quot;&gt;Linux&lt;/h2&gt;

&lt;p&gt;You can edit &lt;strong&gt;~/.bashrc&lt;/strong&gt; and include the following&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;export ANDROID_HOME=/&amp;lt;location of android home&amp;gt;/
export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools:.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Replace &lt;location of=&quot;&quot; android=&quot;&quot; home=&quot;&quot;&gt; with the actual directory location of your ANDROID_HOME&lt;/location&gt;&lt;/p&gt;

&lt;h2 id=&quot;windows&quot;&gt;Windows&lt;/h2&gt;

&lt;p&gt;You can include the path of tools and platform-tools using the GUI tools. Go to Control Panel, System Properties then go Environment Variables. You can put it either in User or System variables. If you do not have Administrative privileges on the Windows machine, you may not be able to alter the System variables. &lt;/p&gt;

&lt;p&gt;Alternatively, you can create a batch file somewhere on your user directory and set the paths on that batch file.  Launch cmd.exe to get a command line window&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;cd \Users\&amp;lt;yourname&amp;gt;
notepad adev.bat
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Write the following inside adev.bat&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;set ANDROID_HOME=c:\&amp;lt;location of android home&amp;gt;
set PATH=%PATH%/tools;%ANDROID_HOME%/platform-tools:.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You will need to run this batch file each time you open a cmd window so the Android path information can be recognized. The moment you close the cmd window, these path settings will be lost. If you include the Android paths using the System Properties method, will be permanent. Each time you open an cmd window, the paths will be set automatically.&lt;/p&gt;

&lt;h1 id=&quot;download-the-android-versions&quot;&gt;Download the Android versions&lt;/h1&gt;

&lt;p&gt;The SDK includes just the tools and utilities for development. It does not include the specific Android platform libraries that you will need. You will need to download the specific platforms/versions using the SDK manager. You may also need to download system images which will be required when you create an emulator. &lt;/p&gt;

&lt;p&gt;Open a terminal window and type &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;android
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;This will launch the Android SDK Manager. If you get a “bad command command or file name” or a “command not found error” that means you have not included the &lt;strong&gt;tools&lt;/strong&gt; and &lt;strong&gt;platform-tools&lt;/strong&gt; folder of the SDK into your PATH. Fix that first, then try the &lt;strong&gt;android&lt;/strong&gt; command again.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/android-sdk-manager.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;Android has been around for quite some time. There have been several versions of it over the course of many years. Each version of the Android platform can be downloaded using this tool. If you have a lot of bandwidth and disk space, you can download all of them by ticking all the check boxes you can find. Or you can simply download the Android versions you want to target for development.&lt;/p&gt;

&lt;p&gt;If you want to develop apps that will run on KitKat, tick the box of “Android 4.4.2 (API 19)”. At a minimum, you will need the “SDK platform”, so check that. You will also need the “System Images”, check those that you want to use. If you are unsure which System Image you will need, just check “Intel X86 Atom System Image”.  &lt;/p&gt;

&lt;p&gt;Scroll down the SDK Manager window and go to the “Extras” section. Check the “Android Support Library”. If you are on Windows, you might need the “Google USB Driver”, Mac and Linux users do not need the drivers. The USB drivers will be needed if you use a real Android device to run and test your app.  &lt;/p&gt;

&lt;p&gt;Once you are happy with your selections, click the “Install packages” button. You will have to accept the license agreement before any download begins.  &lt;/p&gt;

&lt;h1 id=&quot;android-virtual-device&quot;&gt;Android Virtual Device&lt;/h1&gt;

&lt;p&gt;The Android Virtual Device emulate a real device. You can use this for testing your apps in place of a real device. I do highly recommend testing your app on real device before you release to the public. There is no substitute with real device testing.&lt;/p&gt;

&lt;h2 id=&quot;create-an-avd&quot;&gt;Create an AVD&lt;/h2&gt;

&lt;p&gt;You can create an AVD for any Android platform provided that you downloaded a System Image for it. To create an AVD, use the following command on a terminal window&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;android create avd --name kitkat --target android-19 --abi default/x86
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The preceding command will create an an AVD for android API level 19, which is KitKat version. It was given a name “kitkat” using the –name flag. The System Image used was the default/x86 image (Intel X86 Atom System Image). &lt;/p&gt;

&lt;p&gt;If you are wondering how did we know what values to put in the command options, that answer is we got them by listing the available android targets. You can view the available targets using the command &lt;strong&gt;android list targets&lt;/strong&gt;. It will list all the versions of Android which you downloaded earlier using the SDK Manager. Below is a snippet of the output using the android list targets command.&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;id: 11 or &quot;android-19&quot;
     Name: Android 4.4.2
     Type: Platform
     API level: 19
     Revision: 4
     Skins: HVGA, QVGA, WQVGA400, WQVGA432, WSVGA, WVGA800 (default), WVGA854, WXGA720, WXGA800, WXGA800-7in
 Tag/ABIs : default/armeabi-v7a, default/x86 -   **&amp;amp;#x2013;name**. Put any name you want, I used kikat in our example because it is descriptive -   **&amp;amp;#x2013;target**. The API Level for your application. If we wanted to create a project that will run on JellyBean, I would have put android-18. But I did put android-19 because I wanted the app to run on KitKat -   **&amp;amp;#x2013;abi**.  The name of the System Image that you will use for the emulator, in our example above I could also have used default/armeabi-v7a because it is also listed among the available targets
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;The create avd command has a couple of options you can use, but we only used three of them. You can pass other options that will affect things like skin, sdcard storage etc.  You can learn the other options of this command by typing &lt;strong&gt;android create avd&lt;/strong&gt; without any options. That will display the help file.&lt;/p&gt;

&lt;p&gt;Overtime, you will create more AVDs because you will target different Android versions. To find out how many and what kind of AVDs you already have created, use the command&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;android list avd
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;That command will output the details of all the AVDs you have created.&lt;/p&gt;

&lt;p&gt;You can test the AVD you just created by launching it&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;emulator -avd kitkat
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;This command will launch the emulator in its own window. Just close the window if you want to terminate the emulator. &lt;/p&gt;

&lt;h2 id=&quot;alternative-avd-management&quot;&gt;Alternative AVD Management&lt;/h2&gt;

&lt;p&gt;The creation and management of AVDs can also be accomplished using the GUI tool of the Android SDK Manager. Launch the SDK Manager again using the &lt;strong&gt;android&lt;/strong&gt; command from a terminal window. Go to “Tools” then “Manage AVDs”. &lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/avd-creation.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/avd-launched.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>Layout Weights</title>
   <link href="http://tedhagos.com/android/AS-layout-weights.html"/>
   <updated>2012-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/AS-layout-weights</id>
   <content type="html">&lt;p&gt;A project created with a default Activity will include an XML file and an Activity class. These two are given to you so you can get started quickly in displaying at least one screen in your application. The screen is of vanilla construction. This default layout is mostly used for some quick fire challenges. The kinds of things you do when you just want to test some things.&lt;/p&gt;

&lt;p&gt;Real applications typically display some sophistication in their layouts. Let’s look at one small change you can do to effect some layout changes. The code snippet below is typical of the vanilla XML file given to us during project creation.&lt;/p&gt;

&lt;h2 id=&quot;review-of-linearlayout&quot;&gt;Review of LinearLayout&lt;/h2&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-xml&quot; data-lang=&quot;xml&quot;&gt;&lt;span class=&quot;cp&quot;&gt;&amp;lt;?xml version=&amp;quot;1.0&amp;quot; encoding=&amp;quot;utf-8&amp;quot;?&amp;gt;&lt;/span&gt;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;LinearLayout&lt;/span&gt; &lt;span class=&quot;na&quot;&gt;xmlns:android=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;http://schemas...&amp;quot;&lt;/span&gt;&lt;span class=&quot;nt&quot;&gt;&amp;gt;&lt;/span&gt;
  android:orientation=&amp;quot;vertical&amp;quot;
  android:layout_width=&amp;quot;fill_parent&amp;quot;
&lt;span class=&quot;nt&quot;&gt;&amp;lt;/LinearLayout&amp;gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;The orientation is set to &lt;strong&gt;vertical&lt;/strong&gt; and the layout_width is set to &lt;strong&gt;fill_parent&lt;/strong&gt;. With those setting in effect, each View component you will add to this layout will be stacked on top of each other. The first View object to be added will be the one on top, the next View object will follow suit.&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-xml&quot; data-lang=&quot;xml&quot;&gt;&lt;span class=&quot;nt&quot;&gt;&amp;lt;LinearLayout&amp;gt;&lt;/span&gt;
  android:orientation=&amp;quot;vertical&amp;quot;
  android:layout_width=&amp;quot;fill_parent&amp;quot;  

  &lt;span class=&quot;nt&quot;&gt;&amp;lt;Button&lt;/span&gt; &lt;span class=&quot;err&quot;&gt;...&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;/&amp;gt;&lt;/span&gt;
  &lt;span class=&quot;nt&quot;&gt;&amp;lt;CheckBox&lt;/span&gt; &lt;span class=&quot;err&quot;&gt;...&lt;/span&gt; &lt;span class=&quot;nt&quot;&gt;/&amp;gt;&lt;/span&gt;

&lt;span class=&quot;nt&quot;&gt;&amp;lt;/LinearLayout&amp;gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;The preceding code example will result in a screen which has a CheckBox with a Button View right on top of it. The Button occupied whole width of the screen because the LinearLayout container says &lt;strong&gt;fill_parent&lt;/strong&gt; on its layout width. The Checkbox did the same, it spanned the whole width of the screen for the same reason the Button did. No surprises here. This is how a linear layout is supposed to work. The behavior is expected. &lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/linear-layout-vertical.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;There are some class of applications for which this kind of layouting might be okay. If the screen for example just wants to display a series of check boxes stacked on top of each other, like in a survey form, then this layout will suffice. No further tweaking is needed. &lt;/p&gt;

&lt;h2 id=&quot;weighted-layout&quot;&gt;Weighted layout&lt;/h2&gt;

&lt;p&gt;Let’s go to another layout example. Suppose we create a screen that allows a user to enter some data and when the user clicks a button, whatever he entered onto the text box will be added to a static text View, like a label. To build this example, we will need the following View objects: &lt;strong&gt;EditText, TextView&lt;/strong&gt; and &lt;strong&gt;Button&lt;/strong&gt;. &lt;/p&gt;

&lt;p&gt;An EditText View allows for user input. If you have programming experience, you may have called this by a different name, perhaps you referred to it before as a textfield. Its the same thing. A TextView is a static text control. Some programmers call it a label. And you already know what a Button View does. &lt;/p&gt;

&lt;p&gt;Once we have created a new project with a default Activity, we can add the Views to our Layout. It could look like the next code sample&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-xml&quot; data-lang=&quot;xml&quot;&gt;&lt;span class=&quot;nt&quot;&gt;&amp;lt;LinearLayout&lt;/span&gt;
  &lt;span class=&quot;na&quot;&gt;android:orientation=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;vertical&amp;quot;&lt;/span&gt;
  &lt;span class=&quot;na&quot;&gt;android:layout_width=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;fill_parent&amp;quot;&lt;/span&gt;
  &lt;span class=&quot;na&quot;&gt;android:layout_height=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;fill_parent&amp;quot;&lt;/span&gt;
  &lt;span class=&quot;na&quot;&gt;xmlns:android=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;&lt;/span&gt;&lt;span class=&quot;nt&quot;&gt;&amp;gt;&lt;/span&gt;

  &lt;span class=&quot;nt&quot;&gt;&amp;lt;EditText&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_width=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;match_parent&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_height=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;wrap_content&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:id=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;@+id/editText&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_gravity=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;center_horizontal&amp;quot;&lt;/span&gt;&lt;span class=&quot;nt&quot;&gt;/&amp;gt;&lt;/span&gt;

  &lt;span class=&quot;nt&quot;&gt;&amp;lt;TextView&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_width=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;match_parent&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_height=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;wrap_content&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:textAppearance=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;?android:attr/textAppearanceLarge&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:text=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;Large Text&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:id=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;@+id/textView&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_gravity=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;center_horizontal&amp;quot;&lt;/span&gt;&lt;span class=&quot;nt&quot;&gt;/&amp;gt;&lt;/span&gt;

  &lt;span class=&quot;nt&quot;&gt;&amp;lt;Button&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_width=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;match_parent&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_height=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;wrap_content&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:text=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;New Button&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:id=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;@+id/button&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_gravity=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;center_horizontal&amp;quot;&lt;/span&gt;&lt;span class=&quot;nt&quot;&gt;/&amp;gt;&lt;/span&gt;

&lt;span class=&quot;nt&quot;&gt;&amp;lt;/LinearLayout&amp;gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;This will result to a screen where the EditText, TextView and the Button are stacked on top of one another. Each of the Views stretching out to the whole width of the screen. That may not be a problem for the width but it will be a problem for the height. Each View will vertically occupy just what it needs to display their content. The Button will size its height just enough to accomodate its label, the TextEdit will size itself accordingly to accomodate its current textual content and the EditText will size itself to accomodate a single line text input. &lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/weighted-layout.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The screen layout is not tight. There is plenty of unutilized screen space after the Button which makes the design clumsy. Putting screen space to good use gives the the app a more sophiscticated look. &lt;/p&gt;

&lt;p&gt;A small change we can make is to anchor the EditText and the Button on the opposite sides of the screen, top and bottom to be precise, so that the TextEdit can stretch itself vertically in between and occupy whatever space is left. To achieve this result, we will assign layout weights on each View.&lt;/p&gt;

&lt;p&gt;LinearLayout allows weight assignment for each View that is contained within. All Views by default are assigned a weight of zero. If the weight of a View is zero, the View cannot grow in space beyond what it needs to display its content. A View whose weight is more than zero is deemed to be more important. Hence it is allowed to stretch and hog the remaining space left on the screen.&lt;/p&gt;

&lt;p&gt;To achieve our desired layout, we will make the weight of the Button and EditText zero and assign a weight of one to the TextView&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-xml&quot; data-lang=&quot;xml&quot;&gt;&lt;span class=&quot;nt&quot;&gt;&amp;lt;LinearLayout&lt;/span&gt;
  &lt;span class=&quot;na&quot;&gt;android:orientation=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;vertical&amp;quot;&lt;/span&gt;
  &lt;span class=&quot;na&quot;&gt;android:layout_width=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;fill_parent&amp;quot;&lt;/span&gt;
  &lt;span class=&quot;na&quot;&gt;android:layout_height=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;fill_parent&amp;quot;&lt;/span&gt;
  &lt;span class=&quot;na&quot;&gt;xmlns:android=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;http://schemas.android.com/apk/res/android&amp;quot;&lt;/span&gt;&lt;span class=&quot;nt&quot;&gt;&amp;gt;&lt;/span&gt;

  &lt;span class=&quot;nt&quot;&gt;&amp;lt;EditText&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_width=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;match_parent&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_height=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;wrap_content&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:id=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;@+id/editText&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_gravity=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;center_horizontal&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_weight=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;0&amp;quot;&lt;/span&gt;&lt;span class=&quot;nt&quot;&gt;/&amp;gt;&lt;/span&gt;

  &lt;span class=&quot;nt&quot;&gt;&amp;lt;TextView&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_width=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;match_parent&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_height=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;wrap_content&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:textAppearance=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;?android:attr/textAppearanceLarge&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:text=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;Large Text&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:id=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;@+id/textView&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_gravity=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;center_horizontal&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_weight=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;1&amp;quot;&lt;/span&gt;&lt;span class=&quot;nt&quot;&gt;/&amp;gt;&lt;/span&gt;

  &lt;span class=&quot;nt&quot;&gt;&amp;lt;Button&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_width=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;match_parent&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_height=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;wrap_content&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:text=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;New Button&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:id=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;@+id/button&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_gravity=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;center_horizontal&amp;quot;&lt;/span&gt;
    &lt;span class=&quot;na&quot;&gt;android:layout_weight=&lt;/span&gt;&lt;span class=&quot;s&quot;&gt;&amp;quot;0&amp;quot;&lt;/span&gt;&lt;span class=&quot;nt&quot;&gt;/&amp;gt;&lt;/span&gt;

&lt;span class=&quot;nt&quot;&gt;&amp;lt;/LinearLayout&amp;gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;The EditText and Button Views are now pushed to the top and bottom and the TextView stretched its height to fill up the gap left by other two Views. Because only the TextView is assigned a weight more than zero, it is the only View that can grow beyond what it needs for display. &lt;/p&gt;

&lt;p&gt;Had we assigned all three Views equal weights of 1, all of them will grow in space beyond what they need but they will grow uniformly. The whole screen will be divided by three components. If we assigned each of them a weight of 2, you will not see any difference when they all had a weight of 1. Interesting things happen Views are assigned varying weights.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/different-weights.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>Android Project</title>
   <link href="http://tedhagos.com/android/AS-android-project.html"/>
   <updated>2012-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/AS-android-project</id>
   <content type="html">&lt;p&gt;An Android project is required to build a single app. A project is a simple folder that contains a collection of XML, Java source, images, property and optional audio or video files. It has a very specific structure that stays the same for all android projects. With the exception of images, audio and video files which may be used by your project, everything inside this project are plain text files. This is the reason why some programmers can develop android applications using just plain editors.&lt;/p&gt;

&lt;p&gt;The commands and utilities inside the Android SDK expects this structure and is optimized to work with it. If you deviate from this structure, you will have grief. It is not wise to build this project structure unaided by tools. At the very least, you should use the &lt;strong&gt;android create project&lt;/strong&gt; utility included in the SDK. &lt;/p&gt;

&lt;p&gt;Popular IDEs like Eclipse, NetBeans and Android Studio have wizard driven ways of creating an android project. But no matter what you use to create such a project, the structure of the files and folders is pretty uniform. &lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/android-project-structure.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;p&gt;The one on the left is a project created using just the SDK. The picture on the right was created using the Android App Studio, a full blown IDE for Android development. While the Android Studio project appears to have more files, the basic structure of the two projects remains very consistent. &lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;src:&lt;/strong&gt; a folder that contains the Java source files. All your program logic will be written on these files. If you want to do something when a user clicks a Button, starts you application or suspends an application, you will write them on the source files. And these source files will be stored here&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;res:&lt;/strong&gt; a folder that contains other folders like “res”, “values” and some images. The folder names that says “drawable” contains images that your application may use&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;bin:&lt;/strong&gt; When you compile the project, this is where the binary or executable files will be stored&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;libs:&lt;/strong&gt; Sometimes you will need to use codes that other developers built. These third party libraries should be located on this folder&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;layout:&lt;/strong&gt; Android favors the declarative way of defining user interfaces. The screens you will create can be built using XML files. This is the folder where XML screen files are stored&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;values:&lt;/strong&gt; Hard coding string values is a dangerous practice. The idiomatic way of defining strings for labels, buttons etc is to store them in resource bundles. These resource bundles are nothing more than XML files. You will store the resource bundles in this folder&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;AndroidManifest.xml:&lt;/strong&gt; Every project needs one manifest file. This file is important. It declares the things that are inside your application like how many screens it have, what their names are. It also affects the things your application can do. It declares whether your application can use the network, the local storage or the camera for example&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;local.properties:&lt;/strong&gt; This file contains build configuration information e.g. the location of the Android SDK in your system&lt;/li&gt;
&lt;/ul&gt;
</content>
 </entry>
 
 <entry>
   <title>Activities</title>
   <link href="http://tedhagos.com/android/AS-activities.html"/>
   <updated>2012-10-20T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/AS-activities</id>
   <content type="html">&lt;p&gt;An Activity is single screen that a user sees in your application. They are the primary ways for xyour application to communicate with the user. You will capture inputs with them and you will show outputs using them. If you  built Java applications for the desktop before, you might think of them as AWT Frames. &lt;/p&gt;

&lt;p&gt;An activity is made up of two files. An XML file which defines what the screen looks like. How many buttons, menus and textfields it has etc. The second one is a Java source file which contains all the programming logic that will support the behavior of the screen. If you want something to happen when a user does something to the screen e.g. inputs a text or clicks a button, you will write that program logic on the Java source file.&lt;/p&gt;

&lt;p&gt;We will create another Hello project in this chapter&lt;/p&gt;

&lt;p&gt;Create another project with the following details.&lt;/p&gt;

&lt;ol&gt;
  &lt;li&gt;&lt;strong&gt;Application Name&lt;/strong&gt; is Hello&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Minimum SDK&lt;/strong&gt; is API 15&lt;/li&gt;
  &lt;li&gt;Use a &lt;strong&gt;Blank Activity&lt;/strong&gt;&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;Activity Name&lt;/strong&gt; is Main&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;the-xml-file&quot;&gt;The XML file&lt;/h2&gt;

&lt;p&gt;The XML file for an Activity will be stored in &lt;strong&gt;Hello/app/src/main/res/layout&lt;/strong&gt; folder. There will be one XML file for each Activity. If you only have one Activity in your application, then there will only be one XML file in this folder. If you have several screens in the app, there will be several XML files here, one for each screen. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;lt;RelativeLayout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;
    xmlns:tools=&quot;http://schemas.android.com/tools&quot;
    android:layout_width=&quot;match_parent&quot;
    android:layout_height=&quot;match_parent&quot;
    android:paddingLeft=&quot;@dimen/activity_horizontal_margin&quot;
    android:paddingRight=&quot;@dimen/activity_horizontal_margin&quot;
    android:paddingTop=&quot;@dimen/activity_vertical_margin&quot;
    android:paddingBottom=&quot;@dimen/activity_vertical_margin&quot;
    tools:context=&quot;.Main&quot;&amp;gt;

    &amp;lt;TextView
        android:text=&quot;@string/hello_world&quot;
        android:layout_width=&quot;wrap_content&quot;
        android:layout_height=&quot;wrap_content&quot; /&amp;gt;

&amp;lt;/RelativeLayout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&quot;java-source-file&quot;&gt;Java source file&lt;/h2&gt;

&lt;p&gt;This is just filler text. Delete this at once, once you find it&lt;/p&gt;

&lt;p&gt;What is it
Why do I care
What is it made of&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;Java file. What, where is it. how di we get it.what’s in it, what are they for&lt;/li&gt;
  &lt;li&gt;Layout file. What. Where is it. How did we gert it. What’s in it&lt;/li&gt;
  &lt;li&gt;how are the two related. Via setcontentView nad R.layout.main – what’s that. Well the appt (Android Asset Packaging Tool) will compile all layout and values XML. It will produce the the R clas which you can use to get a programmatic reference to Views and resource bundles you have defined.&lt;/li&gt;
&lt;/ul&gt;

&lt;h2 id=&quot;multiple-activities&quot;&gt;Multiple Activities&lt;/h2&gt;

&lt;h1 id=&quot;scratch&quot;&gt;scratch&lt;/h1&gt;

&lt;p&gt;An Activity is a screen. It is the visible part of the Android application. &lt;/p&gt;

&lt;p&gt;What is it made of - a java file and an XML. It describes in XML what the screen should look like, will it have buttons, text fields etc. Will the Buttons stretch vertically or horizontally etc&lt;/p&gt;

&lt;p&gt;What about it, what do they need to know&lt;/p&gt;

&lt;p&gt;Create it. which is covered already but recapitulate it
Point out min sdk
package
name of Main Activity&lt;/p&gt;

&lt;p&gt;Show the project structure again&lt;/p&gt;

&lt;p&gt;What is Main.java
The visual content, is referencing R.layout.main 
What does setContentView does. Where did we get it
What does it mean that it extends androiv.view.Activity&lt;/p&gt;

&lt;p&gt;Manifest&lt;/p&gt;

&lt;p&gt;activity declaration
  intent filter, why is it important. It tells the Android runtime, that this is the activity that should be launched when the application is launched by the user. When you launch me, launch this activity first.&lt;/p&gt;

&lt;p&gt;What about another activity.
  how to add another activity&lt;/p&gt;

&lt;p&gt;What to put
 onCreate, why does it have super.onCreate(), then setContent again
 create another XML file, how in Android Studio. will the name of the XMLs case sensitive&lt;/p&gt;

&lt;p&gt;How to put some things inside the screen file&lt;/p&gt;

&lt;p&gt;Simple way to launch another Activity, use the XML for declaration&lt;/p&gt;

&lt;p&gt;Why do we have add the second activity into the Manifest, because it will throw an ActivityNotFoundException - How did we know. Because I launched the DDMS. I need to tell them about the DDMS&lt;/p&gt;

&lt;p&gt;What happens if you omit the setContentView of the second Activity, well, you will launch the second activity but you won’t see anything&lt;/p&gt;

&lt;p&gt;This needs a video supplement&lt;/p&gt;

&lt;p&gt;Summary&lt;/p&gt;

&lt;p&gt;AndroidManifest controls everything
What it takes to add a new activity, create a Java file, create an XML, define in manifest and how to launch another activity is to use Intents, Excplicit intents&lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>Getting Started</title>
   <link href="http://tedhagos.com/android/GettingStarted.html"/>
   <updated>2012-10-16T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/GettingStarted</id>
   <content type="html">&lt;p&gt;An Android project is made up of lots of files. Even the humble Hello World app, which is what we will do, requires more than just a single Java source file. An Android app needs a couple of XML files to facilitate user interfaces and overall configuration. Obviously it will need at least one Java program file and it could contain a couple of other resource files e.g. images etc.&lt;/p&gt;

&lt;p&gt;While you can create and assemble an Android application without the aid of a tool, it can be difficult and cumbersome, not to mention, quite impractical. The Android SDK contains tools that helps to create, build and test projects. &lt;/p&gt;

&lt;p&gt;At this point, you must already have a properly configured development environment. If you haven’t setup your environment, you can refer to &lt;a href=&quot;android-environment-setup.md&quot;&gt;Android Environment Setup&lt;/a&gt; before going any further.&lt;/p&gt;

&lt;h1 id=&quot;creating-a-project&quot;&gt;Creating a project&lt;/h1&gt;

&lt;p&gt;We need to create a folder or directory where we can put all our project files. We will give that folder a name that is descriptive of the project. You can put this folder anywhere you like but there are some locations in your hard drive that might not be suited for a project like ours. Normally these places are folders where you don’t have read, write and execute privileges (rwx). You  have &lt;strong&gt;rwx&lt;/strong&gt; capabilities on your home folder, so we wil put our project in there. The location of your home folder varies depending on what OS you use. &lt;/p&gt;

&lt;table border=&quot;2&quot; cellspacing=&quot;0&quot; cellpadding=&quot;6&quot; rules=&quot;groups&quot; frame=&quot;hsides&quot;&gt;


&lt;colgroup&gt;
&lt;col class=&quot;left&quot; /&gt;

&lt;col class=&quot;left&quot; /&gt;
&lt;/colgroup&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;Windows 7 or 8&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;`C:\Users\YourName`&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;Linux&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;`/home/YourName/` or simply `~/`&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;left&quot;&gt;OSX&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;`/Users/YourName` or simply `~/`&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;&lt;code&gt;android create project --target 16 --path hello --activity HelloWorld --package com.tedhagos&lt;/code&gt;&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;–target 16:&lt;/strong&gt; &lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;–path hello :&lt;/strong&gt; &lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;–activity HelloWorld:&lt;/strong&gt; &lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;–package:&lt;/strong&gt; &lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;What will you have&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;../images/android-project-structure.png&quot; alt=&quot;img&quot; /&gt;&lt;/p&gt;

&lt;h1 id=&quot;compiling&quot;&gt;Compiling&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;ant debug install&lt;/code&gt; &lt;/p&gt;

&lt;p&gt;Why debug, what happens if I remove it.&lt;/p&gt;

&lt;p&gt;Are there any other  targets&lt;/p&gt;

&lt;h1 id=&quot;how-will-you-test&quot;&gt;How will you test&lt;/h1&gt;

&lt;h2 id=&quot;avd&quot;&gt;AVD&lt;/h2&gt;

&lt;h2 id=&quot;physical-device&quot;&gt;Physical device&lt;/h2&gt;

&lt;p&gt;What are the insights, have you answered the description above. Can Steph understand this&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Emulator</title>
   <link href="http://tedhagos.com/android/Emulator.html"/>
   <updated>2012-10-16T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/Emulator</id>
   <content type="html">&lt;h1 id=&quot;what-is-it&quot;&gt;What is it&lt;/h1&gt;

&lt;h1 id=&quot;what-is-it-good-for&quot;&gt;What is it good for&lt;/h1&gt;

&lt;h1 id=&quot;shortcomings&quot;&gt;Shortcomings&lt;/h1&gt;

&lt;h1 id=&quot;advantage-over-a-real-device&quot;&gt;Advantage over a real device&lt;/h1&gt;

&lt;h1 id=&quot;creation-and-use&quot;&gt;Creation and use&lt;/h1&gt;

&lt;h1 id=&quot;intel-haxm&quot;&gt;Intel HAXM&lt;/h1&gt;

&lt;p&gt;Hardware Accelerated Execution Manager. It’s a hypervisor (hardware assisted virtualization) that uses Intel VT. You use it to speed up the Android emulation on your machine. it allows for faster android emulation&lt;/p&gt;

&lt;p&gt;There are two ways to get it, one is by downloading it from the intel site &lt;a href=&quot;https://software.intel.com/en-us/android&quot;&gt;https://software.intel.com/en-us/android&lt;/a&gt; the other way is to download it using the Android SDK Manager. Under Extras, you will see the Intel x86 emulation installer (HAXM). Note that even if you download it using the SDK manager, it will not install the HAXM automatically. You still have to run the the HAXM installer. The installation instruction for HAXM is at &lt;a href=&quot;https://software.intel.com/en-us/android/articles/installation-instructions-for-intel-hardware-accelerated-execution-manager-mac-os-x&quot;&gt;https://software.intel.com/en-us/android/articles/installation-instructions-for-intel-hardware-accelerated-execution-manager-mac-os-x&lt;/a&gt;&lt;/p&gt;

&lt;h2 id=&quot;managing-the-haxm&quot;&gt;Managing the HAXM&lt;/h2&gt;

&lt;p&gt;To verify that its running&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;kextstat | grep intel
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;You should see the kernel extension “com.intel.kext.intelhaxm”&lt;/p&gt;

&lt;p&gt;To stop the haxm&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;sudo kextunload -b com.intel.kext.intelhaxm
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;To start the haxm&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;sudo kextload -b com.intel.kext.intelhaxm
&lt;/code&gt;&lt;/pre&gt;
</content>
 </entry>
 
 <entry>
   <title>Cannot Find Build.xml</title>
   <link href="http://tedhagos.com/android/CannotFindBuildXml.html"/>
   <updated>2012-10-16T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/CannotFindBuildXml</id>
   <content type="html">&lt;p&gt;After running &lt;code&gt;ant debug install&lt;/code&gt;, an error happens. &lt;/p&gt;

&lt;pre&gt;&lt;code&gt;BUILD FAILED
/Users/ted/workarea/android-training/practice
/helloworld/    build.xml:83: Cannot find /Users/ted/
progtools/android-sdk-macos/tools/ant/build.xml 
imported from /Users/ted/workarea/android-training/
practice/helloworld/build.xml

Total time: 0 seconds
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;This is a build script problem. What the error tells us is that, it cannot find &lt;strong&gt;build.xml&lt;/strong&gt;. How can that be? The android SDK tools actually setup up everything for us, the ant build script is one of the artefacts created when you start a new android project. There could be lots of reasons, but this particular error happened to me because I was using two machines for program development. And that I forgot to include the &lt;strong&gt;local.properties&lt;/strong&gt; in the .gitignore file. So the settings and properties on one machine spilled over to the other development machine. &lt;/p&gt;

&lt;p&gt;If you inspect the build.xml in your android project folder, you will find out that it is actually importing another &lt;strong&gt;build.xml&lt;/strong&gt;, a generic one. This generic build.xml is located at &lt;code&gt;SDK.DIR/tools/build.xml&lt;/code&gt;. SDK.DIR is the folder where you installed the Android SDK when you downloaded it. &lt;/p&gt;

&lt;p&gt;The following example downloads and installs the android SDK&lt;/p&gt;

&lt;div class=&quot;highlight&quot;&gt;&lt;pre&gt;&lt;code class=&quot;language-bash&quot; data-lang=&quot;bash&quot;&gt;&lt;span class=&quot;nv&quot;&gt;$ &lt;/span&gt;mkdir ~/progtools
&lt;span class=&quot;nv&quot;&gt;$ &lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;cd &lt;/span&gt;progtools
&lt;span class=&quot;nv&quot;&gt;$ &lt;/span&gt;wget http://dl.google.com/android/android-sdk-linux.tgz
&lt;span class=&quot;nv&quot;&gt;$ &lt;/span&gt;tar -xzvf android-sdk-linux.tgz&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;

&lt;p&gt;This will create a folder named &lt;code&gt;~/progtools/android-sdk-linux/&lt;/code&gt;. This is the SDK.DIR&lt;/p&gt;

&lt;p&gt;To fix this error, you need to make sure that the value of &lt;strong&gt;SDK.DIR&lt;/strong&gt; inside the local.properties file is pointing to the correct location. The local.properties file is located on the top level folder of your android project. &lt;/p&gt;

</content>
 </entry>
 
 <entry>
   <title>android device camera</title>
   <link href="http://tedhagos.com/android/AS-simple-camera.html"/>
   <updated>2012-10-16T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/AS-simple-camera</id>
   <content type="html">&lt;pre&gt;&lt;code&gt;package com.thelogbox;

import android.app.Activity;
import android.os.Bundle;
import android.view.View;
import android.view.View.OnClickListener;
import android.content.Context;
import android.widget.LinearLayout;
import android.hardware.Camera;
import android.hardware.Camera.ShutterCallback;
import android.hardware.Camera.PictureCallback;
import android.view.SurfaceView;
import android.util.Log;
import java.io.FileOutputStream;
import java.io.IOException;
import java.io.FileNotFoundException;
import android.os.Handler;


class VSimpleCamera extends LinearLayout implements OnClickListener {

  private String TAG = null;
  private SurfaceView surfaceview = null;
  private Camera camera = null;
  private FileOutputStream out = null;

  public VSimpleCamera(Context ctx){
    super(ctx);

    TAG = getClass().getName();
    surfaceview = new SurfaceView(ctx);
    camera = Camera.open();

    setOnClickListener(this);


    Handler handler = new Handler();
    handler.postDelayed(new Runnable(){
      public void run() {
        Log.v(TAG, &quot;Runnable is running&quot;);

        //You can put the camera hooba jub here

      }
    }, 3000);

  }

  public void onClick(View v){

    boolean cameratest = (camera == null);
    Log.v(TAG, &quot;Clicked&quot;);

    try {
      camera.setPreviewDisplay(surfaceview.getHolder());
      camera.startPreview();
      camera.takePicture(shutterCallBack, rawCallBack, jpgCallBack);
    }
    catch(Exception e){
      Log.v(TAG, &quot;Exception &quot; + e.toString());
    }
  }

  ShutterCallback shutterCallBack = new ShutterCallback() {
    public void onShutter() {
      Log.v(TAG, &quot;shutter call&quot;);
    }
  };

  PictureCallback rawCallBack = new PictureCallback() {
    public void onPictureTaken(byte[] data, Camera cam){
      Log.v(TAG, &quot;raw Call back&quot;);
    }
  };

  PictureCallback jpgCallBack = new PictureCallback() {
    public void onPictureTaken(byte[] data, Camera cam){

      try {
        out = new FileOutputStream(String.format(&quot;/sdcard/%d.jpg&quot;, System.currentTimeMillis()));
        out.write(data);
      }
      catch(FileNotFoundException fe) {
        Log.v(TAG, fe.getMessage());
      }
      catch(IOException ioe) {
        Log.v(TAG,ioe.getMessage());
      }

    Log.v(TAG, &quot;jpg Call Back&quot;);
    }
  };

}

public class SimpleCamera extends Activity {

  @Override
  public void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);
    setContentView(new VSimpleCamera(this));
  }
}

&lt;/code&gt;&lt;/pre&gt;
</content>
 </entry>
 
 <entry>
   <title>Network Connectivity</title>
   <link href="http://tedhagos.com/android/AS-network-connectivity.html"/>
   <updated>2012-10-16T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/AS-network-connectivity</id>
   <content type="html">&lt;pre&gt;&lt;code&gt;package com.thelogbox;

import android.app.Activity;
import android.os.Bundle;
import android.content.Context;
import android.widget.LinearLayout;
import android.widget.TextView;
import android.view.View;
import android.view.View.OnClickListener;
import android.net.ConnectivityManager;
import android.net.NetworkInfo;

class ViewConnnTest extends LinearLayout implements OnClickListener {

  Context context = null;
  TextView tv = null;
  ConnectivityManager connman = null;
  NetworkInfo netinfo = null;

  public ViewConnnTest(Context context) {

    super(context);
    this.context = context;
    tv = new TextView(context);
    addView(tv);
    setOnClickListener(this);

  }

  public void onClick(View v){

    StringBuilder sb = new StringBuilder();
    connman = (ConnectivityManager) context.getSystemService(Context.CONNECTIVITY_SERVICE);
    netinfo = connman.getActiveNetworkInfo();

    switch(netinfo.getType()) {
      case ConnectivityManager.TYPE_MOBILE:
        sb.append(&quot;\nMobile data OK.&quot;);
        break;
      case ConnectivityManager.TYPE_WIFI:
        sb.append(&quot;\nWIFI OK.&quot;);
        break;
      case ConnectivityManager.TYPE_WIMAX:
        sb.append(&quot;\nWIMAX OK.&quot;);
        break;
      case ConnectivityManager.TYPE_ETHERNET:
        sb.append(&quot;\nOn Ethernet.&quot;);
        break;
      case ConnectivityManager.TYPE_BLUETOOTH:
        sb.append(&quot;\nBluetooth OK.&quot;);
        break;
      default:
        sb.append(&quot;\nNot connected. Not OK.&quot;);
    }
    sb.append(&quot;\nState= &quot;);
    sb.append(netinfo.getState().toString());

    tv.setText(sb.toString());
  }
}

public class ConnTest extends Activity {
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(new ViewConnnTest(this));
    }
}
&lt;/code&gt;&lt;/pre&gt;
</content>
 </entry>
 
 <entry>
   <title>Key Concepts</title>
   <link href="http://tedhagos.com/android/AS-key-concepts.html"/>
   <updated>2012-10-16T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/AS-key-concepts</id>
   <content type="html">&lt;p&gt;Android applications are comprised of Java executable codes, XML files and other application resources like video, images and audio. These are all bundled together inside an .apk file.&lt;/p&gt;

&lt;p&gt;You will write your program in the Java programming language and compile them using the  Android SDK (software development kit). When the app is compiled, it becomes an android package. An android package is an archive file with a .apk extension. Think of them like zipped files for Android.&lt;/p&gt;

&lt;p&gt;When applications have been built and digitally signed, they can be distributed on market places like Google Play or Amazon. They don’t have to be but these market places are frequented by a lot of people, your can benefit from exposure which the market places can offer. &lt;/p&gt;

&lt;h2 id=&quot;architecture&quot;&gt;Architecture&lt;/h2&gt;

&lt;p&gt;Android may refer to many things, some people use it to refer to their mobile device and some others use the term to refer to the programming environment. That’s fine. For our purpose, what we mean by Android is a platform which have runtime components where mobile apps can run.&lt;/p&gt;

&lt;p&gt;This platform is special version of Linux that is optimized for mobile devices. The Linux part takes care system services e.g. network and file operations, concurrency etc. The platform includes a bunch of other
things like WebKit which is the same browser engine that powers Chrome and Safari. It also has SQLite which you can use for database programming. It has other components useful for application development, but instead of listing all of them here, I will point them out in later when we need to use them.&lt;/p&gt;

&lt;p&gt;The one other thing to point out in the Android software stack is the framework. The framework lets us do things such as create screens, write code that runs in background, listen to some events that may happen on the mobile device (and do something about it). The platform enables us to do all these things by simply extending existing classes in the
Android SDK.&lt;/p&gt;

&lt;p&gt;Extending classes means utilizing the inheritance mechanism of Java to reuse existing functionality. The Android development team already wrote codes to display buttons and textfields for example. All we need to do, is to reuse them, mash them up or mix it up so we can use them in our own applications.&lt;/p&gt;

&lt;p&gt;The Android SDK is a collection of tools and code libraries. SDK is short for software development kit. You will need to download, install and configure the SDK before you can start any development work.&lt;/p&gt;

&lt;h2 id=&quot;versions-and-api-level&quot;&gt;Versions and API level&lt;/h2&gt;

&lt;p&gt;Android has been around for quite some time. It has evolved so many times and as a result, there are various versions of it.&lt;/p&gt;

&lt;p&gt;A version number will correspond to a code name. Buyers of Android devices would probably recognize the android versions using the codename rather than the actual number. As a developer, we need to be familiar not only with both the version number and codename, we also need to know the API level.&lt;/p&gt;

&lt;p&gt;The API level is what you will see on the Android SDK manager. You will use the SDK manager when updating your software development kit.&lt;/p&gt;

&lt;table border=&quot;2&quot; cellspacing=&quot;0&quot; cellpadding=&quot;6&quot; rules=&quot;groups&quot; frame=&quot;hsides&quot;&gt;


&lt;colgroup&gt;
&lt;col class=&quot;right&quot; /&gt;

&lt;col class=&quot;left&quot; /&gt;

&lt;col class=&quot;right&quot; /&gt;
&lt;/colgroup&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th scope=&quot;col&quot; class=&quot;right&quot;&gt;Version&lt;/th&gt;
&lt;th scope=&quot;col&quot; class=&quot;left&quot;&gt;Codename&lt;/th&gt;
&lt;th scope=&quot;col&quot; class=&quot;right&quot;&gt;API&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;

&lt;tbody&gt;
&lt;tr&gt;
&lt;td class=&quot;right&quot;&gt;2.2&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Froyo&lt;/td&gt;
&lt;td class=&quot;right&quot;&gt;8&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;right&quot;&gt;2.3.3 - 2.3.7&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Gingerbread&lt;/td&gt;
&lt;td class=&quot;right&quot;&gt;10&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;right&quot;&gt;4.0.3 - 4.0.4&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Ice Cream Sandwich&lt;/td&gt;
&lt;td class=&quot;right&quot;&gt;15&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;right&quot;&gt;4.1.x&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Jelly Bean&lt;/td&gt;
&lt;td class=&quot;right&quot;&gt;16&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;right&quot;&gt;4.2.x&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Jelly Bean&lt;/td&gt;
&lt;td class=&quot;right&quot;&gt;17&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;right&quot;&gt;4.3&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;Jelly Bean&lt;/td&gt;
&lt;td class=&quot;right&quot;&gt;18&lt;/td&gt;
&lt;/tr&gt;


&lt;tr&gt;
&lt;td class=&quot;right&quot;&gt;4.4&lt;/td&gt;
&lt;td class=&quot;left&quot;&gt;KitKat&lt;/td&gt;
&lt;td class=&quot;right&quot;&gt;19&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;By the time you read this, there could be changes or additions to the Android platform. The official site of Android keeps an updated list of this info over at &lt;a href=&quot;https://developer.android.com/about/dashboards/index.html&quot;&gt;https://developer.android.com/about/dashboards/index.html&lt;/a&gt;. &lt;/p&gt;

&lt;h2 id=&quot;fundamentals&quot;&gt;Fundamentals&lt;/h2&gt;

&lt;ol&gt;
  &lt;li&gt;Creation of an app starts by creating an Android project. It is a simple folder that contains the Java sources, XML files, videos, audio, images etc. Depending on what IDE or tool you use, a project can be created and managed using either Ant, Maven or Gradle. These are called build tools and they are usually managed by the IDE. Most of the time, they are invisible to you&lt;/li&gt;
  &lt;li&gt;Write the program logic using Java. Whether you would like to show something on the screen, listen to an event or do something in the background, Android has prebuilt classes to handle those. These prebuilt classes are part of the Android framework that lets us create rich apps by extending exisiting classes in the framework. We’ll discuss Application components a bit more later&lt;/li&gt;
  &lt;li&gt;The screen user interface is, usually, constructed using XML. Widgets like buttons, textfields and other UI components are declared in the XML. Things such as size, alignments and weights of each component can be declared in this file as well&lt;/li&gt;
  &lt;li&gt;Most of apps are event driven. They wait for the user to enter or press something then then some processing happens. Program logic is usually written as response to an event. These codes are the ones you write using the Java language&lt;/li&gt;
  &lt;li&gt;The Java source file are converted to an executable form and the XMl is flattened as a result of the compilation process. The XML is converted to byte code representation, this is what we mean by flattened&lt;/li&gt;
  &lt;li&gt;If the compilation is not successful, the compiler will you what’s wrong with it and you should fix it then compile again. If it compiles successfully, you will test the application by deploying it either an emulator or a real device connected to your computer&lt;/li&gt;
&lt;/ol&gt;

&lt;h2 id=&quot;application-components&quot;&gt;Application Components&lt;/h2&gt;

&lt;h3 id=&quot;activities&quot;&gt;Activities&lt;/h3&gt;

&lt;p&gt;If you want to create a screen that can interact with the user, you can do that by creating an Activity class. It is called that because the class you will create will extend the existing Activity class that is part of the Android framework.&lt;/p&gt;

&lt;p&gt;By extending this class, you will be able to create other components such as buttons, textfields, lists etc and contain them inside a screen. You can also react to some events such as when an incoming call interrupts the user while using your application. &lt;/p&gt;

&lt;p&gt;An Activity is basically a single screen that a user can see and interact with. Most applications will have more than one Activity. They have more than one screen. You can stitch Activities together using &lt;strong&gt;Intents&lt;/strong&gt;. You can define a button on one Activity then write program logic such that when the button is clicked, it will launch another Activity. You will use Intents to launch other Activities.  &lt;/p&gt;

&lt;h3 id=&quot;services&quot;&gt;Services&lt;/h3&gt;

&lt;p&gt;Sometimes you will need to write code that is invisible to the user. One that doesn’t have a user interface. Surely you will need to launch it somehow from an Activity but once launched, the application just keeps on running. Even if the UI that launched it has already disappeared from view. A music player is such an example of this kind of app. A GPS enabled application which updates your location every now and then is another example.&lt;/p&gt;

&lt;p&gt;If you need to build these kinds of apps, you will use &lt;strong&gt;Services&lt;/strong&gt;. These are long running operations that are executed in the background. A Service is independent of the Activity that launched. Even if the UI screen that was used to launch the service has already died, the Service code just keeps on going.&lt;/p&gt;

&lt;h3 id=&quot;content-providers&quot;&gt;Content Providers&lt;/h3&gt;

&lt;p&gt;Each application runs on its own process. Its own virtual machine. This behavior of Android protects each application. If one badly written application goes wonky, it cannot bring the other running applications down. Its good for application stability. But this makes it nearly impossible for one application to access data from another. &lt;/p&gt;

&lt;p&gt;Content Providers solves the problem of data sharing between applications. It is possible for you to write an application and share whatever data it has with other applications. The &lt;strong&gt;Contacts&lt;/strong&gt; and &lt;strong&gt;Calendar&lt;/strong&gt; apps in Android are good examples of apps that have Content Providers.&lt;/p&gt;

&lt;p&gt;Don’t confuse Content Provider with your own database. If you create an application that uses a SQLite database, of course your application can access that. If, for example, you want to allow other applications to gain access to your app’s data, you can build a Content Provider. Your application will make the data available to other apps using standard URIs. &lt;/p&gt;

&lt;h3 id=&quot;broadcast-receivers&quot;&gt;Broadcast Receivers&lt;/h3&gt;

&lt;p&gt;Broadcast Receivers are used if you want to execute some program logic in your app as a response to events generated by either the Android system or other applications. You can, for example, inititate a database write when the phone receives an SMS message. You probably want to examine the SMS message and if it fits a certain criteria, you will record it to the database. This is one example of how to use Broadcast receivers.  &lt;/p&gt;

&lt;p&gt;You can make your application listen to certain events. To do this, you need to register your application to listen to a specific event. Its the same concept when you subcribe to a mailing list. When there is a new mail, you get notified.&lt;/p&gt;

&lt;p&gt;Apart from listening to broadcasted events, you can also make your application broadcast specific events. To do that, you will extend a specific Android class called the &lt;strong&gt;BroadcastReceiver&lt;/strong&gt;.&lt;/p&gt;

&lt;p&gt;BroadcastReceivers typically don’t have user interfaces. But you can create notifications that will show up on the status bar.&lt;/p&gt;

&lt;h3 id=&quot;android-manifest-file&quot;&gt;Android Manifest file&lt;/h3&gt;

&lt;p&gt;This is one of the most important parts of an Android app. The manifest is an XML file that needs be on the root folder of the application project. Every project needs one, and there can only be one per project. It needs to be named &lt;strong&gt;AndroidManifest.xml&lt;/strong&gt;. The manifest declares lots of things like:&lt;/p&gt;

&lt;p&gt;-the name of the application
-what kinds of components does the app have. Does it have Activities, what are their names. Does it listen to broadcast etc
-what kinds of things can it do with the mobile device? Can it access the network. The internet. Will it use the camera. Will it record a GPS location
-can other applications interact with this applcation. If so, what kinds of permission should these other applications have
-does this application use any external libraries (usually jar files that other people wrote)
-what versions of Android will this application run on. Will it run on Froyo (API level 8)&lt;/p&gt;

&lt;p&gt;The Android manifest file is usually created by default when you start a new project. But it only provides some minimum declaration. Beyond the basic HelloWorld apps, you will need to learn how to work and manage the manifest file.&lt;/p&gt;

&lt;h3 id=&quot;other-resources&quot;&gt;Other Resources&lt;/h3&gt;

&lt;p&gt;Besides the programming components and the Android manifest file, your application may also include the following:&lt;/p&gt;

&lt;p&gt;-*XML resource files* . XMLs are everywhere in Android. They are used to define screen layouts, manifest file, resource bundles etc.
-*Image Resources*. At a minimum, your app needs an icon that will appear on the Android apps menu
-*Video and Audio  Resources* .If you are building a game or a training application, your apps may contain lots of these
-*Third Party Libraries*. You include codes that other people built e.g. social media functionalities. These are included in the project in the form of jar files (Java archive)&lt;/p&gt;

&lt;h2 id=&quot;other-ways-to-build-android-apps&quot;&gt;Other ways to build Android apps&lt;/h2&gt;

&lt;p&gt;There are other ways to build an Android app. You can build apps using using languages other than Java. An app can be built using HTML, CSS, JavaScript and a middleware like the Cordova API, a good example of this is Phonegap. You can use C# to write apps and retarget the compilation to Android using a product called Xamarin. There are ways to build Android apps as well using Ruby or Python.  &lt;/p&gt;

&lt;p&gt;There are tools or products that can build an Android app without coding or with minimal coding. MIT’s App Inventor is a example of this kind of tool. There are other tools like AppyPie, MakeMeDroid and Como. These app making tools are far too many to list down here. And by the time you are reading this, some of these names could be out of business already and new app makers have taken their places. Just search Google for “android app builder no code 2014” if you happen to still be reading this text in 2015, just substitute the current year in search term.&lt;/p&gt;

&lt;p&gt;Our focus in here, of course, is to write our app using the Java language and build it using the Android SDK.&lt;/p&gt;

&lt;div id=&quot;footnotes&quot;&gt;
&lt;h2 class=&quot;footnotes&quot;&gt;Footnotes: &lt;/h2&gt;
&lt;div id=&quot;text-footnotes&quot;&gt;

&lt;div class=&quot;footdef&quot;&gt;&lt;sup&gt;&lt;a id=&quot;fn.1&quot; name=&quot;fn.1&quot; class=&quot;footnum&quot; href=&quot;#fnr.1&quot;&gt;1&lt;/a&gt;&lt;/sup&gt; Uniform Resource Identifier is just a piece of string that is used to identify a resource, such as a data resource that you would like to export using a Content Provider. It can appear like this: content://yourapp/resourcename. It looks a lot like what you would type in a browser address bar, although technically, what you use in a browser is called a URL (Uniform Resource Locator). Becauase a URL specifies not only the name of the resource, it also specifies the manner on how to get the resource (http). But for all intents and purposes, a URL is also a URI.&lt;/div&gt;


&lt;/div&gt;
&lt;/div&gt;
</content>
 </entry>
 
 <entry>
   <title>Development Environment</title>
   <link href="http://tedhagos.com/android/AS-development-environment.html"/>
   <updated>2012-10-16T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/AS-development-environment</id>
   <content type="html">&lt;p&gt;Setting up a development rig for Android varies a bit depending on what tool you will use. We will use the Android App Studio but we will brush a little bit on instlaling the barebones SDK as well. You do not have to follow the barebones SDK install &lt;/p&gt;

&lt;p&gt;There are two different ways to install a dev environment, one is is via the pure SDK and using only the tools provided by the SDK  and some rudimentary programmign editor. The other way is using an IDE like Eclipse, Netbeans and Android App Studio&lt;/p&gt;

&lt;p&gt;The IDE is a monilithic way and the SDK is more involved. &lt;/p&gt;

&lt;p&gt;We will cover the Android App Studio. Why, because this is the tool endorsed by Google. While it is still in Beta, I don’t know, get some words from other authors&lt;/p&gt;

&lt;h1 id=&quot;sdk&quot;&gt;SDK&lt;/h1&gt;

&lt;p&gt;Screenshot of where to download it, and which link. &lt;/p&gt;

&lt;h2 id=&quot;windows&quot;&gt;Windows&lt;/h2&gt;

&lt;p&gt;double click, installer&lt;/p&gt;

&lt;h2 id=&quot;mac&quot;&gt;Mac&lt;/h2&gt;

&lt;p&gt;unzip
system path&lt;/p&gt;

&lt;h2 id=&quot;linux&quot;&gt;Linux&lt;/h2&gt;

&lt;p&gt;unzip
system path&lt;/p&gt;

&lt;h1 id=&quot;android-app-studio&quot;&gt;Android App Studio&lt;/h1&gt;

&lt;h2 id=&quot;windows-1&quot;&gt;Windows&lt;/h2&gt;

&lt;p&gt;double click&lt;/p&gt;

&lt;h2 id=&quot;mac-1&quot;&gt;Mac&lt;/h2&gt;

&lt;p&gt;double click, drag into /Applications&lt;/p&gt;

&lt;h2 id=&quot;linux-1&quot;&gt;Linux&lt;/h2&gt;

&lt;p&gt;unzip
include in system path&lt;/p&gt;
</content>
 </entry>
 
 <entry>
   <title>Workflow</title>
   <link href="http://tedhagos.com/android/Workflow.html"/>
   <updated>2012-07-09T00:00:00+08:00</updated>
   <id>http://tedhagos.com/android/Workflow</id>
   <content type="html">&lt;h1 id=&quot;project-creation&quot;&gt;Project creation&lt;/h1&gt;

&lt;p&gt;&lt;code&gt;android create project --path &amp;lt;project folder&amp;gt; --target &amp;lt;API level&amp;gt; --package &amp;lt;package name&amp;gt; --Activity &amp;lt;Activity name&amp;gt;&lt;/code&gt;&lt;/p&gt;

&lt;h1 id=&quot;work-on-manifestxml&quot;&gt;Work on Manifest.xml&lt;/h1&gt;

&lt;p&gt;If necessary, modify the &lt;strong&gt;*AndroidManifest.xml&lt;/strong&gt;. There will be certain applications that will require explicit permissions to be declared on manifest e.g. use of camera, use of the MIC for media recording etc&lt;/p&gt;

&lt;h1 id=&quot;get-the-project-in-source-control&quot;&gt;Get the project in source control&lt;/h1&gt;

&lt;p&gt;This is for bitbucket.org, adjust the url accordingly for github.com&lt;/p&gt;

&lt;pre class=&quot;codeblock&quot;&gt;

$ cd /path/to/your/project
$ git init
$ git remote add origin &amp;lt;https://yourname@bitbucket.org/yourname/yourproject.git&amp;gt;

&lt;/pre&gt;

&lt;h1 id=&quot;add-the-gitignore-and-readme-file&quot;&gt;Add the .gitignore and README file&lt;/h1&gt;

&lt;p&gt;cd to project folder, create a .gitignore file. You don’t want all of the files to be under source control e.g. location of the build.xml and the android-SDK, these information are specific to developer workstation and may not be true for all developers. Also, you don’t want generated files to be part of source control&lt;/p&gt;

&lt;p&gt;A sample content of .gitignore&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;bin/
gen/
ant.properties
local.properties
build.properties
project.properties
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Don’t forget the README.txt. Even if you are working alone, it can’t hurt to write down your thoughts why you were trying to write that piece of code in the first place, put it in the README.txt&lt;/p&gt;

&lt;h1 id=&quot;work-often-commit-often-iterate&quot;&gt;Work often, commit often. Iterate&lt;/h1&gt;

&lt;p&gt;Keep adding files to the project, keep modifying files. At the end of the day, commit your changes. Now, the frequency of the commit is usually decided as a group. There are projects where you cannot until certain number of unit tests have been performed (and passed) etc. Know your project practices and adhere to them, but the last thing you want is losing a days worth of coding/unit testing, so commit to the repo religiously&lt;/p&gt;

&lt;h1 id=&quot;license-the-source-file&quot;&gt;License the source file&lt;/h1&gt;

&lt;p&gt;You need to decide which license you will use. Remember, if there is no explicit license on the source file, it means its a full copyright. No one can use your code, they will have to contact you and ask for express permissions — not that everybody abides by that, but still, if you intend other people to benefit from your creation, then license it either using a permissive license (like MIT or BSD) or the GPL or what have you.&lt;/p&gt;

&lt;h1 id=&quot;debug-observe-inspect-adjust&quot;&gt;Debug, Observe, Inspect, Adjust&lt;/h1&gt;

&lt;p&gt;Learn how to type ant debug install really quickly, better yet, use a text expander so you have a fighting chance against carpal tunnel syndrome&lt;/p&gt;

&lt;h1 id=&quot;beta-test&quot;&gt;Beta test&lt;/h1&gt;

&lt;p&gt;Send the app to friends, relatives, colleagues etc, look for feedback. Go back to the app, refine. Let your thoughts germinate some more. You need to iterate a bit on the app before you release to the wild&lt;/p&gt;

&lt;h1 id=&quot;sign-the-app&quot;&gt;Sign the app&lt;/h1&gt;

&lt;p&gt;ant debug install was fine for testing and development, but it cannot be consumed by other people without the developer signature. You need to &lt;a href=&quot;/signing-an-android-app&quot;&gt;digitally sign your app&lt;/a&gt; before you release it to the public &lt;/p&gt;

&lt;h1 id=&quot;publish-to-google-play&quot;&gt;Publish to Google Play&lt;/h1&gt;

&lt;p&gt;You can make the app available from your website as a downloadable apk (android package) or you could pay Google Play so you can sell the app at the Google Market Place (its like iTunes or the Apple App Store for Android apps)&lt;/p&gt;
</content>
 </entry>
 
 
</feed>